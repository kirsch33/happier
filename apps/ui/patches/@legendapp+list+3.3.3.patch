diff --git a/node_modules/@legendapp/list/animated.d.ts b/node_modules/@legendapp/list/animated.d.ts
index 4b38f24..a47a6bd 100644
--- a/node_modules/@legendapp/list/animated.d.ts
+++ b/node_modules/@legendapp/list/animated.d.ts
@@ -199,6 +199,12 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Version token for the item size. Changing it invalidates only that item’s cached size.
+     * An observed `undefined` token is distinct from an item that has not been observed yet.
+     */
+    getItemSizeVersion?: (item: ItemT, index: number) => Key | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
@@ -492,6 +498,12 @@ interface MaintainScrollAtEndOptions {
      * Whether maintainScrollAtEnd should animate when it scrolls to the end.
      */
     animated?: boolean;
+    /**
+     * Reports an authoritative semantic end-follow intent. Event-driven maintenance
+     * remains active while this is true even when late measurement moved the
+     * physical viewport beyond the proximity threshold.
+     */
+    isMaintainingScrollAtEnd?: () => boolean;
     /**
      * Which events should keep the list pinned to the end.
      * - If omitted, object values default to all triggers.
@@ -547,6 +559,10 @@ type LegendListState = {
     startBuffered: number;
 };
 type LegendListRef$1 = {
+    /**
+     * Cancels retained initial-scroll correction authority without scrolling.
+     */
+    cancelInitialScrollPreservation(): void;
     /**
      * Clears internal virtualization caches.
      * @param options - Cache clearing options.
diff --git a/node_modules/@legendapp/list/react-native.d.ts b/node_modules/@legendapp/list/react-native.d.ts
index 8204015..00b30cf 100644
--- a/node_modules/@legendapp/list/react-native.d.ts
+++ b/node_modules/@legendapp/list/react-native.d.ts
@@ -199,6 +199,12 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Version token for the item size. Changing it invalidates only that item’s cached size.
+     * An observed `undefined` token is distinct from an item that has not been observed yet.
+     */
+    getItemSizeVersion?: (item: ItemT, index: number) => Key | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
@@ -492,6 +498,12 @@ interface MaintainScrollAtEndOptions {
      * Whether maintainScrollAtEnd should animate when it scrolls to the end.
      */
     animated?: boolean;
+    /**
+     * Reports an authoritative semantic end-follow intent. Event-driven maintenance
+     * remains active while this is true even when late measurement moved the
+     * physical viewport beyond the proximity threshold.
+     */
+    isMaintainingScrollAtEnd?: () => boolean;
     /**
      * Which events should keep the list pinned to the end.
      * - If omitted, object values default to all triggers.
@@ -547,6 +559,10 @@ type LegendListState$1 = {
     startBuffered: number;
 };
 type LegendListRef$1 = {
+    /**
+     * Cancels retained initial-scroll correction authority without scrolling.
+     */
+    cancelInitialScrollPreservation(): void;
     /**
      * Clears internal virtualization caches.
      * @param options - Cache clearing options.
diff --git a/node_modules/@legendapp/list/react-native.js b/node_modules/@legendapp/list/react-native.js
index 229f09a..b4b9d8f 100644
--- a/node_modules/@legendapp/list/react-native.js
+++ b/node_modules/@legendapp/list/react-native.js
@@ -902,7 +902,8 @@ function scheduleFullDrawDistancePrewarm(ctx) {
 // src/utils/setInitialRenderState.ts
 function resetInitialRenderState(ctx, {
   resetLayout,
-  resetInitialScroll
+  resetInitialScroll,
+  preserveReadyToRender
 }) {
   const { state } = ctx;
   if (resetLayout) {
@@ -912,7 +913,9 @@ function resetInitialRenderState(ctx, {
   if (resetInitialScroll) {
     state.didFinishInitialScroll = false;
   }
-  set$(ctx, "readyToRender", false);
+  if (!preserveReadyToRender) {
+    set$(ctx, "readyToRender", false);
+  }
   resetAdaptiveRender(ctx);
 }
 function setInitialRenderState(ctx, {
@@ -965,6 +968,34 @@ function clearPreservedInitialScrollTarget(state) {
   state.initialScroll = void 0;
   setInitialScrollSession(state);
 }
+function cancelInitialScrollPreservation(ctx) {
+  const state = ctx.state;
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTargetTimeout(state);
+  clearBootstrapInitialScrollSession(state);
+  const activeInitialScroll = state.scrollingTo;
+  if (activeInitialScroll == null ? void 0 : activeInitialScroll.isInitialScroll) {
+    if (state.animFrameCheckFinishedScroll !== void 0) {
+      cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+      state.animFrameCheckFinishedScroll = void 0;
+    }
+    if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+      clearTimeout(state.timeoutCheckFinishedScrollFallback);
+      state.timeoutCheckFinishedScrollFallback = void 0;
+    }
+    if (state.scrollingTo === activeInitialScroll) {
+      state.scrollingTo = void 0;
+      state.scrollTargetPinnedRange = void 0;
+    }
+  }
+  clearPreservedInitialScrollTarget(state);
+  clearInitialScrollSession(state);
+  setInitialRenderState(ctx, { didInitialScroll: true });
+  state.pendingMaintainScrollAtEnd = false;
+  if (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "pending-instant") {
+    state.maintainingScrollAtEnd = void 0;
+  }
+}
 function finishInitialScroll(ctx, options) {
   var _a3, _b, _c;
   const state = ctx.state;
@@ -1002,6 +1033,9 @@ function finishInitialScroll(ctx, options) {
       recalculateSettledScroll(ctx);
     }
     setInitialRenderState(ctx, { didInitialScroll: true });
+    if (state.pendingMaintainScrollAtEnd) {
+      doMaintainScrollAtEnd(ctx);
+    }
     if (shouldReleaseDeferredPublicOnScroll) {
       releaseDeferredPublicOnScroll(ctx, finalScrollOffset);
     }
@@ -1057,11 +1091,25 @@ function updateContentMetricsState(ctx) {
 }
 
 // src/core/addTotalSize.ts
+// HAPPIER-NULLKEY-ABSOLUTE-TOTAL (owner half): `addTotalSize` overloaded `key === null` as its
+// ABSOLUTE-WRITE sentinel ("this number IS the total"), and `getId` returns `null` for any index
+// past the end of `data` ("there is no such row"). Two meanings, one value. One probe of a row the
+// current `data` no longer contains therefore assigned the whole content size to be one unmeasured
+// row: getId -> null -> getItemSize falls through to `estimatedItemSize` -> setSize with a null key
+// -> addTotalSize with a null key -> totalSize = est, written absolutely. MEASURED on device with a
+// 4/4 identical stack (checkResetContainers -> calculateItemsInView -> prepareMVCP's closure ->
+// getItemSize(ctx, getId(state, scrollingTo.index), ...)): totalSize 2,322 -> 240 on a 43-row
+// transcript AND 266,020 -> 240 on a 419-row one - the SAME constant, i.e. one row rather than a
+// sum - then alignItemsAtEndPadding grown to 297 (12 + 142 + 240 + 297 = 691, the viewport, to the
+// pixel) and UIScrollView clamping contentOffset to that one-viewport content.
+// The sentinel is given a private value no item key can ever take, so the invalid state stops being
+// representable rather than being guarded against at one caller.
+var HAPPIER_TOTAL_SIZE_ABSOLUTE = { happierTotalSizeAbsolute: true };
 function addTotalSize(ctx, key, add, notifyTotalSize = true) {
   const state = ctx.state;
   const prevTotalSize = state.totalSize;
   let totalSize = state.totalSize;
-  if (key === null) {
+  if (key === HAPPIER_TOTAL_SIZE_ABSOLUTE) {
     totalSize = add;
     if (state.timeoutSetPaddingTop) {
       clearTimeout(state.timeoutSetPaddingTop);
@@ -1088,6 +1136,17 @@ function addTotalSize(ctx, key, add, notifyTotalSize = true) {
 
 // src/core/setSize.ts
 function setSize(ctx, itemKey, size, notifyTotalSize = true) {
+  // HAPPIER-NULLKEY-NO-ROW-NO-SIZE (choke-point half): `getId` returns `null` for an index past the
+  // end of `data`, and ~15 call sites hand a `getId` result straight to `getItemSize` with no null
+  // check (the MEASURED device stack goes through prepareMVCP's closure, NOT through
+  // `getItemSizeAtIndex`, so guarding that one caller would not have covered it). `setSize` is the
+  // single choke point every resolved size passes, so refusing a key that is not a row here keeps a
+  // non-existent row from contributing to the running total or occupying a `sizes` entry, for every
+  // present and future caller. Paired with HAPPIER-NULLKEY-ABSOLUTE-TOTAL: that one removes the
+  // ambiguity, this one removes the phantom contribution the disambiguation would otherwise leave.
+  if (itemKey === null) {
+    return;
+  }
   const state = ctx.state;
   const { sizes } = state;
   const previousSize = sizes.get(itemKey);
@@ -1143,11 +1202,58 @@ function findContainerId(ctx, key) {
 }
 
 // src/utils/getItemSize.ts
-function getKnownOrFixedSize(ctx, key, index, data, resolved) {
+function reverseItemSizeContribution(state, key) {
+  const contribution = state.sizeContributions.get(key);
+  if (contribution === void 0) {
+    return;
+  }
+  state.sizeContributions.delete(key);
+  const averages = state.averageSizes[contribution.itemType];
+  if (!averages) {
+    return;
+  }
+  if (averages.num <= 1) {
+    delete state.averageSizes[contribution.itemType];
+    return;
+  }
+  const remainingNum = averages.num - 1;
+  averages.avg = (averages.avg * averages.num - contribution.size) / remainingNum;
+  averages.num = remainingNum;
+}
+function validateItemSizeVersion(ctx, key, index, data) {
+  const state = ctx.state;
+  const getItemSizeVersion = state.props.getItemSizeVersion;
+  if (!getItemSizeVersion) {
+    return;
+  }
+  const itemSizeVersions = state.itemSizeVersions;
+  const nextVersion = getItemSizeVersion(data, index);
+  if (itemSizeVersions.has(key) && itemSizeVersions.get(key) !== nextVersion) {
+    reverseItemSizeContribution(state, key);
+    state.sizesKnown.delete(key);
+    const previouslyContributedSize = state.sizes.get(key);
+    if (previouslyContributedSize !== void 0) {
+      addTotalSize(ctx, key, -previouslyContributedSize, false);
+    }
+    state.sizes.delete(key);
+    const mountedContainerId = state.containerItemKeys.get(key);
+    if (mountedContainerId !== void 0) {
+      scheduleContainerLayout(ctx, mountedContainerId);
+    }
+  }
+  itemSizeVersions.set(key, nextVersion);
+}
+function getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion = false) {
+  if (!didValidateItemSizeVersion) {
+    validateItemSizeVersion(ctx, key, index, data);
+  }
+  return ctx.state.sizesKnown.get(key);
+}
+function getKnownOrFixedSize(ctx, key, index, data, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const { getFixedItemSize, getItemType } = state.props;
-  let size = key ? state.sizesKnown.get(key) : void 0;
+  let size = key ? getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion) : void 0;
   if (size === void 0 && key && getFixedItemSize) {
     const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
     const fixedSize = (resolved == null ? void 0 : resolved.didResolveFixedItemSize) ? resolved.fixedItemSize : getFixedItemSize(data, index, itemType);
@@ -1170,7 +1276,7 @@ function areKnownOrFixedItemSizesAvailable(ctx, startIndex, endIndex) {
   }
   return true;
 }
-function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved) {
+function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b, _c, _d;
   const state = ctx.state;
   const {
@@ -1179,7 +1285,7 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     props: { estimatedItemSize, getItemType },
     scrollingTo
   } = state;
-  const sizeKnown = state.sizesKnown.get(key);
+  const sizeKnown = getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion);
   if (sizeKnown !== void 0) {
     return sizeKnown;
   }
@@ -1190,12 +1296,23 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
       return renderedSize;
     }
   }
-  size = getKnownOrFixedSize(ctx, key, index, data, resolved);
+  size = getKnownOrFixedSize(ctx, key, index, data, resolved, true);
   if (size !== void 0) {
     setSize(ctx, key, size, notifyTotalSize);
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  if (size === void 0) {
+    const getEstimatedItemSizeProp = state.props.getEstimatedItemSize;
+    if (getEstimatedItemSizeProp) {
+      const estimatedForItem = getEstimatedItemSizeProp(data, index, itemType);
+      if (estimatedForItem !== void 0) {
+        size = roundSize(estimatedForItem);
+        setSize(ctx, key, size, notifyTotalSize);
+        return size;
+      }
+    }
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1286,7 +1403,7 @@ function finishScrollTo(ctx) {
     state.scrollingTo = void 0;
     state.scrollTargetPinnedRange = void 0;
     if (state.pendingTotalSize !== void 0) {
-      addTotalSize(ctx, null, state.pendingTotalSize);
+      addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, state.pendingTotalSize);
     }
     if (PlatformAdjustBreaksScroll) {
       state.scrollAdjustHandler.commitPendingAdjust(scrollingTo);
@@ -1501,6 +1618,21 @@ function doScrollTo(ctx, params) {
 }
 
 // src/core/doMaintainScrollAtEnd.ts
+function hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd) {
+  return !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+}
+function retireFinishedInitialScrollForMaintainedEnd(ctx) {
+  const state = ctx.state;
+  if (!state.initialScroll) {
+    return true;
+  }
+  if (!state.didFinishInitialScroll || state.scrollingTo && state.scrollingTo.isInitialScroll) {
+    return false;
+  }
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTarget(state);
+  return true;
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
   const {
@@ -1510,7 +1642,14 @@ function doMaintainScrollAtEnd(ctx) {
     props: { maintainScrollAtEnd }
   } = state;
   const isWithinMaintainScrollAtEndThreshold = peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
-  const shouldMaintainScrollAtEnd = !!(isWithinMaintainScrollAtEndThreshold && maintainScrollAtEnd && didContainersLayout);
+  const hasExternalMaintainScrollAtEndIntent2 = hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+  const shouldMaintainScrollAtEnd = !!((isWithinMaintainScrollAtEndThreshold || hasExternalMaintainScrollAtEndIntent2) && maintainScrollAtEnd && didContainersLayout);
+  if (shouldMaintainScrollAtEnd && state.initialScroll) {
+    state.pendingMaintainScrollAtEnd = true;
+    if (!retireFinishedInitialScrollForMaintainedEnd(ctx)) {
+      return false;
+    }
+  }
   if (pendingNativeMVCPAdjust) {
     state.pendingMaintainScrollAtEnd = shouldMaintainScrollAtEnd;
     return false;
@@ -1526,7 +1665,10 @@ function doMaintainScrollAtEnd(ctx) {
       const activeState = maintainScrollAtEnd.animated ? "animated" : "instant";
       state.maintainingScrollAtEnd = pendingState;
       requestAnimationFrame(() => {
-        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold")) {
+        if (state.maintainingScrollAtEnd !== pendingState) {
+          return;
+        }
+        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold") || typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd()) {
           state.maintainingScrollAtEnd = activeState;
           const scroller = refScroller.current;
           if (state.props.horizontal && isHorizontalRTL(state)) {
@@ -1592,11 +1734,20 @@ function requestAdjust(ctx, positionDiff, dataChanged) {
         if (!state.ignoreScrollFromMVCP) {
           state.ignoreScrollFromMVCP = {};
         }
+        // HAPPIER-MVCP-IGNORE-BAND-DIRECTION: this threshold object is REUSED across adjusts,
+        // so an earlier opposite-direction adjust left its bound in place and the accept set
+        // became the interval [lt, gt] - MEASURED collapsing to a single acceptable offset
+        // (lt === gt === 2100) across an insert+delete pair on a 4000px list, and empty outright
+        // whenever lt > gt, with nothing anywhere enforcing lt <= gt. Only the most recent
+        // adjust can explain an observation, so it now defines the whole band.
         if (positionDiff > 0) {
           state.ignoreScrollFromMVCP.lt = threshold;
+          state.ignoreScrollFromMVCP.gt = void 0;
         } else {
           state.ignoreScrollFromMVCP.gt = threshold;
+          state.ignoreScrollFromMVCP.lt = void 0;
         }
+        state.ignoreScrollFromMVCPDeferredScroll = void 0;
         if (state.ignoreScrollFromMVCPTimeout) {
           clearTimeout(state.ignoreScrollFromMVCPTimeout);
         }
@@ -1605,10 +1756,17 @@ function requestAdjust(ctx, positionDiff, dataChanged) {
           var _a3;
           state.ignoreScrollFromMVCP = void 0;
           const shouldForceUpdate = state.ignoreScrollFromMVCPIgnored && state.scrollProcessingEnabled !== false;
+          // Reprocess the offset the band REJECTED, not state.scroll. On native state.scroll is an
+          // open-loop prediction (this build reads the real scroll offset nowhere), so reprocessing
+          // it re-decides on the same unverified number while the observed offset is gone for good
+          // - the suppressed observation was destroyed, not deferred. No timer duration changes.
+          const deferredScroll = state.ignoreScrollFromMVCPDeferredScroll;
+          state.ignoreScrollFromMVCPDeferredScroll = void 0;
           if (shouldForceUpdate) {
             state.ignoreScrollFromMVCPIgnored = false;
-            state.scrollPending = state.scroll;
-            (_a3 = state.reprocessCurrentScroll) == null ? void 0 : _a3.call(state);
+            const reprocessScroll = deferredScroll !== void 0 ? deferredScroll : state.scroll;
+            state.scrollPending = reprocessScroll;
+            (_a3 = state.reprocessCurrentScroll) == null ? void 0 : _a3.call(state, reprocessScroll);
           }
         }, delay);
       }
@@ -1642,26 +1800,30 @@ function resolveAnchorLock(state, enableMVCPAnchorLock, mvcpData, now) {
   return lock;
 }
 function updateAnchorLock(state, params) {
-  if (Platform.OS === "web") {
-    const { anchorId, anchorPosition, dataChanged, now, positionDiff } = params;
-    const enableMVCPAnchorLock = !!dataChanged || !!state.mvcpAnchorLock;
-    const mvcpData = state.props.maintainVisibleContentPosition.data;
-    if (!enableMVCPAnchorLock || !mvcpData || state.scrollingTo || !anchorId || anchorPosition === void 0) {
-      return;
-    }
-    const existingLock = state.mvcpAnchorLock;
-    const quietPasses = !dataChanged && Math.abs(positionDiff) <= MVCP_POSITION_EPSILON && (existingLock == null ? void 0 : existingLock.id) === anchorId ? existingLock.quietPasses + 1 : 0;
-    if (!dataChanged && quietPasses >= MVCP_ANCHOR_LOCK_QUIET_PASSES_TO_RELEASE) {
-      state.mvcpAnchorLock = void 0;
-      return;
-    }
-    state.mvcpAnchorLock = {
-      expiresAt: now + MVCP_ANCHOR_LOCK_TTL_MS,
-      id: anchorId,
-      position: anchorPosition,
-      quietPasses
-    };
+  // HAPPIER-MVCP-ANCHOR-LOCK-NATIVE: this whole body was wrapped in `Platform.OS === "web"`, so
+  // state.mvcpAnchorLock was never written on native and resolveAnchorLock /
+  // shouldValidateLockedAnchor were permanently dead there. Native therefore re-picked its MVCP
+  // anchor out of idsInView on every pass, with no identity or position continuity across a data
+  // change - which is exactly the shape of a message-send crossover. Nothing here is web-specific:
+  // it reads props and indexByKey and writes state, with no DOM read and no measurement.
+  const { anchorId, anchorPosition, dataChanged, now, positionDiff } = params;
+  const enableMVCPAnchorLock = !!dataChanged || !!state.mvcpAnchorLock;
+  const mvcpData = state.props.maintainVisibleContentPosition.data;
+  if (!enableMVCPAnchorLock || !mvcpData || state.scrollingTo || !anchorId || anchorPosition === void 0) {
+    return;
+  }
+  const existingLock = state.mvcpAnchorLock;
+  const quietPasses = !dataChanged && Math.abs(positionDiff) <= MVCP_POSITION_EPSILON && (existingLock == null ? void 0 : existingLock.id) === anchorId ? existingLock.quietPasses + 1 : 0;
+  if (!dataChanged && quietPasses >= MVCP_ANCHOR_LOCK_QUIET_PASSES_TO_RELEASE) {
+    state.mvcpAnchorLock = void 0;
+    return;
   }
+  state.mvcpAnchorLock = {
+    expiresAt: now + MVCP_ANCHOR_LOCK_TTL_MS,
+    id: anchorId,
+    position: anchorPosition,
+    quietPasses
+  };
 }
 function shouldQueueNativeMVCPAdjust(dataChanged, state, positionDiff, prevTotalSize, prevScroll, scrollTarget) {
   if (!dataChanged || Platform.OS === "web" || !state.props.maintainVisibleContentPosition.data || scrollTarget !== void 0 || positionDiff >= -MVCP_POSITION_EPSILON) {
@@ -1765,13 +1927,20 @@ function prepareMVCP(ctx, dataChanged) {
   } = props;
   const isWeb = Platform.OS === "web";
   const now = Date.now();
-  const enableMVCPAnchorLock = isWeb && (!!dataChanged || !!state.mvcpAnchorLock);
+  const enableMVCPAnchorLock = !!dataChanged || !!state.mvcpAnchorLock;
   const scrollingTo = state.scrollingTo;
-  if (isWeb && dataChanged && state.pendingScrollToEnd && scrollingTo === void 0) {
+  // The lock must be dropped on both platforms when a scroll-to-end is queued, or native would
+  // anchor to a row the queued scroll is about to leave. Skipping MVCP entirely stays web-only:
+  // that is a separate upstream decision this patch does not change.
+  if (dataChanged && state.pendingScrollToEnd && scrollingTo === void 0) {
     state.mvcpAnchorLock = void 0;
-    return void 0;
+    if (isWeb) {
+      return void 0;
+    }
   }
-  const anchorLock = isWeb ? resolveAnchorLock(state, enableMVCPAnchorLock, mvcpData, now) : void 0;
+  // HAPPIER-MVCP-ANCHOR-LOCK-NATIVE (consumer half): native resolved to `void 0` unconditionally,
+  // so even a written lock could never be read back.
+  const anchorLock = resolveAnchorLock(state, enableMVCPAnchorLock, mvcpData, now);
   let prevPosition;
   let targetId;
   const idsInViewWithPositions = [];
@@ -1822,7 +1991,7 @@ function prepareMVCP(ctx, dataChanged) {
       let anchorPositionForLock;
       let skipTargetAnchor = false;
       const data = state.props.data;
-      const shouldValidateLockedAnchor = isWeb && dataChanged && mvcpData && scrollTarget === void 0 && targetId !== void 0 && (anchorLock == null ? void 0 : anchorLock.id) === targetId && shouldRestorePosition !== void 0;
+      const shouldValidateLockedAnchor = dataChanged && mvcpData && scrollTarget === void 0 && targetId !== void 0 && (anchorLock == null ? void 0 : anchorLock.id) === targetId && shouldRestorePosition !== void 0;
       if (shouldValidateLockedAnchor && targetId !== void 0) {
         const index = indexByKey.get(targetId);
         if (index !== void 0) {
@@ -1909,7 +2078,11 @@ function prepareMVCP(ctx, dataChanged) {
         return;
       }
       if (Math.abs(positionDiff) > MVCP_POSITION_EPSILON) {
-        const shouldSkipAdjustForMaintainedEnd = (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "animated") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const maintainScrollAtEnd = props.maintainScrollAtEnd;
+        const maintainingScrollAtEnd = state.maintainingScrollAtEnd;
+        const hasExternalMaintainedEndOwnership = state.didContainersLayout && hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+        const hasActiveMaintainedEndOwnership = !!maintainScrollAtEnd && (maintainingScrollAtEnd === "pending-animated" || maintainingScrollAtEnd === "animated" || maintainingScrollAtEnd === "pending-instant" || maintainingScrollAtEnd === "instant") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const shouldSkipAdjustForMaintainedEnd = scrollTarget === void 0 && (hasExternalMaintainedEndOwnership || hasActiveMaintainedEndOwnership);
         if (!shouldSkipAdjustForMaintainedEnd) {
           requestAdjust(ctx, positionDiff, dataChanged && mvcpData);
         }
@@ -1973,7 +2146,6 @@ function isInMVCPActiveMode(state) {
 
 // src/core/updateScroll.ts
 function updateScroll(ctx, newScroll, forceUpdate, options) {
-  var _a3;
   const state = ctx.state;
   const { ignoreScrollFromMVCP, lastScrollAdjustForHistory, scrollAdjustHandler, scrollHistory, scrollingTo } = state;
   const prevScroll = state.scroll;
@@ -1999,10 +2171,32 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
   if (ignoreScrollFromMVCP && !scrollingTo) {
     const { lt, gt } = ignoreScrollFromMVCP;
     if (lt && newScroll < lt || gt && newScroll > gt) {
+      // HAPPIER-MVCP-IGNORE-RESOLVES-PENDING: resolvePendingNativeMVCPAdjust used to sit BELOW
+      // this early return, so an armed pendingNativeMVCPAdjust blinded the only channel that can
+      // retire it - and arming it is what armed this band in the first place. While it survives,
+      // native turns off render-range projection, user-scroll detection, the large-jump escape
+      // hatch, every subsequent MVCP pass and doMaintainScrollAtEnd. Retiring it here is the fix.
+      // BOUNDED, NOT UNIVERSAL - the offset handed to the resolve is the one this band just
+      // rejected. When the band in force was armed by this pending's OWN manual leg the rejection
+      // is wrong-direction for it and the pending is merely dropped: shouldQueueNativeMVCPAdjust
+      // admits only amount < 0, so the leg is <= 0, arms the `gt` bound, and a rejected offset
+      // lies above the post-leg prediction. But maybeApplyPredictedNativeMVCPAdjust returns with
+      // manualApplied still 0 whenever the predicted native clamp is 0, and such a pending arms no
+      // band of its own - the band in force can then be a FOREIGN one from a requestAdjust caller
+      // outside prepareMVCP's pending gate (scroll-offset diff, header size, content inset, list
+      // padding). In that composition the rejected offset need not be wrong-direction and
+      // settlePendingNativeMVCPAdjust can re-request an adjust computed from it. DERIVED, never
+      // observed in any capture: left documented rather than guarded, and named as an instrumented
+      // target so a guard is built only if it is measured to fire. See
+      // .project/reviews/2026-08-01-send-transition/Q4-adversarial-review.md (F-Q4-1) and
+      // Z1-divergence-instrument.md.
+      resolvePendingNativeMVCPAdjust(ctx, newScroll);
       state.ignoreScrollFromMVCPIgnored = true;
+      state.ignoreScrollFromMVCPDeferredScroll = newScroll;
       return;
     }
   }
+  state.ignoreScrollFromMVCPDeferredScroll = void 0;
   state.scrollPrev = prevScroll;
   state.scrollPrevTime = state.scrollTime;
   state.scroll = newScroll;
@@ -2047,7 +2241,7 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
     } else {
       runCalculateItems();
     }
-    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd || !!((_a3 = state.props.maintainScrollAtEnd) == null ? void 0 : _a3.onDataChange);
+    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd;
     if (didResolvePendingNativeMVCPAdjust && shouldMaintainScrollAtEndAfterPendingSettle) {
       state.pendingMaintainScrollAtEnd = false;
       doMaintainScrollAtEnd(ctx);
@@ -2398,7 +2592,8 @@ function advanceCurrentInitialScrollSession(ctx, options) {
 }
 
 // src/utils/checkAllSizesKnown.ts
-function checkAllSizesKnown(state, start, end) {
+function checkAllSizesKnown(ctx, start, end) {
+  const state = ctx.state;
   if (start == null || end == null || start < 0 || end < start) {
     return false;
   }
@@ -2408,7 +2603,7 @@ function checkAllSizesKnown(state, start, end) {
     if (index !== void 0 && index >= start && index <= end) {
       hasMountedIndex = true;
       const id = getId(state, index);
-      if (id === void 0 || !state.sizesKnown.has(id)) {
+      if (id === void 0 || getKnownItemSize(ctx, id, index, state.props.data[index]) === void 0) {
         return false;
       }
     }
@@ -2788,11 +2983,13 @@ function handleBootstrapInitialScrollDataChange(ctx, options) {
       setInitialScrollTarget(ctx, updatedInitialScroll, {
         resetDidFinish: shouldResetDidFinish
       });
-      rearmBootstrapInitialScroll(ctx, {
-        scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
-        seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
-        targetIndexSeed: updatedInitialScroll.index
-      });
+      if (!!bootstrapInitialScroll || shouldResetDidFinish || !state.didFinishInitialScroll) {
+        rearmBootstrapInitialScroll(ctx, {
+          scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
+          seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
+          targetIndexSeed: updatedInitialScroll.index
+        });
+      }
       return;
     }
   }
@@ -2913,7 +3110,7 @@ function evaluateBootstrapInitialScroll(ctx) {
     bootstrapInitialScroll.targetIndexSeed = void 0;
   }
   const resolvedOffset = resolveInitialScrollOffset(ctx, initialScroll);
-  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
   const didResolvedOffsetChange = Math.abs(bootstrapInitialScroll.scroll - resolvedOffset) > 1;
   const { data } = state.props;
   const visibleIndices = getBootstrapRevealVisibleIndices({
@@ -2931,7 +3128,7 @@ function evaluateBootstrapInitialScroll(ctx) {
   const areVisibleIndicesMeasured = visibleIndices.length > 0 && visibleIndices.every((index) => {
     var _a4;
     const id = (_a4 = state.idCache[index]) != null ? _a4 : getId(state, index);
-    return state.sizesKnown.has(id);
+    return getKnownItemSize(ctx, id, index, data[index]) !== void 0;
   });
   const previousResolvedOffset = bootstrapInitialScroll.previousResolvedOffset;
   const previousVisibleIndices = bootstrapInitialScroll.visibleIndices;
@@ -3324,9 +3521,27 @@ function updateTotalSize(ctx) {
     positions,
     props: { data }
   } = state;
+  // HAPPIER-TOTALSIZE-STALE-POSITIONS: `state.props` is mutated during RENDER, so `props.data`
+  // can already be the next render's array while `positions` still describes the previous one.
+  // Any event-driven pass (a delivered scroll, a layout) that runs before the reconciling
+  // `dataChanged` pass sees that pair. When the new data is SHORTER, updateItemPositions' walk
+  // range (`startIndex = minIndexSizeChanged ?? startBuffered`) is already past its end, so no row
+  // is walked and the absolute total below is computed from stale positions: with a one-row
+  // `data`, `positions[0]` is 0 and the row size is unknown, so the list publishes exactly
+  // `estimatedItemSize` for a transcript of any height. Measured on device: 2,322 -> 240 on a
+  // 43-row transcript and 266,020 -> 240 on a 419-row one (the SAME 240, i.e. one row, not a sum),
+  // alignItemsAtEndPadding then grown to fill the viewport and UIScrollView clamping
+  // contentOffset to that one-viewport content.
+  // The mirror case is already refused: when data GREW, `positions[lastIndex]` is undefined and
+  // the `lastPosition !== void 0` guard below suppresses the publish. This makes the shrink
+  // direction symmetric. Nothing is lost by waiting - the reconciling pass walks from index 0,
+  // leaves `positions.length === data.length`, and republishes the exact total.
+  if (positions.length > data.length) {
+    return;
+  }
   const numColumns = (_a3 = peek$(ctx, "numColumns")) != null ? _a3 : 1;
   if (data.length === 0) {
-    addTotalSize(ctx, null, 0);
+    addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, 0);
   } else {
     const lastIndex = data.length - 1;
     const lastId = getId(state, lastIndex);
@@ -3349,12 +3564,12 @@ function updateTotalSize(ctx) {
             maxSize = size;
           }
         }
-        addTotalSize(ctx, null, lastPosition + maxSize);
+        addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, lastPosition + maxSize);
       } else {
         const lastSize = getItemSize(ctx, lastId, lastIndex, data[lastIndex]);
         if (lastSize !== void 0) {
           const totalSize = lastPosition + lastSize;
-          addTotalSize(ctx, null, totalSize);
+          addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, totalSize);
         }
       }
     }
@@ -3417,7 +3632,7 @@ function updateItemPositions(ctx, dataChanged, {
   const shouldOptimize = !forceFullUpdate && !dataChanged && (optimizeForVisibleWindow || Math.abs(velocity) > 0 || Platform.OS === "web" && state.scrollLength > 0 && lastScrollDelta > state.scrollLength);
   const maxVisibleArea = scrollBottomBuffered + 1e3;
   const useAverageSize = true;
-  const preferCachedSize = !doMVCP || dataChanged || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
+  const preferCachedSize = !doMVCP || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
   const notifyTotalSizeWhileCachingSizes = false;
   let currentRowTop = 0;
   let column = 1;
@@ -3446,7 +3661,7 @@ function updateItemPositions(ctx, dataChanged, {
       const prevIndex = startIndex - 1;
       const prevId = getId(state, prevIndex);
       const prevPosition = (_c = positions[prevIndex]) != null ? _c : 0;
-      const prevSize = (_d = sizesKnown.get(prevId)) != null ? _d : getItemSize(
+      const prevSize = getItemSize(
         ctx,
         prevId,
         prevIndex,
@@ -3464,7 +3679,25 @@ function updateItemPositions(ctx, dataChanged, {
   let breakAt;
   for (let i = startIndex; i < dataLength; i++) {
     if (shouldOptimize && breakAt !== void 0 && i > breakAt) {
-      didBreakEarly = true;
+      const staleRowTop = positions[i];
+      if (staleRowTop === void 0) {
+        didBreakEarly = true;
+        break;
+      }
+      const positionShift = currentRowTop - staleRowTop;
+      if (positionShift !== 0) {
+        for (let j = i; j < dataLength; j++) {
+          const stalePosition = positions[j];
+          if (stalePosition === void 0) {
+            break;
+          }
+          const shiftedPosition = stalePosition + positionShift;
+          positions[j] = shiftedPosition;
+          if (hasPositionListeners) {
+            notifyPosition$(ctx, idCache[j] !== void 0 ? idCache[j] : getId(state, j), shiftedPosition);
+          }
+        }
+      }
       break;
     }
     if (shouldOptimize && breakAt === void 0 && !scrollingTo && !dataChanged && currentRowTop > maxVisibleArea) {
@@ -3486,8 +3719,7 @@ function updateItemPositions(ctx, dataChanged, {
       column = 1;
       maxSizeInRow = 0;
     }
-    const knownSize = sizesKnown.get(id);
-    const size = knownSize !== void 0 ? knownSize : getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
+    const size = getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
     if (IS_DEV && needsIndexByKey) {
       if (indexByKeyForChecking.has(id)) {
         console.error(
@@ -4379,6 +4611,17 @@ function calculateItemsInView(ctx, params = {}) {
         }
       }
     }
+    if (startBuffered === null && endBuffered === null && dataLength > 0) {
+      const lastIndexInData = dataLength - 1;
+      const lastIdInData = idCache[lastIndexInData] != null ? idCache[lastIndexInData] : getId(state, lastIndexInData);
+      const lastTopInData = positions[lastIndexInData];
+      const lastSizeInData = sizes.get(lastIdInData) != null ? sizes.get(lastIdInData) : getItemSize(ctx, lastIdInData, lastIndexInData, data[lastIndexInData]);
+      if (lastTopInData !== void 0 && lastSizeInData !== void 0 && lastTopInData + lastSizeInData <= scroll) {
+        startBuffered = lastIndexInData;
+        startBufferedId = lastIdInData;
+        endBuffered = lastIndexInData;
+      }
+    }
     Object.assign(state, {
       endBuffered,
       endNoBuffer: visibleRange.endNoBuffer,
@@ -4582,7 +4825,7 @@ function calculateItemsInView(ctx, params = {}) {
     }
     maybeEmitFirstVisibleItemChanged(state, visibleRange.startNoBuffer);
     if (!queuedInitialLayout && !state.didContainersLayout) {
-      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(state, state.startBuffered, state.endBuffered) : checkAllSizesKnown(state, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered) : checkAllSizesKnown(ctx, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
       if (isInitialLayoutReady) {
         setDidLayout(ctx);
         handleInitialScrollLayoutReady(ctx);
@@ -4781,11 +5024,16 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   } = state;
   if (!data) return { didMeasureUserScrollAnchorResetItem };
   const index = state.indexByKey.get(itemKey);
+  const itemData = index === void 0 ? void 0 : state.props.data[index];
+  if (index === void 0 && state.props.getItemSizeVersion) {
+    return { didMeasureUserScrollAnchorResetItem };
+  }
+  const didValidateItemSizeVersion = index !== void 0;
+  const prevSizeKnown = didValidateItemSizeVersion ? getKnownItemSize(ctx, itemKey, index, itemData) : state.sizesKnown.get(itemKey);
   if (getFixedItemSize) {
     if (index === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
-    const itemData = state.props.data[index];
     if (itemData === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4798,7 +5046,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       };
     }
     const size2 = resolvedMeasurementItem.fixedItemSize;
-    if (size2 !== void 0 && size2 === sizesKnown.get(itemKey)) {
+    if (size2 !== void 0 && size2 === prevSizeKnown) {
       updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4806,8 +5054,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   let needsRecalculate = !didContainersLayout;
   let shouldMaintainScrollAtEnd = false;
   let minIndexSizeChanged;
-  const prevSizeKnown = state.sizesKnown.get(itemKey);
-  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem);
+  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion);
   const size = roundSize(horizontal ? sizeObj.width : sizeObj.height);
   if (diff !== 0) {
     minIndexSizeChanged = minIndexSizeChanged !== void 0 ? Math.min(minIndexSizeChanged, index) : index;
@@ -4816,7 +5063,8 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     if (!needsRecalculate && state.containerItemKeys.has(itemKey)) {
       needsRecalculate = true;
     }
-    if (prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
+    const hasExternalMaintainScrollAtEndIntent = !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+    if (hasExternalMaintainScrollAtEndIntent || prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
       shouldMaintainScrollAtEnd = true;
     }
     onItemSizeChanged == null ? void 0 : onItemSizeChanged({
@@ -4832,7 +5080,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     state.minIndexSizeChanged = state.minIndexSizeChanged !== void 0 ? Math.min(state.minIndexSizeChanged, minIndexSizeChanged) : minIndexSizeChanged;
   }
   updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
-  if (didContainersLayout || checkAllSizesKnown(state, state.startBuffered, state.endBuffered)) {
+  if (didContainersLayout || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered)) {
     const canMaintainScrollAtEnd = shouldMaintainScrollAtEnd && !!(maintainScrollAtEnd == null ? void 0 : maintainScrollAtEnd.onItemLayout);
     return {
       didChange: diff !== 0,
@@ -4846,7 +5094,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     didMeasureUserScrollAnchorResetItem
   };
 }
-function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
+function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const {
@@ -4869,7 +5117,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     fixedItemSize,
     itemType
   } : void 0;
-  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize);
+  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize, didValidateItemSizeVersion);
   const rawSize = horizontal ? sizeObj.width : sizeObj.height;
   const prevSizeKnown = sizesKnown.get(itemKey);
   if (Platform.OS !== "web" && prevSizeKnown !== void 0 && isNativeLayoutNoise(rawSize - prevSizeKnown)) {
@@ -4892,6 +5140,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       averages.avg = (averages.avg * averages.num + size) / (averages.num + 1);
       averages.num++;
     }
+    state.sizeContributions.set(itemKey, { itemType, size });
   }
   if (!prevSize || Math.abs(prevSize - size) > 0.1) {
     setSize(ctx, itemKey, size);
@@ -4935,7 +5184,9 @@ function measureContainersInLayoutEffect(ctx, targetContainerIds = null) {
       const generation = ((_a3 = state.containerItemGenerations[containerId]) != null ? _a3 : 0) + 1;
       state.containerItemGenerations[containerId] = generation;
       const fixedItemSize = resolveFixedItemSize(ctx, containerId, itemKey);
-      const canSkipMeasurement = !state.needsOtherAxisSize && fixedItemSize !== void 0 && state.sizesKnown.get(itemKey) === fixedItemSize + ctx.scrollAxisGap;
+      const itemIndex = state.indexByKey.get(itemKey);
+      const knownSize = itemIndex === void 0 ? void 0 : getKnownItemSize(ctx, itemKey, itemIndex, state.props.data[itemIndex]);
+      const canSkipMeasurement = !state.needsOtherAxisSize && fixedItemSize !== void 0 && knownSize === fixedItemSize + ctx.scrollAxisGap;
       if (canSkipMeasurement) {
         resolveSkippedAnchorReset(ctx, itemKey);
       } else if (viewRef) {
@@ -5395,7 +5646,8 @@ function processContainerLayout({ containerId, ctx, rectangle, ref, state }) {
   const axis = state.horizontal ? "width" : "height";
   const size = roundSize(rectangle[axis]);
   const localPreviousSize = state.lastSize ? roundSize(state.lastSize[axis]) : void 0;
-  const coreKnownSize = listState.sizesKnown.get(currentItemKey);
+  const currentItemIndex = listState.indexByKey.get(currentItemKey);
+  const coreKnownSize = currentItemIndex === void 0 ? void 0 : getKnownItemSize(ctx, currentItemKey, currentItemIndex, listState.props.data[currentItemIndex]);
   const previousSize = Platform.OS === "web" ? coreKnownSize : localPreviousSize;
   const applyLayout = () => {
     state.lastSize = layout;
@@ -6055,6 +6307,8 @@ function checkResetContainers(ctx, dataProp, { didColumnsChange = false } = {})
   if (didColumnsChange) {
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -6491,6 +6745,13 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   const IMPERATIVE_SCROLL_SETTLE_MAX_WAIT_MS = 800;
   const IMPERATIVE_SCROLL_SETTLE_STABLE_FRAMES = 2;
   let imperativeScrollToken = 0;
+  let pendingReadinessFrame;
+  const cancelPendingReadinessFrame = () => {
+    if (pendingReadinessFrame !== void 0) {
+      cancelAnimationFrame(pendingReadinessFrame);
+      pendingReadinessFrame = void 0;
+    }
+  };
   const isSettlingAfterDataChange = () => !!state.didDataChange || !!state.didColumnsChange || state.queuedMVCPRecalculate !== void 0 || state.ignoreScrollFromMVCP !== void 0;
   const isScrollToIndexReady = (targetIndex, allowEmpty = false) => {
     var _a3;
@@ -6512,6 +6773,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const startedAt = Date.now();
     let stableFrames = 0;
     const check = () => {
+      pendingReadinessFrame = void 0;
       if (token !== imperativeScrollToken) {
         return;
       }
@@ -6525,9 +6787,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
         run();
         return;
       }
-      requestAnimationFrame(check);
+      pendingReadinessFrame = requestAnimationFrame(check);
     };
-    requestAnimationFrame(check);
+    cancelPendingReadinessFrame();
+    pendingReadinessFrame = requestAnimationFrame(check);
   };
   const runScrollRequest = (token, resolve, run, isReady = () => true) => {
     const runNow = () => {
@@ -6550,12 +6813,21 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   };
   const startImperativeScroll = (resolve) => {
     var _a3;
+    cancelPendingReadinessFrame();
     const token = ++imperativeScrollToken;
     state.pendingScrollToEnd = void 0;
     (_a3 = state.pendingScrollResolve) == null ? void 0 : _a3.call(state);
     state.pendingScrollResolve = resolve;
     return token;
   };
+  state.disposeImperativeHandle = () => {
+    imperativeScrollToken++;
+    cancelPendingReadinessFrame();
+    state.pendingScrollToEnd = void 0;
+    const resolvePendingScroll = state.pendingScrollResolve;
+    state.pendingScrollResolve = void 0;
+    resolvePendingScroll == null ? void 0 : resolvePendingScroll();
+  };
   const runScrollWithPromise = (run, isReady = () => true) => new Promise((resolve) => {
     const token = startImperativeScroll(resolve);
     runScrollRequest(token, resolve, run, isReady);
@@ -6596,6 +6868,8 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const mode = (_a3 = options == null ? void 0 : options.mode) != null ? _a3 : "sizes";
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -6616,6 +6890,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     (_b = state.triggerCalculateItemsInView) == null ? void 0 : _b.call(state, { forceFullItemPositions: true });
   };
   return {
+    cancelInitialScrollPreservation: () => cancelInitialScrollPreservation(ctx),
     clearCaches,
     flashScrollIndicators: () => refScroller.current.flashScrollIndicators(),
     getAnimatableRef: () => {
@@ -6653,7 +6928,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
       scroll: state.scroll,
       scrollLength: state.scrollLength,
       scrollVelocity: getScrollVelocity(state),
-      sizeAtIndex: (index) => state.sizesKnown.get(getId(state, index)),
+      sizeAtIndex: (index) => {
+        const key = getId(state, index);
+        return getKnownItemSize(ctx, key, index, state.props.data[index]);
+      },
       sizes: state.sizesKnown,
       start: state.startNoBuffer,
       startBuffered: state.startBuffered
@@ -6834,7 +7112,8 @@ function normalizeMaintainScrollAtEnd(value) {
   const normalizedTriggers = normalizeMaintainScrollAtEndOn(value.on, "on" in value);
   return {
     ...normalizedTriggers,
-    animated: (_a3 = value.animated) != null ? _a3 : false
+    animated: (_a3 = value.animated) != null ? _a3 : false,
+    isMaintainingScrollAtEnd: value.isMaintainingScrollAtEnd
   };
 }
 
@@ -6964,6 +7243,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
+    getItemSizeVersion,
     getItemType,
     horizontal,
     rtl,
@@ -7156,6 +7437,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         scrollTime: 0,
         sizes: /* @__PURE__ */ new Map(),
         sizesKnown: /* @__PURE__ */ new Map(),
+        itemSizeVersions: /* @__PURE__ */ new Map(),
+        sizeContributions: /* @__PURE__ */ new Map(),
         startBuffered: -1,
         startNoBuffer: -1,
         startReachedSnapshot: void 0,
@@ -7168,7 +7451,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
       };
       const internalState = ctx.state;
       internalState.triggerCalculateItemsInView = (params) => calculateItemsInView(ctx, params);
-      internalState.reprocessCurrentScroll = () => updateScroll(ctx, internalState.scroll, true);
+      internalState.reprocessCurrentScroll = (offset) => updateScroll(ctx, offset !== void 0 ? offset : internalState.scroll, true);
       set$(ctx, "maintainVisibleContentPosition", maintainVisibleContentPositionConfig);
       set$(ctx, "extraData", extraData);
       if (estimatedHeaderSize !== void 0) {
@@ -7221,6 +7504,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
+    getItemSizeVersion: useWrapIfItem(getItemSizeVersion),
     getItemType: useWrapIfItem(getItemType),
     horizontal: !!horizontal,
     itemsAreEqual,
@@ -7262,7 +7547,10 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
   if (shouldResetFreshDataLayout) {
     resetInitialRenderState(ctx, {
       resetInitialScroll: !!initialScrollProp,
-      resetLayout: true
+      resetLayout: true,
+      // HAPPIER-MONOTONIC-DATASET-VISIBILITY: a transient empty projection of the same
+      // dataset still needs fresh layout, but must not turn an already painted list opaque.
+      preserveReadyToRender: !didDataKeyChangeLocal && state.didLoad
     });
   }
   const memoizedLastItemKeys = React2.useMemo(() => {
@@ -7498,10 +7786,20 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         cancelAnimationFrame(state.queuedFullDrawDistancePrewarm);
         state.queuedFullDrawDistancePrewarm = void 0;
       }
+      if (state.animFrameCheckFinishedScroll !== void 0) {
+        cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+        state.animFrameCheckFinishedScroll = void 0;
+      }
+      if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+        clearTimeout(state.timeoutCheckFinishedScrollFallback);
+        state.timeoutCheckFinishedScrollFallback = void 0;
+      }
       for (const timeout of state.timeouts) {
         clearTimeout(timeout);
       }
       state.timeouts.clear();
+      state.disposeImperativeHandle == null ? void 0 : state.disposeImperativeHandle();
+      state.disposeImperativeHandle = void 0;
     };
   }, [state]);
   React2.useLayoutEffect(() => {
diff --git a/node_modules/@legendapp/list/react-native.mjs b/node_modules/@legendapp/list/react-native.mjs
index c2e0f38..97cb9dd 100644
--- a/node_modules/@legendapp/list/react-native.mjs
+++ b/node_modules/@legendapp/list/react-native.mjs
@@ -881,7 +881,8 @@ function scheduleFullDrawDistancePrewarm(ctx) {
 // src/utils/setInitialRenderState.ts
 function resetInitialRenderState(ctx, {
   resetLayout,
-  resetInitialScroll
+  resetInitialScroll,
+  preserveReadyToRender
 }) {
   const { state } = ctx;
   if (resetLayout) {
@@ -891,7 +892,9 @@ function resetInitialRenderState(ctx, {
   if (resetInitialScroll) {
     state.didFinishInitialScroll = false;
   }
-  set$(ctx, "readyToRender", false);
+  if (!preserveReadyToRender) {
+    set$(ctx, "readyToRender", false);
+  }
   resetAdaptiveRender(ctx);
 }
 function setInitialRenderState(ctx, {
@@ -944,6 +947,34 @@ function clearPreservedInitialScrollTarget(state) {
   state.initialScroll = void 0;
   setInitialScrollSession(state);
 }
+function cancelInitialScrollPreservation(ctx) {
+  const state = ctx.state;
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTargetTimeout(state);
+  clearBootstrapInitialScrollSession(state);
+  const activeInitialScroll = state.scrollingTo;
+  if (activeInitialScroll == null ? void 0 : activeInitialScroll.isInitialScroll) {
+    if (state.animFrameCheckFinishedScroll !== void 0) {
+      cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+      state.animFrameCheckFinishedScroll = void 0;
+    }
+    if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+      clearTimeout(state.timeoutCheckFinishedScrollFallback);
+      state.timeoutCheckFinishedScrollFallback = void 0;
+    }
+    if (state.scrollingTo === activeInitialScroll) {
+      state.scrollingTo = void 0;
+      state.scrollTargetPinnedRange = void 0;
+    }
+  }
+  clearPreservedInitialScrollTarget(state);
+  clearInitialScrollSession(state);
+  setInitialRenderState(ctx, { didInitialScroll: true });
+  state.pendingMaintainScrollAtEnd = false;
+  if (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "pending-instant") {
+    state.maintainingScrollAtEnd = void 0;
+  }
+}
 function finishInitialScroll(ctx, options) {
   var _a3, _b, _c;
   const state = ctx.state;
@@ -981,6 +1012,9 @@ function finishInitialScroll(ctx, options) {
       recalculateSettledScroll(ctx);
     }
     setInitialRenderState(ctx, { didInitialScroll: true });
+    if (state.pendingMaintainScrollAtEnd) {
+      doMaintainScrollAtEnd(ctx);
+    }
     if (shouldReleaseDeferredPublicOnScroll) {
       releaseDeferredPublicOnScroll(ctx, finalScrollOffset);
     }
@@ -1036,11 +1070,25 @@ function updateContentMetricsState(ctx) {
 }
 
 // src/core/addTotalSize.ts
+// HAPPIER-NULLKEY-ABSOLUTE-TOTAL (owner half): `addTotalSize` overloaded `key === null` as its
+// ABSOLUTE-WRITE sentinel ("this number IS the total"), and `getId` returns `null` for any index
+// past the end of `data` ("there is no such row"). Two meanings, one value. One probe of a row the
+// current `data` no longer contains therefore assigned the whole content size to be one unmeasured
+// row: getId -> null -> getItemSize falls through to `estimatedItemSize` -> setSize with a null key
+// -> addTotalSize with a null key -> totalSize = est, written absolutely. MEASURED on device with a
+// 4/4 identical stack (checkResetContainers -> calculateItemsInView -> prepareMVCP's closure ->
+// getItemSize(ctx, getId(state, scrollingTo.index), ...)): totalSize 2,322 -> 240 on a 43-row
+// transcript AND 266,020 -> 240 on a 419-row one - the SAME constant, i.e. one row rather than a
+// sum - then alignItemsAtEndPadding grown to 297 (12 + 142 + 240 + 297 = 691, the viewport, to the
+// pixel) and UIScrollView clamping contentOffset to that one-viewport content.
+// The sentinel is given a private value no item key can ever take, so the invalid state stops being
+// representable rather than being guarded against at one caller.
+var HAPPIER_TOTAL_SIZE_ABSOLUTE = { happierTotalSizeAbsolute: true };
 function addTotalSize(ctx, key, add, notifyTotalSize = true) {
   const state = ctx.state;
   const prevTotalSize = state.totalSize;
   let totalSize = state.totalSize;
-  if (key === null) {
+  if (key === HAPPIER_TOTAL_SIZE_ABSOLUTE) {
     totalSize = add;
     if (state.timeoutSetPaddingTop) {
       clearTimeout(state.timeoutSetPaddingTop);
@@ -1067,6 +1115,17 @@ function addTotalSize(ctx, key, add, notifyTotalSize = true) {
 
 // src/core/setSize.ts
 function setSize(ctx, itemKey, size, notifyTotalSize = true) {
+  // HAPPIER-NULLKEY-NO-ROW-NO-SIZE (choke-point half): `getId` returns `null` for an index past the
+  // end of `data`, and ~15 call sites hand a `getId` result straight to `getItemSize` with no null
+  // check (the MEASURED device stack goes through prepareMVCP's closure, NOT through
+  // `getItemSizeAtIndex`, so guarding that one caller would not have covered it). `setSize` is the
+  // single choke point every resolved size passes, so refusing a key that is not a row here keeps a
+  // non-existent row from contributing to the running total or occupying a `sizes` entry, for every
+  // present and future caller. Paired with HAPPIER-NULLKEY-ABSOLUTE-TOTAL: that one removes the
+  // ambiguity, this one removes the phantom contribution the disambiguation would otherwise leave.
+  if (itemKey === null) {
+    return;
+  }
   const state = ctx.state;
   const { sizes } = state;
   const previousSize = sizes.get(itemKey);
@@ -1122,11 +1181,58 @@ function findContainerId(ctx, key) {
 }
 
 // src/utils/getItemSize.ts
-function getKnownOrFixedSize(ctx, key, index, data, resolved) {
+function reverseItemSizeContribution(state, key) {
+  const contribution = state.sizeContributions.get(key);
+  if (contribution === void 0) {
+    return;
+  }
+  state.sizeContributions.delete(key);
+  const averages = state.averageSizes[contribution.itemType];
+  if (!averages) {
+    return;
+  }
+  if (averages.num <= 1) {
+    delete state.averageSizes[contribution.itemType];
+    return;
+  }
+  const remainingNum = averages.num - 1;
+  averages.avg = (averages.avg * averages.num - contribution.size) / remainingNum;
+  averages.num = remainingNum;
+}
+function validateItemSizeVersion(ctx, key, index, data) {
+  const state = ctx.state;
+  const getItemSizeVersion = state.props.getItemSizeVersion;
+  if (!getItemSizeVersion) {
+    return;
+  }
+  const itemSizeVersions = state.itemSizeVersions;
+  const nextVersion = getItemSizeVersion(data, index);
+  if (itemSizeVersions.has(key) && itemSizeVersions.get(key) !== nextVersion) {
+    reverseItemSizeContribution(state, key);
+    state.sizesKnown.delete(key);
+    const previouslyContributedSize = state.sizes.get(key);
+    if (previouslyContributedSize !== void 0) {
+      addTotalSize(ctx, key, -previouslyContributedSize, false);
+    }
+    state.sizes.delete(key);
+    const mountedContainerId = state.containerItemKeys.get(key);
+    if (mountedContainerId !== void 0) {
+      scheduleContainerLayout(ctx, mountedContainerId);
+    }
+  }
+  itemSizeVersions.set(key, nextVersion);
+}
+function getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion = false) {
+  if (!didValidateItemSizeVersion) {
+    validateItemSizeVersion(ctx, key, index, data);
+  }
+  return ctx.state.sizesKnown.get(key);
+}
+function getKnownOrFixedSize(ctx, key, index, data, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const { getFixedItemSize, getItemType } = state.props;
-  let size = key ? state.sizesKnown.get(key) : void 0;
+  let size = key ? getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion) : void 0;
   if (size === void 0 && key && getFixedItemSize) {
     const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
     const fixedSize = (resolved == null ? void 0 : resolved.didResolveFixedItemSize) ? resolved.fixedItemSize : getFixedItemSize(data, index, itemType);
@@ -1149,7 +1255,7 @@ function areKnownOrFixedItemSizesAvailable(ctx, startIndex, endIndex) {
   }
   return true;
 }
-function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved) {
+function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b, _c, _d;
   const state = ctx.state;
   const {
@@ -1158,7 +1264,7 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     props: { estimatedItemSize, getItemType },
     scrollingTo
   } = state;
-  const sizeKnown = state.sizesKnown.get(key);
+  const sizeKnown = getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion);
   if (sizeKnown !== void 0) {
     return sizeKnown;
   }
@@ -1169,12 +1275,23 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
       return renderedSize;
     }
   }
-  size = getKnownOrFixedSize(ctx, key, index, data, resolved);
+  size = getKnownOrFixedSize(ctx, key, index, data, resolved, true);
   if (size !== void 0) {
     setSize(ctx, key, size, notifyTotalSize);
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  if (size === void 0) {
+    const getEstimatedItemSizeProp = state.props.getEstimatedItemSize;
+    if (getEstimatedItemSizeProp) {
+      const estimatedForItem = getEstimatedItemSizeProp(data, index, itemType);
+      if (estimatedForItem !== void 0) {
+        size = roundSize(estimatedForItem);
+        setSize(ctx, key, size, notifyTotalSize);
+        return size;
+      }
+    }
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1265,7 +1382,7 @@ function finishScrollTo(ctx) {
     state.scrollingTo = void 0;
     state.scrollTargetPinnedRange = void 0;
     if (state.pendingTotalSize !== void 0) {
-      addTotalSize(ctx, null, state.pendingTotalSize);
+      addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, state.pendingTotalSize);
     }
     if (PlatformAdjustBreaksScroll) {
       state.scrollAdjustHandler.commitPendingAdjust(scrollingTo);
@@ -1480,6 +1597,21 @@ function doScrollTo(ctx, params) {
 }
 
 // src/core/doMaintainScrollAtEnd.ts
+function hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd) {
+  return !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+}
+function retireFinishedInitialScrollForMaintainedEnd(ctx) {
+  const state = ctx.state;
+  if (!state.initialScroll) {
+    return true;
+  }
+  if (!state.didFinishInitialScroll || state.scrollingTo && state.scrollingTo.isInitialScroll) {
+    return false;
+  }
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTarget(state);
+  return true;
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
   const {
@@ -1489,7 +1621,14 @@ function doMaintainScrollAtEnd(ctx) {
     props: { maintainScrollAtEnd }
   } = state;
   const isWithinMaintainScrollAtEndThreshold = peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
-  const shouldMaintainScrollAtEnd = !!(isWithinMaintainScrollAtEndThreshold && maintainScrollAtEnd && didContainersLayout);
+  const hasExternalMaintainScrollAtEndIntent2 = hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+  const shouldMaintainScrollAtEnd = !!((isWithinMaintainScrollAtEndThreshold || hasExternalMaintainScrollAtEndIntent2) && maintainScrollAtEnd && didContainersLayout);
+  if (shouldMaintainScrollAtEnd && state.initialScroll) {
+    state.pendingMaintainScrollAtEnd = true;
+    if (!retireFinishedInitialScrollForMaintainedEnd(ctx)) {
+      return false;
+    }
+  }
   if (pendingNativeMVCPAdjust) {
     state.pendingMaintainScrollAtEnd = shouldMaintainScrollAtEnd;
     return false;
@@ -1505,7 +1644,10 @@ function doMaintainScrollAtEnd(ctx) {
       const activeState = maintainScrollAtEnd.animated ? "animated" : "instant";
       state.maintainingScrollAtEnd = pendingState;
       requestAnimationFrame(() => {
-        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold")) {
+        if (state.maintainingScrollAtEnd !== pendingState) {
+          return;
+        }
+        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold") || typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd()) {
           state.maintainingScrollAtEnd = activeState;
           const scroller = refScroller.current;
           if (state.props.horizontal && isHorizontalRTL(state)) {
@@ -1571,11 +1713,20 @@ function requestAdjust(ctx, positionDiff, dataChanged) {
         if (!state.ignoreScrollFromMVCP) {
           state.ignoreScrollFromMVCP = {};
         }
+        // HAPPIER-MVCP-IGNORE-BAND-DIRECTION: this threshold object is REUSED across adjusts,
+        // so an earlier opposite-direction adjust left its bound in place and the accept set
+        // became the interval [lt, gt] - MEASURED collapsing to a single acceptable offset
+        // (lt === gt === 2100) across an insert+delete pair on a 4000px list, and empty outright
+        // whenever lt > gt, with nothing anywhere enforcing lt <= gt. Only the most recent
+        // adjust can explain an observation, so it now defines the whole band.
         if (positionDiff > 0) {
           state.ignoreScrollFromMVCP.lt = threshold;
+          state.ignoreScrollFromMVCP.gt = void 0;
         } else {
           state.ignoreScrollFromMVCP.gt = threshold;
+          state.ignoreScrollFromMVCP.lt = void 0;
         }
+        state.ignoreScrollFromMVCPDeferredScroll = void 0;
         if (state.ignoreScrollFromMVCPTimeout) {
           clearTimeout(state.ignoreScrollFromMVCPTimeout);
         }
@@ -1584,10 +1735,17 @@ function requestAdjust(ctx, positionDiff, dataChanged) {
           var _a3;
           state.ignoreScrollFromMVCP = void 0;
           const shouldForceUpdate = state.ignoreScrollFromMVCPIgnored && state.scrollProcessingEnabled !== false;
+          // Reprocess the offset the band REJECTED, not state.scroll. On native state.scroll is an
+          // open-loop prediction (this build reads the real scroll offset nowhere), so reprocessing
+          // it re-decides on the same unverified number while the observed offset is gone for good
+          // - the suppressed observation was destroyed, not deferred. No timer duration changes.
+          const deferredScroll = state.ignoreScrollFromMVCPDeferredScroll;
+          state.ignoreScrollFromMVCPDeferredScroll = void 0;
           if (shouldForceUpdate) {
             state.ignoreScrollFromMVCPIgnored = false;
-            state.scrollPending = state.scroll;
-            (_a3 = state.reprocessCurrentScroll) == null ? void 0 : _a3.call(state);
+            const reprocessScroll = deferredScroll !== void 0 ? deferredScroll : state.scroll;
+            state.scrollPending = reprocessScroll;
+            (_a3 = state.reprocessCurrentScroll) == null ? void 0 : _a3.call(state, reprocessScroll);
           }
         }, delay);
       }
@@ -1621,26 +1779,30 @@ function resolveAnchorLock(state, enableMVCPAnchorLock, mvcpData, now) {
   return lock;
 }
 function updateAnchorLock(state, params) {
-  if (Platform.OS === "web") {
-    const { anchorId, anchorPosition, dataChanged, now, positionDiff } = params;
-    const enableMVCPAnchorLock = !!dataChanged || !!state.mvcpAnchorLock;
-    const mvcpData = state.props.maintainVisibleContentPosition.data;
-    if (!enableMVCPAnchorLock || !mvcpData || state.scrollingTo || !anchorId || anchorPosition === void 0) {
-      return;
-    }
-    const existingLock = state.mvcpAnchorLock;
-    const quietPasses = !dataChanged && Math.abs(positionDiff) <= MVCP_POSITION_EPSILON && (existingLock == null ? void 0 : existingLock.id) === anchorId ? existingLock.quietPasses + 1 : 0;
-    if (!dataChanged && quietPasses >= MVCP_ANCHOR_LOCK_QUIET_PASSES_TO_RELEASE) {
-      state.mvcpAnchorLock = void 0;
-      return;
-    }
-    state.mvcpAnchorLock = {
-      expiresAt: now + MVCP_ANCHOR_LOCK_TTL_MS,
-      id: anchorId,
-      position: anchorPosition,
-      quietPasses
-    };
+  // HAPPIER-MVCP-ANCHOR-LOCK-NATIVE: this whole body was wrapped in `Platform.OS === "web"`, so
+  // state.mvcpAnchorLock was never written on native and resolveAnchorLock /
+  // shouldValidateLockedAnchor were permanently dead there. Native therefore re-picked its MVCP
+  // anchor out of idsInView on every pass, with no identity or position continuity across a data
+  // change - which is exactly the shape of a message-send crossover. Nothing here is web-specific:
+  // it reads props and indexByKey and writes state, with no DOM read and no measurement.
+  const { anchorId, anchorPosition, dataChanged, now, positionDiff } = params;
+  const enableMVCPAnchorLock = !!dataChanged || !!state.mvcpAnchorLock;
+  const mvcpData = state.props.maintainVisibleContentPosition.data;
+  if (!enableMVCPAnchorLock || !mvcpData || state.scrollingTo || !anchorId || anchorPosition === void 0) {
+    return;
+  }
+  const existingLock = state.mvcpAnchorLock;
+  const quietPasses = !dataChanged && Math.abs(positionDiff) <= MVCP_POSITION_EPSILON && (existingLock == null ? void 0 : existingLock.id) === anchorId ? existingLock.quietPasses + 1 : 0;
+  if (!dataChanged && quietPasses >= MVCP_ANCHOR_LOCK_QUIET_PASSES_TO_RELEASE) {
+    state.mvcpAnchorLock = void 0;
+    return;
   }
+  state.mvcpAnchorLock = {
+    expiresAt: now + MVCP_ANCHOR_LOCK_TTL_MS,
+    id: anchorId,
+    position: anchorPosition,
+    quietPasses
+  };
 }
 function shouldQueueNativeMVCPAdjust(dataChanged, state, positionDiff, prevTotalSize, prevScroll, scrollTarget) {
   if (!dataChanged || Platform.OS === "web" || !state.props.maintainVisibleContentPosition.data || scrollTarget !== void 0 || positionDiff >= -MVCP_POSITION_EPSILON) {
@@ -1744,13 +1906,20 @@ function prepareMVCP(ctx, dataChanged) {
   } = props;
   const isWeb = Platform.OS === "web";
   const now = Date.now();
-  const enableMVCPAnchorLock = isWeb && (!!dataChanged || !!state.mvcpAnchorLock);
+  const enableMVCPAnchorLock = !!dataChanged || !!state.mvcpAnchorLock;
   const scrollingTo = state.scrollingTo;
-  if (isWeb && dataChanged && state.pendingScrollToEnd && scrollingTo === void 0) {
+  // The lock must be dropped on both platforms when a scroll-to-end is queued, or native would
+  // anchor to a row the queued scroll is about to leave. Skipping MVCP entirely stays web-only:
+  // that is a separate upstream decision this patch does not change.
+  if (dataChanged && state.pendingScrollToEnd && scrollingTo === void 0) {
     state.mvcpAnchorLock = void 0;
-    return void 0;
+    if (isWeb) {
+      return void 0;
+    }
   }
-  const anchorLock = isWeb ? resolveAnchorLock(state, enableMVCPAnchorLock, mvcpData, now) : void 0;
+  // HAPPIER-MVCP-ANCHOR-LOCK-NATIVE (consumer half): native resolved to `void 0` unconditionally,
+  // so even a written lock could never be read back.
+  const anchorLock = resolveAnchorLock(state, enableMVCPAnchorLock, mvcpData, now);
   let prevPosition;
   let targetId;
   const idsInViewWithPositions = [];
@@ -1801,7 +1970,7 @@ function prepareMVCP(ctx, dataChanged) {
       let anchorPositionForLock;
       let skipTargetAnchor = false;
       const data = state.props.data;
-      const shouldValidateLockedAnchor = isWeb && dataChanged && mvcpData && scrollTarget === void 0 && targetId !== void 0 && (anchorLock == null ? void 0 : anchorLock.id) === targetId && shouldRestorePosition !== void 0;
+      const shouldValidateLockedAnchor = dataChanged && mvcpData && scrollTarget === void 0 && targetId !== void 0 && (anchorLock == null ? void 0 : anchorLock.id) === targetId && shouldRestorePosition !== void 0;
       if (shouldValidateLockedAnchor && targetId !== void 0) {
         const index = indexByKey.get(targetId);
         if (index !== void 0) {
@@ -1888,7 +2057,11 @@ function prepareMVCP(ctx, dataChanged) {
         return;
       }
       if (Math.abs(positionDiff) > MVCP_POSITION_EPSILON) {
-        const shouldSkipAdjustForMaintainedEnd = (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "animated") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const maintainScrollAtEnd = props.maintainScrollAtEnd;
+        const maintainingScrollAtEnd = state.maintainingScrollAtEnd;
+        const hasExternalMaintainedEndOwnership = state.didContainersLayout && hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+        const hasActiveMaintainedEndOwnership = !!maintainScrollAtEnd && (maintainingScrollAtEnd === "pending-animated" || maintainingScrollAtEnd === "animated" || maintainingScrollAtEnd === "pending-instant" || maintainingScrollAtEnd === "instant") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const shouldSkipAdjustForMaintainedEnd = scrollTarget === void 0 && (hasExternalMaintainedEndOwnership || hasActiveMaintainedEndOwnership);
         if (!shouldSkipAdjustForMaintainedEnd) {
           requestAdjust(ctx, positionDiff, dataChanged && mvcpData);
         }
@@ -1952,7 +2125,6 @@ function isInMVCPActiveMode(state) {
 
 // src/core/updateScroll.ts
 function updateScroll(ctx, newScroll, forceUpdate, options) {
-  var _a3;
   const state = ctx.state;
   const { ignoreScrollFromMVCP, lastScrollAdjustForHistory, scrollAdjustHandler, scrollHistory, scrollingTo } = state;
   const prevScroll = state.scroll;
@@ -1978,10 +2150,32 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
   if (ignoreScrollFromMVCP && !scrollingTo) {
     const { lt, gt } = ignoreScrollFromMVCP;
     if (lt && newScroll < lt || gt && newScroll > gt) {
+      // HAPPIER-MVCP-IGNORE-RESOLVES-PENDING: resolvePendingNativeMVCPAdjust used to sit BELOW
+      // this early return, so an armed pendingNativeMVCPAdjust blinded the only channel that can
+      // retire it - and arming it is what armed this band in the first place. While it survives,
+      // native turns off render-range projection, user-scroll detection, the large-jump escape
+      // hatch, every subsequent MVCP pass and doMaintainScrollAtEnd. Retiring it here is the fix.
+      // BOUNDED, NOT UNIVERSAL - the offset handed to the resolve is the one this band just
+      // rejected. When the band in force was armed by this pending's OWN manual leg the rejection
+      // is wrong-direction for it and the pending is merely dropped: shouldQueueNativeMVCPAdjust
+      // admits only amount < 0, so the leg is <= 0, arms the `gt` bound, and a rejected offset
+      // lies above the post-leg prediction. But maybeApplyPredictedNativeMVCPAdjust returns with
+      // manualApplied still 0 whenever the predicted native clamp is 0, and such a pending arms no
+      // band of its own - the band in force can then be a FOREIGN one from a requestAdjust caller
+      // outside prepareMVCP's pending gate (scroll-offset diff, header size, content inset, list
+      // padding). In that composition the rejected offset need not be wrong-direction and
+      // settlePendingNativeMVCPAdjust can re-request an adjust computed from it. DERIVED, never
+      // observed in any capture: left documented rather than guarded, and named as an instrumented
+      // target so a guard is built only if it is measured to fire. See
+      // .project/reviews/2026-08-01-send-transition/Q4-adversarial-review.md (F-Q4-1) and
+      // Z1-divergence-instrument.md.
+      resolvePendingNativeMVCPAdjust(ctx, newScroll);
       state.ignoreScrollFromMVCPIgnored = true;
+      state.ignoreScrollFromMVCPDeferredScroll = newScroll;
       return;
     }
   }
+  state.ignoreScrollFromMVCPDeferredScroll = void 0;
   state.scrollPrev = prevScroll;
   state.scrollPrevTime = state.scrollTime;
   state.scroll = newScroll;
@@ -2026,7 +2220,7 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
     } else {
       runCalculateItems();
     }
-    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd || !!((_a3 = state.props.maintainScrollAtEnd) == null ? void 0 : _a3.onDataChange);
+    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd;
     if (didResolvePendingNativeMVCPAdjust && shouldMaintainScrollAtEndAfterPendingSettle) {
       state.pendingMaintainScrollAtEnd = false;
       doMaintainScrollAtEnd(ctx);
@@ -2377,7 +2571,8 @@ function advanceCurrentInitialScrollSession(ctx, options) {
 }
 
 // src/utils/checkAllSizesKnown.ts
-function checkAllSizesKnown(state, start, end) {
+function checkAllSizesKnown(ctx, start, end) {
+  const state = ctx.state;
   if (start == null || end == null || start < 0 || end < start) {
     return false;
   }
@@ -2387,7 +2582,7 @@ function checkAllSizesKnown(state, start, end) {
     if (index !== void 0 && index >= start && index <= end) {
       hasMountedIndex = true;
       const id = getId(state, index);
-      if (id === void 0 || !state.sizesKnown.has(id)) {
+      if (id === void 0 || getKnownItemSize(ctx, id, index, state.props.data[index]) === void 0) {
         return false;
       }
     }
@@ -2767,11 +2962,13 @@ function handleBootstrapInitialScrollDataChange(ctx, options) {
       setInitialScrollTarget(ctx, updatedInitialScroll, {
         resetDidFinish: shouldResetDidFinish
       });
-      rearmBootstrapInitialScroll(ctx, {
-        scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
-        seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
-        targetIndexSeed: updatedInitialScroll.index
-      });
+      if (!!bootstrapInitialScroll || shouldResetDidFinish || !state.didFinishInitialScroll) {
+        rearmBootstrapInitialScroll(ctx, {
+          scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
+          seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
+          targetIndexSeed: updatedInitialScroll.index
+        });
+      }
       return;
     }
   }
@@ -2892,7 +3089,7 @@ function evaluateBootstrapInitialScroll(ctx) {
     bootstrapInitialScroll.targetIndexSeed = void 0;
   }
   const resolvedOffset = resolveInitialScrollOffset(ctx, initialScroll);
-  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
   const didResolvedOffsetChange = Math.abs(bootstrapInitialScroll.scroll - resolvedOffset) > 1;
   const { data } = state.props;
   const visibleIndices = getBootstrapRevealVisibleIndices({
@@ -2910,7 +3107,7 @@ function evaluateBootstrapInitialScroll(ctx) {
   const areVisibleIndicesMeasured = visibleIndices.length > 0 && visibleIndices.every((index) => {
     var _a4;
     const id = (_a4 = state.idCache[index]) != null ? _a4 : getId(state, index);
-    return state.sizesKnown.has(id);
+    return getKnownItemSize(ctx, id, index, data[index]) !== void 0;
   });
   const previousResolvedOffset = bootstrapInitialScroll.previousResolvedOffset;
   const previousVisibleIndices = bootstrapInitialScroll.visibleIndices;
@@ -3303,9 +3500,27 @@ function updateTotalSize(ctx) {
     positions,
     props: { data }
   } = state;
+  // HAPPIER-TOTALSIZE-STALE-POSITIONS: `state.props` is mutated during RENDER, so `props.data`
+  // can already be the next render's array while `positions` still describes the previous one.
+  // Any event-driven pass (a delivered scroll, a layout) that runs before the reconciling
+  // `dataChanged` pass sees that pair. When the new data is SHORTER, updateItemPositions' walk
+  // range (`startIndex = minIndexSizeChanged ?? startBuffered`) is already past its end, so no row
+  // is walked and the absolute total below is computed from stale positions: with a one-row
+  // `data`, `positions[0]` is 0 and the row size is unknown, so the list publishes exactly
+  // `estimatedItemSize` for a transcript of any height. Measured on device: 2,322 -> 240 on a
+  // 43-row transcript and 266,020 -> 240 on a 419-row one (the SAME 240, i.e. one row, not a sum),
+  // alignItemsAtEndPadding then grown to fill the viewport and UIScrollView clamping
+  // contentOffset to that one-viewport content.
+  // The mirror case is already refused: when data GREW, `positions[lastIndex]` is undefined and
+  // the `lastPosition !== void 0` guard below suppresses the publish. This makes the shrink
+  // direction symmetric. Nothing is lost by waiting - the reconciling pass walks from index 0,
+  // leaves `positions.length === data.length`, and republishes the exact total.
+  if (positions.length > data.length) {
+    return;
+  }
   const numColumns = (_a3 = peek$(ctx, "numColumns")) != null ? _a3 : 1;
   if (data.length === 0) {
-    addTotalSize(ctx, null, 0);
+    addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, 0);
   } else {
     const lastIndex = data.length - 1;
     const lastId = getId(state, lastIndex);
@@ -3328,12 +3543,12 @@ function updateTotalSize(ctx) {
             maxSize = size;
           }
         }
-        addTotalSize(ctx, null, lastPosition + maxSize);
+        addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, lastPosition + maxSize);
       } else {
         const lastSize = getItemSize(ctx, lastId, lastIndex, data[lastIndex]);
         if (lastSize !== void 0) {
           const totalSize = lastPosition + lastSize;
-          addTotalSize(ctx, null, totalSize);
+          addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, totalSize);
         }
       }
     }
@@ -3396,7 +3611,7 @@ function updateItemPositions(ctx, dataChanged, {
   const shouldOptimize = !forceFullUpdate && !dataChanged && (optimizeForVisibleWindow || Math.abs(velocity) > 0 || Platform.OS === "web" && state.scrollLength > 0 && lastScrollDelta > state.scrollLength);
   const maxVisibleArea = scrollBottomBuffered + 1e3;
   const useAverageSize = true;
-  const preferCachedSize = !doMVCP || dataChanged || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
+  const preferCachedSize = !doMVCP || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
   const notifyTotalSizeWhileCachingSizes = false;
   let currentRowTop = 0;
   let column = 1;
@@ -3425,7 +3640,7 @@ function updateItemPositions(ctx, dataChanged, {
       const prevIndex = startIndex - 1;
       const prevId = getId(state, prevIndex);
       const prevPosition = (_c = positions[prevIndex]) != null ? _c : 0;
-      const prevSize = (_d = sizesKnown.get(prevId)) != null ? _d : getItemSize(
+      const prevSize = getItemSize(
         ctx,
         prevId,
         prevIndex,
@@ -3443,7 +3658,25 @@ function updateItemPositions(ctx, dataChanged, {
   let breakAt;
   for (let i = startIndex; i < dataLength; i++) {
     if (shouldOptimize && breakAt !== void 0 && i > breakAt) {
-      didBreakEarly = true;
+      const staleRowTop = positions[i];
+      if (staleRowTop === void 0) {
+        didBreakEarly = true;
+        break;
+      }
+      const positionShift = currentRowTop - staleRowTop;
+      if (positionShift !== 0) {
+        for (let j = i; j < dataLength; j++) {
+          const stalePosition = positions[j];
+          if (stalePosition === void 0) {
+            break;
+          }
+          const shiftedPosition = stalePosition + positionShift;
+          positions[j] = shiftedPosition;
+          if (hasPositionListeners) {
+            notifyPosition$(ctx, idCache[j] !== void 0 ? idCache[j] : getId(state, j), shiftedPosition);
+          }
+        }
+      }
       break;
     }
     if (shouldOptimize && breakAt === void 0 && !scrollingTo && !dataChanged && currentRowTop > maxVisibleArea) {
@@ -3465,8 +3698,7 @@ function updateItemPositions(ctx, dataChanged, {
       column = 1;
       maxSizeInRow = 0;
     }
-    const knownSize = sizesKnown.get(id);
-    const size = knownSize !== void 0 ? knownSize : getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
+    const size = getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
     if (IS_DEV && needsIndexByKey) {
       if (indexByKeyForChecking.has(id)) {
         console.error(
@@ -4358,6 +4590,17 @@ function calculateItemsInView(ctx, params = {}) {
         }
       }
     }
+    if (startBuffered === null && endBuffered === null && dataLength > 0) {
+      const lastIndexInData = dataLength - 1;
+      const lastIdInData = idCache[lastIndexInData] != null ? idCache[lastIndexInData] : getId(state, lastIndexInData);
+      const lastTopInData = positions[lastIndexInData];
+      const lastSizeInData = sizes.get(lastIdInData) != null ? sizes.get(lastIdInData) : getItemSize(ctx, lastIdInData, lastIndexInData, data[lastIndexInData]);
+      if (lastTopInData !== void 0 && lastSizeInData !== void 0 && lastTopInData + lastSizeInData <= scroll) {
+        startBuffered = lastIndexInData;
+        startBufferedId = lastIdInData;
+        endBuffered = lastIndexInData;
+      }
+    }
     Object.assign(state, {
       endBuffered,
       endNoBuffer: visibleRange.endNoBuffer,
@@ -4561,7 +4804,7 @@ function calculateItemsInView(ctx, params = {}) {
     }
     maybeEmitFirstVisibleItemChanged(state, visibleRange.startNoBuffer);
     if (!queuedInitialLayout && !state.didContainersLayout) {
-      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(state, state.startBuffered, state.endBuffered) : checkAllSizesKnown(state, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered) : checkAllSizesKnown(ctx, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
       if (isInitialLayoutReady) {
         setDidLayout(ctx);
         handleInitialScrollLayoutReady(ctx);
@@ -4760,11 +5003,16 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   } = state;
   if (!data) return { didMeasureUserScrollAnchorResetItem };
   const index = state.indexByKey.get(itemKey);
+  const itemData = index === void 0 ? void 0 : state.props.data[index];
+  if (index === void 0 && state.props.getItemSizeVersion) {
+    return { didMeasureUserScrollAnchorResetItem };
+  }
+  const didValidateItemSizeVersion = index !== void 0;
+  const prevSizeKnown = didValidateItemSizeVersion ? getKnownItemSize(ctx, itemKey, index, itemData) : state.sizesKnown.get(itemKey);
   if (getFixedItemSize) {
     if (index === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
-    const itemData = state.props.data[index];
     if (itemData === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4777,7 +5025,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       };
     }
     const size2 = resolvedMeasurementItem.fixedItemSize;
-    if (size2 !== void 0 && size2 === sizesKnown.get(itemKey)) {
+    if (size2 !== void 0 && size2 === prevSizeKnown) {
       updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4785,8 +5033,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   let needsRecalculate = !didContainersLayout;
   let shouldMaintainScrollAtEnd = false;
   let minIndexSizeChanged;
-  const prevSizeKnown = state.sizesKnown.get(itemKey);
-  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem);
+  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion);
   const size = roundSize(horizontal ? sizeObj.width : sizeObj.height);
   if (diff !== 0) {
     minIndexSizeChanged = minIndexSizeChanged !== void 0 ? Math.min(minIndexSizeChanged, index) : index;
@@ -4795,7 +5042,8 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     if (!needsRecalculate && state.containerItemKeys.has(itemKey)) {
       needsRecalculate = true;
     }
-    if (prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
+    const hasExternalMaintainScrollAtEndIntent = !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+    if (hasExternalMaintainScrollAtEndIntent || prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
       shouldMaintainScrollAtEnd = true;
     }
     onItemSizeChanged == null ? void 0 : onItemSizeChanged({
@@ -4811,7 +5059,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     state.minIndexSizeChanged = state.minIndexSizeChanged !== void 0 ? Math.min(state.minIndexSizeChanged, minIndexSizeChanged) : minIndexSizeChanged;
   }
   updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
-  if (didContainersLayout || checkAllSizesKnown(state, state.startBuffered, state.endBuffered)) {
+  if (didContainersLayout || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered)) {
     const canMaintainScrollAtEnd = shouldMaintainScrollAtEnd && !!(maintainScrollAtEnd == null ? void 0 : maintainScrollAtEnd.onItemLayout);
     return {
       didChange: diff !== 0,
@@ -4825,7 +5073,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     didMeasureUserScrollAnchorResetItem
   };
 }
-function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
+function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const {
@@ -4848,7 +5096,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     fixedItemSize,
     itemType
   } : void 0;
-  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize);
+  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize, didValidateItemSizeVersion);
   const rawSize = horizontal ? sizeObj.width : sizeObj.height;
   const prevSizeKnown = sizesKnown.get(itemKey);
   if (Platform.OS !== "web" && prevSizeKnown !== void 0 && isNativeLayoutNoise(rawSize - prevSizeKnown)) {
@@ -4871,6 +5119,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       averages.avg = (averages.avg * averages.num + size) / (averages.num + 1);
       averages.num++;
     }
+    state.sizeContributions.set(itemKey, { itemType, size });
   }
   if (!prevSize || Math.abs(prevSize - size) > 0.1) {
     setSize(ctx, itemKey, size);
@@ -4914,7 +5163,9 @@ function measureContainersInLayoutEffect(ctx, targetContainerIds = null) {
       const generation = ((_a3 = state.containerItemGenerations[containerId]) != null ? _a3 : 0) + 1;
       state.containerItemGenerations[containerId] = generation;
       const fixedItemSize = resolveFixedItemSize(ctx, containerId, itemKey);
-      const canSkipMeasurement = !state.needsOtherAxisSize && fixedItemSize !== void 0 && state.sizesKnown.get(itemKey) === fixedItemSize + ctx.scrollAxisGap;
+      const itemIndex = state.indexByKey.get(itemKey);
+      const knownSize = itemIndex === void 0 ? void 0 : getKnownItemSize(ctx, itemKey, itemIndex, state.props.data[itemIndex]);
+      const canSkipMeasurement = !state.needsOtherAxisSize && fixedItemSize !== void 0 && knownSize === fixedItemSize + ctx.scrollAxisGap;
       if (canSkipMeasurement) {
         resolveSkippedAnchorReset(ctx, itemKey);
       } else if (viewRef) {
@@ -5374,7 +5625,8 @@ function processContainerLayout({ containerId, ctx, rectangle, ref, state }) {
   const axis = state.horizontal ? "width" : "height";
   const size = roundSize(rectangle[axis]);
   const localPreviousSize = state.lastSize ? roundSize(state.lastSize[axis]) : void 0;
-  const coreKnownSize = listState.sizesKnown.get(currentItemKey);
+  const currentItemIndex = listState.indexByKey.get(currentItemKey);
+  const coreKnownSize = currentItemIndex === void 0 ? void 0 : getKnownItemSize(ctx, currentItemKey, currentItemIndex, listState.props.data[currentItemIndex]);
   const previousSize = Platform.OS === "web" ? coreKnownSize : localPreviousSize;
   const applyLayout = () => {
     state.lastSize = layout;
@@ -6034,6 +6286,8 @@ function checkResetContainers(ctx, dataProp, { didColumnsChange = false } = {})
   if (didColumnsChange) {
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -6470,6 +6724,13 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   const IMPERATIVE_SCROLL_SETTLE_MAX_WAIT_MS = 800;
   const IMPERATIVE_SCROLL_SETTLE_STABLE_FRAMES = 2;
   let imperativeScrollToken = 0;
+  let pendingReadinessFrame;
+  const cancelPendingReadinessFrame = () => {
+    if (pendingReadinessFrame !== void 0) {
+      cancelAnimationFrame(pendingReadinessFrame);
+      pendingReadinessFrame = void 0;
+    }
+  };
   const isSettlingAfterDataChange = () => !!state.didDataChange || !!state.didColumnsChange || state.queuedMVCPRecalculate !== void 0 || state.ignoreScrollFromMVCP !== void 0;
   const isScrollToIndexReady = (targetIndex, allowEmpty = false) => {
     var _a3;
@@ -6491,6 +6752,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const startedAt = Date.now();
     let stableFrames = 0;
     const check = () => {
+      pendingReadinessFrame = void 0;
       if (token !== imperativeScrollToken) {
         return;
       }
@@ -6504,9 +6766,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
         run();
         return;
       }
-      requestAnimationFrame(check);
+      pendingReadinessFrame = requestAnimationFrame(check);
     };
-    requestAnimationFrame(check);
+    cancelPendingReadinessFrame();
+    pendingReadinessFrame = requestAnimationFrame(check);
   };
   const runScrollRequest = (token, resolve, run, isReady = () => true) => {
     const runNow = () => {
@@ -6529,12 +6792,21 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   };
   const startImperativeScroll = (resolve) => {
     var _a3;
+    cancelPendingReadinessFrame();
     const token = ++imperativeScrollToken;
     state.pendingScrollToEnd = void 0;
     (_a3 = state.pendingScrollResolve) == null ? void 0 : _a3.call(state);
     state.pendingScrollResolve = resolve;
     return token;
   };
+  state.disposeImperativeHandle = () => {
+    imperativeScrollToken++;
+    cancelPendingReadinessFrame();
+    state.pendingScrollToEnd = void 0;
+    const resolvePendingScroll = state.pendingScrollResolve;
+    state.pendingScrollResolve = void 0;
+    resolvePendingScroll == null ? void 0 : resolvePendingScroll();
+  };
   const runScrollWithPromise = (run, isReady = () => true) => new Promise((resolve) => {
     const token = startImperativeScroll(resolve);
     runScrollRequest(token, resolve, run, isReady);
@@ -6575,6 +6847,8 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const mode = (_a3 = options == null ? void 0 : options.mode) != null ? _a3 : "sizes";
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -6595,6 +6869,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     (_b = state.triggerCalculateItemsInView) == null ? void 0 : _b.call(state, { forceFullItemPositions: true });
   };
   return {
+    cancelInitialScrollPreservation: () => cancelInitialScrollPreservation(ctx),
     clearCaches,
     flashScrollIndicators: () => refScroller.current.flashScrollIndicators(),
     getAnimatableRef: () => {
@@ -6632,7 +6907,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
       scroll: state.scroll,
       scrollLength: state.scrollLength,
       scrollVelocity: getScrollVelocity(state),
-      sizeAtIndex: (index) => state.sizesKnown.get(getId(state, index)),
+      sizeAtIndex: (index) => {
+        const key = getId(state, index);
+        return getKnownItemSize(ctx, key, index, state.props.data[index]);
+      },
       sizes: state.sizesKnown,
       start: state.startNoBuffer,
       startBuffered: state.startBuffered
@@ -6813,7 +7091,8 @@ function normalizeMaintainScrollAtEnd(value) {
   const normalizedTriggers = normalizeMaintainScrollAtEndOn(value.on, "on" in value);
   return {
     ...normalizedTriggers,
-    animated: (_a3 = value.animated) != null ? _a3 : false
+    animated: (_a3 = value.animated) != null ? _a3 : false,
+    isMaintainingScrollAtEnd: value.isMaintainingScrollAtEnd
   };
 }
 
@@ -6943,6 +7222,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
+    getItemSizeVersion,
     getItemType,
     horizontal,
     rtl,
@@ -7135,6 +7416,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         scrollTime: 0,
         sizes: /* @__PURE__ */ new Map(),
         sizesKnown: /* @__PURE__ */ new Map(),
+        itemSizeVersions: /* @__PURE__ */ new Map(),
+        sizeContributions: /* @__PURE__ */ new Map(),
         startBuffered: -1,
         startNoBuffer: -1,
         startReachedSnapshot: void 0,
@@ -7147,7 +7430,7 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
       };
       const internalState = ctx.state;
       internalState.triggerCalculateItemsInView = (params) => calculateItemsInView(ctx, params);
-      internalState.reprocessCurrentScroll = () => updateScroll(ctx, internalState.scroll, true);
+      internalState.reprocessCurrentScroll = (offset) => updateScroll(ctx, offset !== void 0 ? offset : internalState.scroll, true);
       set$(ctx, "maintainVisibleContentPosition", maintainVisibleContentPositionConfig);
       set$(ctx, "extraData", extraData);
       if (estimatedHeaderSize !== void 0) {
@@ -7200,6 +7483,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
+    getItemSizeVersion: useWrapIfItem(getItemSizeVersion),
     getItemType: useWrapIfItem(getItemType),
     horizontal: !!horizontal,
     itemsAreEqual,
@@ -7241,7 +7526,10 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
   if (shouldResetFreshDataLayout) {
     resetInitialRenderState(ctx, {
       resetInitialScroll: !!initialScrollProp,
-      resetLayout: true
+      resetLayout: true,
+      // HAPPIER-MONOTONIC-DATASET-VISIBILITY: a transient empty projection of the same
+      // dataset still needs fresh layout, but must not turn an already painted list opaque.
+      preserveReadyToRender: !didDataKeyChangeLocal && state.didLoad
     });
   }
   const memoizedLastItemKeys = useMemo(() => {
@@ -7477,10 +7765,20 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         cancelAnimationFrame(state.queuedFullDrawDistancePrewarm);
         state.queuedFullDrawDistancePrewarm = void 0;
       }
+      if (state.animFrameCheckFinishedScroll !== void 0) {
+        cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+        state.animFrameCheckFinishedScroll = void 0;
+      }
+      if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+        clearTimeout(state.timeoutCheckFinishedScrollFallback);
+        state.timeoutCheckFinishedScrollFallback = void 0;
+      }
       for (const timeout of state.timeouts) {
         clearTimeout(timeout);
       }
       state.timeouts.clear();
+      state.disposeImperativeHandle == null ? void 0 : state.disposeImperativeHandle();
+      state.disposeImperativeHandle = void 0;
     };
   }, [state]);
   useLayoutEffect(() => {
diff --git a/node_modules/@legendapp/list/react-native.web.d.ts b/node_modules/@legendapp/list/react-native.web.d.ts
index db7841d..f00d3a3 100644
--- a/node_modules/@legendapp/list/react-native.web.d.ts
+++ b/node_modules/@legendapp/list/react-native.web.d.ts
@@ -222,6 +222,12 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Version token for the item size. Changing it invalidates only that item’s cached size.
+     * An observed `undefined` token is distinct from an item that has not been observed yet.
+     */
+    getItemSizeVersion?: (item: ItemT, index: number) => Key | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
@@ -515,6 +521,12 @@ interface MaintainScrollAtEndOptions {
      * Whether maintainScrollAtEnd should animate when it scrolls to the end.
      */
     animated?: boolean;
+    /**
+     * Reports an authoritative semantic end-follow intent. Event-driven maintenance
+     * remains active while this is true even when late measurement moved the
+     * physical viewport beyond the proximity threshold.
+     */
+    isMaintainingScrollAtEnd?: () => boolean;
     /**
      * Which events should keep the list pinned to the end.
      * - If omitted, object values default to all triggers.
@@ -570,6 +582,10 @@ type LegendListState$1 = {
     startBuffered: number;
 };
 type LegendListRef$1 = {
+    /**
+     * Cancels retained initial-scroll correction authority without scrolling.
+     */
+    cancelInitialScrollPreservation(): void;
     /**
      * Clears internal virtualization caches.
      * @param options - Cache clearing options.
diff --git a/node_modules/@legendapp/list/react-native.web.js b/node_modules/@legendapp/list/react-native.web.js
index 544cca8..6bac4eb 100644
--- a/node_modules/@legendapp/list/react-native.web.js
+++ b/node_modules/@legendapp/list/react-native.web.js
@@ -873,7 +873,8 @@ function scheduleFullDrawDistancePrewarm(ctx) {
 // src/utils/setInitialRenderState.ts
 function resetInitialRenderState(ctx, {
   resetLayout,
-  resetInitialScroll
+  resetInitialScroll,
+  preserveReadyToRender
 }) {
   const { state } = ctx;
   if (resetLayout) {
@@ -883,7 +884,9 @@ function resetInitialRenderState(ctx, {
   if (resetInitialScroll) {
     state.didFinishInitialScroll = false;
   }
-  set$(ctx, "readyToRender", false);
+  if (!preserveReadyToRender) {
+    set$(ctx, "readyToRender", false);
+  }
   resetAdaptiveRender(ctx);
 }
 function setInitialRenderState(ctx, {
@@ -936,6 +939,38 @@ function clearPreservedInitialScrollTarget(state) {
   state.initialScroll = void 0;
   setInitialScrollSession(state);
 }
+function cancelInitialScrollPreservation(ctx) {
+  const state = ctx.state;
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTargetTimeout(state);
+  clearBootstrapInitialScrollSession(state);
+  const activeInitialScroll = state.scrollingTo;
+  if (activeInitialScroll == null ? void 0 : activeInitialScroll.isInitialScroll) {
+    const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+    if ((pendingCompletion == null ? void 0 : pendingCompletion.scrollingTo) === activeInitialScroll) {
+      cancelPendingNonAnimatedScrollCompletion(state);
+    }
+    if (state.animFrameCheckFinishedScroll !== void 0) {
+      cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+      state.animFrameCheckFinishedScroll = void 0;
+    }
+    if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+      clearTimeout(state.timeoutCheckFinishedScrollFallback);
+      state.timeoutCheckFinishedScrollFallback = void 0;
+    }
+    if (state.scrollingTo === activeInitialScroll) {
+      state.scrollingTo = void 0;
+      state.scrollTargetPinnedRange = void 0;
+    }
+  }
+  clearPreservedInitialScrollTarget(state);
+  clearInitialScrollSession(state);
+  setInitialRenderState(ctx, { didInitialScroll: true });
+  state.pendingMaintainScrollAtEnd = false;
+  if (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "pending-instant") {
+    state.maintainingScrollAtEnd = void 0;
+  }
+}
 function finishInitialScroll(ctx, options) {
   var _a3, _b, _c;
   const state = ctx.state;
@@ -973,6 +1008,9 @@ function finishInitialScroll(ctx, options) {
       recalculateSettledScroll(ctx);
     }
     setInitialRenderState(ctx, { didInitialScroll: true });
+    if (state.pendingMaintainScrollAtEnd) {
+      doMaintainScrollAtEnd(ctx);
+    }
     if (shouldReleaseDeferredPublicOnScroll) {
       releaseDeferredPublicOnScroll(ctx, finalScrollOffset);
     }
@@ -1028,11 +1066,25 @@ function updateContentMetricsState(ctx) {
 }
 
 // src/core/addTotalSize.ts
+// HAPPIER-NULLKEY-ABSOLUTE-TOTAL (owner half): `addTotalSize` overloaded `key === null` as its
+// ABSOLUTE-WRITE sentinel ("this number IS the total"), and `getId` returns `null` for any index
+// past the end of `data` ("there is no such row"). Two meanings, one value. One probe of a row the
+// current `data` no longer contains therefore assigned the whole content size to be one unmeasured
+// row: getId -> null -> getItemSize falls through to `estimatedItemSize` -> setSize with a null key
+// -> addTotalSize with a null key -> totalSize = est, written absolutely. MEASURED on device with a
+// 4/4 identical stack (checkResetContainers -> calculateItemsInView -> prepareMVCP's closure ->
+// getItemSize(ctx, getId(state, scrollingTo.index), ...)): totalSize 2,322 -> 240 on a 43-row
+// transcript AND 266,020 -> 240 on a 419-row one - the SAME constant, i.e. one row rather than a
+// sum - then alignItemsAtEndPadding grown to 297 (12 + 142 + 240 + 297 = 691, the viewport, to the
+// pixel) and UIScrollView clamping contentOffset to that one-viewport content.
+// The sentinel is given a private value no item key can ever take, so the invalid state stops being
+// representable rather than being guarded against at one caller.
+var HAPPIER_TOTAL_SIZE_ABSOLUTE = { happierTotalSizeAbsolute: true };
 function addTotalSize(ctx, key, add, notifyTotalSize = true) {
   const state = ctx.state;
   const prevTotalSize = state.totalSize;
   let totalSize = state.totalSize;
-  if (key === null) {
+  if (key === HAPPIER_TOTAL_SIZE_ABSOLUTE) {
     totalSize = add;
     if (state.timeoutSetPaddingTop) {
       clearTimeout(state.timeoutSetPaddingTop);
@@ -1057,6 +1109,17 @@ function addTotalSize(ctx, key, add, notifyTotalSize = true) {
 
 // src/core/setSize.ts
 function setSize(ctx, itemKey, size, notifyTotalSize = true) {
+  // HAPPIER-NULLKEY-NO-ROW-NO-SIZE (choke-point half): `getId` returns `null` for an index past the
+  // end of `data`, and ~15 call sites hand a `getId` result straight to `getItemSize` with no null
+  // check (the MEASURED device stack goes through prepareMVCP's closure, NOT through
+  // `getItemSizeAtIndex`, so guarding that one caller would not have covered it). `setSize` is the
+  // single choke point every resolved size passes, so refusing a key that is not a row here keeps a
+  // non-existent row from contributing to the running total or occupying a `sizes` entry, for every
+  // present and future caller. Paired with HAPPIER-NULLKEY-ABSOLUTE-TOTAL: that one removes the
+  // ambiguity, this one removes the phantom contribution the disambiguation would otherwise leave.
+  if (itemKey === null) {
+    return;
+  }
   const state = ctx.state;
   const { sizes } = state;
   const previousSize = sizes.get(itemKey);
@@ -1112,11 +1175,58 @@ function findContainerId(ctx, key) {
 }
 
 // src/utils/getItemSize.ts
-function getKnownOrFixedSize(ctx, key, index, data, resolved) {
+function reverseItemSizeContribution(state, key) {
+  const contribution = state.sizeContributions.get(key);
+  if (contribution === void 0) {
+    return;
+  }
+  state.sizeContributions.delete(key);
+  const averages = state.averageSizes[contribution.itemType];
+  if (!averages) {
+    return;
+  }
+  if (averages.num <= 1) {
+    delete state.averageSizes[contribution.itemType];
+    return;
+  }
+  const remainingNum = averages.num - 1;
+  averages.avg = (averages.avg * averages.num - contribution.size) / remainingNum;
+  averages.num = remainingNum;
+}
+function validateItemSizeVersion(ctx, key, index, data) {
+  const state = ctx.state;
+  const getItemSizeVersion = state.props.getItemSizeVersion;
+  if (!getItemSizeVersion) {
+    return;
+  }
+  const itemSizeVersions = state.itemSizeVersions;
+  const nextVersion = getItemSizeVersion(data, index);
+  if (itemSizeVersions.has(key) && itemSizeVersions.get(key) !== nextVersion) {
+    reverseItemSizeContribution(state, key);
+    state.sizesKnown.delete(key);
+    const previouslyContributedSize = state.sizes.get(key);
+    if (previouslyContributedSize !== void 0) {
+      addTotalSize(ctx, key, -previouslyContributedSize, false);
+    }
+    state.sizes.delete(key);
+    const mountedContainerId = state.containerItemKeys.get(key);
+    if (mountedContainerId !== void 0) {
+      scheduleContainerLayout(ctx, mountedContainerId);
+    }
+  }
+  itemSizeVersions.set(key, nextVersion);
+}
+function getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion = false) {
+  if (!didValidateItemSizeVersion) {
+    validateItemSizeVersion(ctx, key, index, data);
+  }
+  return ctx.state.sizesKnown.get(key);
+}
+function getKnownOrFixedSize(ctx, key, index, data, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const { getFixedItemSize, getItemType } = state.props;
-  let size = key ? state.sizesKnown.get(key) : void 0;
+  let size = key ? getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion) : void 0;
   if (size === void 0 && key && getFixedItemSize) {
     const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
     const fixedSize = (resolved == null ? void 0 : resolved.didResolveFixedItemSize) ? resolved.fixedItemSize : getFixedItemSize(data, index, itemType);
@@ -1139,7 +1249,7 @@ function areKnownOrFixedItemSizesAvailable(ctx, startIndex, endIndex) {
   }
   return true;
 }
-function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved) {
+function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b, _c, _d;
   const state = ctx.state;
   const {
@@ -1148,7 +1258,7 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     props: { estimatedItemSize, getItemType },
     scrollingTo
   } = state;
-  const sizeKnown = state.sizesKnown.get(key);
+  const sizeKnown = getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion);
   if (sizeKnown !== void 0) {
     return sizeKnown;
   }
@@ -1159,12 +1269,23 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
       return renderedSize;
     }
   }
-  size = getKnownOrFixedSize(ctx, key, index, data, resolved);
+  size = getKnownOrFixedSize(ctx, key, index, data, resolved, true);
   if (size !== void 0) {
     setSize(ctx, key, size, notifyTotalSize);
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  if (size === void 0) {
+    const getEstimatedItemSizeProp = state.props.getEstimatedItemSize;
+    if (getEstimatedItemSizeProp) {
+      const estimatedForItem = getEstimatedItemSizeProp(data, index, itemType);
+      if (estimatedForItem !== void 0) {
+        size = roundSize(estimatedForItem);
+        setSize(ctx, key, size, notifyTotalSize);
+        return size;
+      }
+    }
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1244,18 +1365,34 @@ function clampScrollOffset(ctx, offset, scrollTarget) {
 }
 
 // src/core/finishScrollTo.ts
-function finishScrollTo(ctx) {
+function cancelPendingNonAnimatedScrollCompletion(state) {
+  if (!state) return;
+  const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+  state.pendingNonAnimatedScrollCompletion = void 0;
+  pendingCompletion == null ? void 0 : pendingCompletion.cancel();
+}
+function finishScrollTo(ctx, requestedCompletion) {
   var _a3, _b;
   const state = ctx.state;
+  const pendingCompletion = requestedCompletion != null ? requestedCompletion : state == null ? void 0 : state.pendingNonAnimatedScrollCompletion;
+  if (pendingCompletion && pendingCompletion.scrollingTo !== (state == null ? void 0 : state.scrollingTo)) {
+    return;
+  }
+  if (pendingCompletion && state.pendingNonAnimatedScrollCompletion === pendingCompletion) {
+    state.pendingNonAnimatedScrollCompletion = void 0;
+    pendingCompletion.cancel();
+  }
   if (state == null ? void 0 : state.scrollingTo) {
-    const resolvePendingScroll = state.pendingScrollResolve;
-    state.pendingScrollResolve = void 0;
+    const resolvePendingScroll = pendingCompletion ? pendingCompletion.resolve : state.pendingScrollResolve;
+    if (!pendingCompletion || state.pendingScrollResolve === pendingCompletion.resolve) {
+      state.pendingScrollResolve = void 0;
+    }
     const scrollingTo = state.scrollingTo;
     state.scrollHistory.length = 0;
     state.scrollingTo = void 0;
     state.scrollTargetPinnedRange = void 0;
     if (state.pendingTotalSize !== void 0) {
-      addTotalSize(ctx, null, state.pendingTotalSize);
+      addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, state.pendingTotalSize);
     }
     {
       state.scrollAdjustHandler.commitPendingAdjust(scrollingTo);
@@ -1299,6 +1436,7 @@ function doScrollTo(ctx, params) {
   const contentSize = isHorizontal ? getContentSize(ctx) : void 0;
   const left = isHorizontal ? toNativeHorizontalOffset(state, offset, contentSize) : 0;
   const top = isHorizontal ? 0 : offset;
+  cancelPendingNonAnimatedScrollCompletion(state);
   scroller.scrollTo({ animated: isAnimated, x: left, y: top });
   if (isAnimated) {
     const target = (_b = (_a3 = scroller.getScrollEventTarget) == null ? void 0 : _a3.call(scroller)) != null ? _b : null;
@@ -1309,9 +1447,16 @@ function doScrollTo(ctx, params) {
     });
   } else {
     state.scroll = offset;
-    setTimeout(() => {
-      finishScrollTo(ctx);
-    }, 100);
+    const scrollingTo = state.scrollingTo;
+    const resolve = state.pendingScrollResolve;
+    let completionTimeout;
+    const completion = {
+      cancel: () => clearTimeout(completionTimeout),
+      resolve,
+      scrollingTo
+    };
+    completionTimeout = setTimeout(() => finishScrollTo(ctx, completion), 100);
+    state.pendingNonAnimatedScrollCompletion = completion;
   }
 }
 function listenForScrollEnd(ctx, params) {
@@ -1367,6 +1512,21 @@ function listenForScrollEnd(ctx, params) {
 }
 
 // src/core/doMaintainScrollAtEnd.ts
+function hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd) {
+  return !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+}
+function retireFinishedInitialScrollForMaintainedEnd(ctx) {
+  const state = ctx.state;
+  if (!state.initialScroll) {
+    return true;
+  }
+  if (!state.didFinishInitialScroll || state.scrollingTo && state.scrollingTo.isInitialScroll) {
+    return false;
+  }
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTarget(state);
+  return true;
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
   const {
@@ -1376,7 +1536,14 @@ function doMaintainScrollAtEnd(ctx) {
     props: { maintainScrollAtEnd }
   } = state;
   const isWithinMaintainScrollAtEndThreshold = peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
-  const shouldMaintainScrollAtEnd = !!(isWithinMaintainScrollAtEndThreshold && maintainScrollAtEnd && didContainersLayout);
+  const hasExternalMaintainScrollAtEndIntent2 = hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+  const shouldMaintainScrollAtEnd = !!((isWithinMaintainScrollAtEndThreshold || hasExternalMaintainScrollAtEndIntent2) && maintainScrollAtEnd && didContainersLayout);
+  if (shouldMaintainScrollAtEnd && state.initialScroll) {
+    state.pendingMaintainScrollAtEnd = true;
+    if (!retireFinishedInitialScrollForMaintainedEnd(ctx)) {
+      return false;
+    }
+  }
   if (pendingNativeMVCPAdjust) {
     state.pendingMaintainScrollAtEnd = shouldMaintainScrollAtEnd;
     return false;
@@ -1392,7 +1559,10 @@ function doMaintainScrollAtEnd(ctx) {
       const activeState = maintainScrollAtEnd.animated ? "animated" : "instant";
       state.maintainingScrollAtEnd = pendingState;
       requestAnimationFrame(() => {
-        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold")) {
+        if (state.maintainingScrollAtEnd !== pendingState) {
+          return;
+        }
+        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold") || typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd()) {
           state.maintainingScrollAtEnd = activeState;
           const scroller = refScroller.current;
           if (state.props.horizontal && isHorizontalRTL(state)) {
@@ -1737,7 +1907,11 @@ function prepareMVCP(ctx, dataChanged) {
         return;
       }
       if (Math.abs(positionDiff) > MVCP_POSITION_EPSILON) {
-        const shouldSkipAdjustForMaintainedEnd = (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "animated") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const maintainScrollAtEnd = props.maintainScrollAtEnd;
+        const maintainingScrollAtEnd = state.maintainingScrollAtEnd;
+        const hasExternalMaintainedEndOwnership = state.didContainersLayout && hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+        const hasActiveMaintainedEndOwnership = !!maintainScrollAtEnd && (maintainingScrollAtEnd === "pending-animated" || maintainingScrollAtEnd === "animated" || maintainingScrollAtEnd === "pending-instant" || maintainingScrollAtEnd === "instant") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const shouldSkipAdjustForMaintainedEnd = scrollTarget === void 0 && (hasExternalMaintainedEndOwnership || hasActiveMaintainedEndOwnership);
         if (!shouldSkipAdjustForMaintainedEnd) {
           requestAdjust(ctx, positionDiff);
         }
@@ -1809,7 +1983,6 @@ function isInMVCPActiveMode(state) {
 
 // src/core/updateScroll.ts
 function updateScroll(ctx, newScroll, forceUpdate, options) {
-  var _a3;
   const state = ctx.state;
   const { ignoreScrollFromMVCP, lastScrollAdjustForHistory, scrollAdjustHandler, scrollHistory, scrollingTo } = state;
   const prevScroll = state.scroll;
@@ -1883,7 +2056,7 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
     } else {
       runCalculateItems();
     }
-    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd || !!((_a3 = state.props.maintainScrollAtEnd) == null ? void 0 : _a3.onDataChange);
+    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd;
     if (didResolvePendingNativeMVCPAdjust && shouldMaintainScrollAtEndAfterPendingSettle) {
       state.pendingMaintainScrollAtEnd = false;
       doMaintainScrollAtEnd(ctx);
@@ -2234,7 +2407,8 @@ function advanceCurrentInitialScrollSession(ctx, options) {
 }
 
 // src/utils/checkAllSizesKnown.ts
-function checkAllSizesKnown(state, start, end) {
+function checkAllSizesKnown(ctx, start, end) {
+  const state = ctx.state;
   if (start == null || end == null || start < 0 || end < start) {
     return false;
   }
@@ -2244,7 +2418,7 @@ function checkAllSizesKnown(state, start, end) {
     if (index !== void 0 && index >= start && index <= end) {
       hasMountedIndex = true;
       const id = getId(state, index);
-      if (id === void 0 || !state.sizesKnown.has(id)) {
+      if (id === void 0 || getKnownItemSize(ctx, id, index, state.props.data[index]) === void 0) {
         return false;
       }
     }
@@ -2624,11 +2798,13 @@ function handleBootstrapInitialScrollDataChange(ctx, options) {
       setInitialScrollTarget(ctx, updatedInitialScroll, {
         resetDidFinish: shouldResetDidFinish
       });
-      rearmBootstrapInitialScroll(ctx, {
-        scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
-        seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
-        targetIndexSeed: updatedInitialScroll.index
-      });
+      if (!!bootstrapInitialScroll || shouldResetDidFinish || !state.didFinishInitialScroll) {
+        rearmBootstrapInitialScroll(ctx, {
+          scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
+          seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
+          targetIndexSeed: updatedInitialScroll.index
+        });
+      }
       return;
     }
   }
@@ -2749,7 +2925,7 @@ function evaluateBootstrapInitialScroll(ctx) {
     bootstrapInitialScroll.targetIndexSeed = void 0;
   }
   const resolvedOffset = resolveInitialScrollOffset(ctx, initialScroll);
-  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
   const didResolvedOffsetChange = Math.abs(bootstrapInitialScroll.scroll - resolvedOffset) > 1;
   const { data } = state.props;
   const visibleIndices = getBootstrapRevealVisibleIndices({
@@ -2767,7 +2943,7 @@ function evaluateBootstrapInitialScroll(ctx) {
   const areVisibleIndicesMeasured = visibleIndices.length > 0 && visibleIndices.every((index) => {
     var _a4;
     const id = (_a4 = state.idCache[index]) != null ? _a4 : getId(state, index);
-    return state.sizesKnown.has(id);
+    return getKnownItemSize(ctx, id, index, data[index]) !== void 0;
   });
   const previousResolvedOffset = bootstrapInitialScroll.previousResolvedOffset;
   const previousVisibleIndices = bootstrapInitialScroll.visibleIndices;
@@ -3346,9 +3522,27 @@ function updateTotalSize(ctx) {
     positions,
     props: { data }
   } = state;
+  // HAPPIER-TOTALSIZE-STALE-POSITIONS: `state.props` is mutated during RENDER, so `props.data`
+  // can already be the next render's array while `positions` still describes the previous one.
+  // Any event-driven pass (a delivered scroll, a layout) that runs before the reconciling
+  // `dataChanged` pass sees that pair. When the new data is SHORTER, updateItemPositions' walk
+  // range (`startIndex = minIndexSizeChanged ?? startBuffered`) is already past its end, so no row
+  // is walked and the absolute total below is computed from stale positions: with a one-row
+  // `data`, `positions[0]` is 0 and the row size is unknown, so the list publishes exactly
+  // `estimatedItemSize` for a transcript of any height. Measured on device: 2,322 -> 240 on a
+  // 43-row transcript and 266,020 -> 240 on a 419-row one (the SAME 240, i.e. one row, not a sum),
+  // alignItemsAtEndPadding then grown to fill the viewport and UIScrollView clamping
+  // contentOffset to that one-viewport content.
+  // The mirror case is already refused: when data GREW, `positions[lastIndex]` is undefined and
+  // the `lastPosition !== void 0` guard below suppresses the publish. This makes the shrink
+  // direction symmetric. Nothing is lost by waiting - the reconciling pass walks from index 0,
+  // leaves `positions.length === data.length`, and republishes the exact total.
+  if (positions.length > data.length) {
+    return;
+  }
   const numColumns = (_a3 = peek$(ctx, "numColumns")) != null ? _a3 : 1;
   if (data.length === 0) {
-    addTotalSize(ctx, null, 0);
+    addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, 0);
   } else {
     const lastIndex = data.length - 1;
     const lastId = getId(state, lastIndex);
@@ -3371,12 +3565,12 @@ function updateTotalSize(ctx) {
             maxSize = size;
           }
         }
-        addTotalSize(ctx, null, lastPosition + maxSize);
+        addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, lastPosition + maxSize);
       } else {
         const lastSize = getItemSize(ctx, lastId, lastIndex, data[lastIndex]);
         if (lastSize !== void 0) {
           const totalSize = lastPosition + lastSize;
-          addTotalSize(ctx, null, totalSize);
+          addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, totalSize);
         }
       }
     }
@@ -3439,7 +3633,7 @@ function updateItemPositions(ctx, dataChanged, {
   const shouldOptimize = !forceFullUpdate && !dataChanged && (optimizeForVisibleWindow || Math.abs(velocity) > 0 || state.scrollLength > 0 && lastScrollDelta > state.scrollLength);
   const maxVisibleArea = scrollBottomBuffered + 1e3;
   const useAverageSize = true;
-  const preferCachedSize = !doMVCP || dataChanged || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
+  const preferCachedSize = !doMVCP || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
   const notifyTotalSizeWhileCachingSizes = false;
   let currentRowTop = 0;
   let column = 1;
@@ -3468,7 +3662,7 @@ function updateItemPositions(ctx, dataChanged, {
       const prevIndex = startIndex - 1;
       const prevId = getId(state, prevIndex);
       const prevPosition = (_c = positions[prevIndex]) != null ? _c : 0;
-      const prevSize = (_d = sizesKnown.get(prevId)) != null ? _d : getItemSize(
+      const prevSize = getItemSize(
         ctx,
         prevId,
         prevIndex,
@@ -3486,7 +3680,25 @@ function updateItemPositions(ctx, dataChanged, {
   let breakAt;
   for (let i = startIndex; i < dataLength; i++) {
     if (shouldOptimize && breakAt !== void 0 && i > breakAt) {
-      didBreakEarly = true;
+      const staleRowTop = positions[i];
+      if (staleRowTop === void 0) {
+        didBreakEarly = true;
+        break;
+      }
+      const positionShift = currentRowTop - staleRowTop;
+      if (positionShift !== 0) {
+        for (let j = i; j < dataLength; j++) {
+          const stalePosition = positions[j];
+          if (stalePosition === void 0) {
+            break;
+          }
+          const shiftedPosition = stalePosition + positionShift;
+          positions[j] = shiftedPosition;
+          if (hasPositionListeners) {
+            notifyPosition$(ctx, idCache[j] !== void 0 ? idCache[j] : getId(state, j), shiftedPosition);
+          }
+        }
+      }
       break;
     }
     if (shouldOptimize && breakAt === void 0 && !scrollingTo && !dataChanged && currentRowTop > maxVisibleArea) {
@@ -3508,8 +3720,7 @@ function updateItemPositions(ctx, dataChanged, {
       column = 1;
       maxSizeInRow = 0;
     }
-    const knownSize = sizesKnown.get(id);
-    const size = knownSize !== void 0 ? knownSize : getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
+    const size = getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
     if (IS_DEV && needsIndexByKey) {
       if (indexByKeyForChecking.has(id)) {
         console.error(
@@ -4401,6 +4612,17 @@ function calculateItemsInView(ctx, params = {}) {
         }
       }
     }
+    if (startBuffered === null && endBuffered === null && dataLength > 0) {
+      const lastIndexInData = dataLength - 1;
+      const lastIdInData = idCache[lastIndexInData] != null ? idCache[lastIndexInData] : getId(state, lastIndexInData);
+      const lastTopInData = positions[lastIndexInData];
+      const lastSizeInData = sizes.get(lastIdInData) != null ? sizes.get(lastIdInData) : getItemSize(ctx, lastIdInData, lastIndexInData, data[lastIndexInData]);
+      if (lastTopInData !== void 0 && lastSizeInData !== void 0 && lastTopInData + lastSizeInData <= scroll) {
+        startBuffered = lastIndexInData;
+        startBufferedId = lastIdInData;
+        endBuffered = lastIndexInData;
+      }
+    }
     Object.assign(state, {
       endBuffered,
       endNoBuffer: visibleRange.endNoBuffer,
@@ -4604,7 +4826,7 @@ function calculateItemsInView(ctx, params = {}) {
     }
     maybeEmitFirstVisibleItemChanged(state, visibleRange.startNoBuffer);
     if (!queuedInitialLayout && !state.didContainersLayout) {
-      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(state, state.startBuffered, state.endBuffered) : checkAllSizesKnown(state, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered) : checkAllSizesKnown(ctx, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
       if (isInitialLayoutReady) {
         setDidLayout(ctx);
         handleInitialScrollLayoutReady(ctx);
@@ -4790,11 +5012,16 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   } = state;
   if (!data) return { didMeasureUserScrollAnchorResetItem };
   const index = state.indexByKey.get(itemKey);
+  const itemData = index === void 0 ? void 0 : state.props.data[index];
+  if (index === void 0 && state.props.getItemSizeVersion) {
+    return { didMeasureUserScrollAnchorResetItem };
+  }
+  const didValidateItemSizeVersion = index !== void 0;
+  const prevSizeKnown = didValidateItemSizeVersion ? getKnownItemSize(ctx, itemKey, index, itemData) : state.sizesKnown.get(itemKey);
   if (getFixedItemSize) {
     if (index === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
-    const itemData = state.props.data[index];
     if (itemData === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4807,7 +5034,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       };
     }
     const size2 = resolvedMeasurementItem.fixedItemSize;
-    if (size2 !== void 0 && size2 === sizesKnown.get(itemKey)) {
+    if (size2 !== void 0 && size2 === prevSizeKnown) {
       updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4815,8 +5042,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   let needsRecalculate = !didContainersLayout;
   let shouldMaintainScrollAtEnd = false;
   let minIndexSizeChanged;
-  const prevSizeKnown = state.sizesKnown.get(itemKey);
-  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem);
+  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion);
   const size = roundSize(horizontal ? sizeObj.width : sizeObj.height);
   if (diff !== 0) {
     minIndexSizeChanged = minIndexSizeChanged !== void 0 ? Math.min(minIndexSizeChanged, index) : index;
@@ -4825,7 +5051,8 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     if (!needsRecalculate && state.containerItemKeys.has(itemKey)) {
       needsRecalculate = true;
     }
-    if (prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
+    const hasExternalMaintainScrollAtEndIntent = !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+    if (hasExternalMaintainScrollAtEndIntent || prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
       shouldMaintainScrollAtEnd = true;
     }
     onItemSizeChanged == null ? void 0 : onItemSizeChanged({
@@ -4841,7 +5068,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     state.minIndexSizeChanged = state.minIndexSizeChanged !== void 0 ? Math.min(state.minIndexSizeChanged, minIndexSizeChanged) : minIndexSizeChanged;
   }
   updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
-  if (didContainersLayout || checkAllSizesKnown(state, state.startBuffered, state.endBuffered)) {
+  if (didContainersLayout || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered)) {
     const canMaintainScrollAtEnd = shouldMaintainScrollAtEnd && !!(maintainScrollAtEnd == null ? void 0 : maintainScrollAtEnd.onItemLayout);
     return {
       didChange: diff !== 0,
@@ -4855,7 +5082,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     didMeasureUserScrollAnchorResetItem
   };
 }
-function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
+function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const {
@@ -4878,7 +5105,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     fixedItemSize,
     itemType
   } : void 0;
-  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize);
+  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize, didValidateItemSizeVersion);
   const rawSize = horizontal ? sizeObj.width : sizeObj.height;
   const prevSizeKnown = sizesKnown.get(itemKey);
   const size = Math.round(rawSize) ;
@@ -4898,6 +5125,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       averages.avg = (averages.avg * averages.num + size) / (averages.num + 1);
       averages.num++;
     }
+    state.sizeContributions.set(itemKey, { itemType, size });
   }
   if (!prevSize || Math.abs(prevSize - size) > 0.1) {
     setSize(ctx, itemKey, size);
@@ -5359,7 +5587,8 @@ function processContainerLayout({ containerId, ctx, rectangle, ref, state }) {
   const axis = state.horizontal ? "width" : "height";
   const size = roundSize(rectangle[axis]);
   state.lastSize ? roundSize(state.lastSize[axis]) : void 0;
-  const coreKnownSize = listState.sizesKnown.get(currentItemKey);
+  const currentItemIndex = listState.indexByKey.get(currentItemKey);
+  const coreKnownSize = currentItemIndex === void 0 ? void 0 : getKnownItemSize(ctx, currentItemKey, currentItemIndex, listState.props.data[currentItemIndex]);
   const previousSize = coreKnownSize ;
   const applyLayout = () => {
     state.lastSize = layout;
@@ -6691,6 +6920,8 @@ function checkResetContainers(ctx, dataProp, { didColumnsChange = false } = {})
   if (didColumnsChange) {
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7110,6 +7341,13 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   const IMPERATIVE_SCROLL_SETTLE_MAX_WAIT_MS = 800;
   const IMPERATIVE_SCROLL_SETTLE_STABLE_FRAMES = 2;
   let imperativeScrollToken = 0;
+  let pendingReadinessFrame;
+  const cancelPendingReadinessFrame = () => {
+    if (pendingReadinessFrame !== void 0) {
+      cancelAnimationFrame(pendingReadinessFrame);
+      pendingReadinessFrame = void 0;
+    }
+  };
   const isSettlingAfterDataChange = () => !!state.didDataChange || !!state.didColumnsChange || state.queuedMVCPRecalculate !== void 0 || state.ignoreScrollFromMVCP !== void 0;
   const isScrollToIndexReady = (targetIndex, allowEmpty = false) => {
     var _a3;
@@ -7131,6 +7369,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const startedAt = Date.now();
     let stableFrames = 0;
     const check = () => {
+      pendingReadinessFrame = void 0;
       if (token !== imperativeScrollToken) {
         return;
       }
@@ -7144,9 +7383,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
         run();
         return;
       }
-      requestAnimationFrame(check);
+      pendingReadinessFrame = requestAnimationFrame(check);
     };
-    requestAnimationFrame(check);
+    cancelPendingReadinessFrame();
+    pendingReadinessFrame = requestAnimationFrame(check);
   };
   const runScrollRequest = (token, resolve, run, isReady = () => true) => {
     const runNow = () => {
@@ -7169,12 +7409,22 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   };
   const startImperativeScroll = (resolve) => {
     var _a3;
+    cancelPendingReadinessFrame();
     const token = ++imperativeScrollToken;
     state.pendingScrollToEnd = void 0;
     (_a3 = state.pendingScrollResolve) == null ? void 0 : _a3.call(state);
     state.pendingScrollResolve = resolve;
     return token;
   };
+  state.disposeImperativeHandle = () => {
+    imperativeScrollToken++;
+    cancelPendingReadinessFrame();
+    cancelPendingNonAnimatedScrollCompletion(state);
+    state.pendingScrollToEnd = void 0;
+    const resolvePendingScroll = state.pendingScrollResolve;
+    state.pendingScrollResolve = void 0;
+    resolvePendingScroll == null ? void 0 : resolvePendingScroll();
+  };
   const runScrollWithPromise = (run, isReady = () => true) => new Promise((resolve) => {
     const token = startImperativeScroll(resolve);
     runScrollRequest(token, resolve, run, isReady);
@@ -7215,6 +7465,8 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const mode = (_a3 = options == null ? void 0 : options.mode) != null ? _a3 : "sizes";
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7235,6 +7487,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     (_b = state.triggerCalculateItemsInView) == null ? void 0 : _b.call(state, { forceFullItemPositions: true });
   };
   return {
+    cancelInitialScrollPreservation: () => cancelInitialScrollPreservation(ctx),
     clearCaches,
     flashScrollIndicators: () => refScroller.current.flashScrollIndicators(),
     getAnimatableRef: () => {
@@ -7272,7 +7525,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
       scroll: state.scroll,
       scrollLength: state.scrollLength,
       scrollVelocity: getScrollVelocity(state),
-      sizeAtIndex: (index) => state.sizesKnown.get(getId(state, index)),
+      sizeAtIndex: (index) => {
+        const key = getId(state, index);
+        return getKnownItemSize(ctx, key, index, state.props.data[index]);
+      },
       sizes: state.sizesKnown,
       start: state.startNoBuffer,
       startBuffered: state.startBuffered
@@ -7453,7 +7709,8 @@ function normalizeMaintainScrollAtEnd(value) {
   const normalizedTriggers = normalizeMaintainScrollAtEndOn(value.on, "on" in value);
   return {
     ...normalizedTriggers,
-    animated: (_a3 = value.animated) != null ? _a3 : false
+    animated: (_a3 = value.animated) != null ? _a3 : false,
+    isMaintainingScrollAtEnd: value.isMaintainingScrollAtEnd
   };
 }
 
@@ -7583,6 +7840,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
+    getItemSizeVersion,
     getItemType,
     horizontal,
     rtl,
@@ -7775,6 +8034,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         scrollTime: 0,
         sizes: /* @__PURE__ */ new Map(),
         sizesKnown: /* @__PURE__ */ new Map(),
+        itemSizeVersions: /* @__PURE__ */ new Map(),
+        sizeContributions: /* @__PURE__ */ new Map(),
         startBuffered: -1,
         startNoBuffer: -1,
         startReachedSnapshot: void 0,
@@ -7840,6 +8101,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
+    getItemSizeVersion: useWrapIfItem(getItemSizeVersion),
     getItemType: useWrapIfItem(getItemType),
     horizontal: !!horizontal,
     itemsAreEqual,
@@ -7881,7 +8144,10 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
   if (shouldResetFreshDataLayout) {
     resetInitialRenderState(ctx, {
       resetInitialScroll: !!initialScrollProp,
-      resetLayout: true
+      resetLayout: true,
+      // HAPPIER-MONOTONIC-DATASET-VISIBILITY: a transient empty projection of the same
+      // dataset still needs fresh layout, but must not turn an already painted list opaque.
+      preserveReadyToRender: !didDataKeyChangeLocal && state.didLoad
     });
   }
   const memoizedLastItemKeys = React3.useMemo(() => {
@@ -8109,10 +8375,20 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         cancelAnimationFrame(state.queuedFullDrawDistancePrewarm);
         state.queuedFullDrawDistancePrewarm = void 0;
       }
+      if (state.animFrameCheckFinishedScroll !== void 0) {
+        cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+        state.animFrameCheckFinishedScroll = void 0;
+      }
+      if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+        clearTimeout(state.timeoutCheckFinishedScrollFallback);
+        state.timeoutCheckFinishedScrollFallback = void 0;
+      }
       for (const timeout of state.timeouts) {
         clearTimeout(timeout);
       }
       state.timeouts.clear();
+      state.disposeImperativeHandle == null ? void 0 : state.disposeImperativeHandle();
+      state.disposeImperativeHandle = void 0;
     };
   }, [state]);
   React3.useLayoutEffect(() => {
diff --git a/node_modules/@legendapp/list/react-native.web.mjs b/node_modules/@legendapp/list/react-native.web.mjs
index 6b2d901..0666b1a 100644
--- a/node_modules/@legendapp/list/react-native.web.mjs
+++ b/node_modules/@legendapp/list/react-native.web.mjs
@@ -852,7 +852,8 @@ function scheduleFullDrawDistancePrewarm(ctx) {
 // src/utils/setInitialRenderState.ts
 function resetInitialRenderState(ctx, {
   resetLayout,
-  resetInitialScroll
+  resetInitialScroll,
+  preserveReadyToRender
 }) {
   const { state } = ctx;
   if (resetLayout) {
@@ -862,7 +863,9 @@ function resetInitialRenderState(ctx, {
   if (resetInitialScroll) {
     state.didFinishInitialScroll = false;
   }
-  set$(ctx, "readyToRender", false);
+  if (!preserveReadyToRender) {
+    set$(ctx, "readyToRender", false);
+  }
   resetAdaptiveRender(ctx);
 }
 function setInitialRenderState(ctx, {
@@ -915,6 +918,38 @@ function clearPreservedInitialScrollTarget(state) {
   state.initialScroll = void 0;
   setInitialScrollSession(state);
 }
+function cancelInitialScrollPreservation(ctx) {
+  const state = ctx.state;
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTargetTimeout(state);
+  clearBootstrapInitialScrollSession(state);
+  const activeInitialScroll = state.scrollingTo;
+  if (activeInitialScroll == null ? void 0 : activeInitialScroll.isInitialScroll) {
+    const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+    if ((pendingCompletion == null ? void 0 : pendingCompletion.scrollingTo) === activeInitialScroll) {
+      cancelPendingNonAnimatedScrollCompletion(state);
+    }
+    if (state.animFrameCheckFinishedScroll !== void 0) {
+      cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+      state.animFrameCheckFinishedScroll = void 0;
+    }
+    if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+      clearTimeout(state.timeoutCheckFinishedScrollFallback);
+      state.timeoutCheckFinishedScrollFallback = void 0;
+    }
+    if (state.scrollingTo === activeInitialScroll) {
+      state.scrollingTo = void 0;
+      state.scrollTargetPinnedRange = void 0;
+    }
+  }
+  clearPreservedInitialScrollTarget(state);
+  clearInitialScrollSession(state);
+  setInitialRenderState(ctx, { didInitialScroll: true });
+  state.pendingMaintainScrollAtEnd = false;
+  if (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "pending-instant") {
+    state.maintainingScrollAtEnd = void 0;
+  }
+}
 function finishInitialScroll(ctx, options) {
   var _a3, _b, _c;
   const state = ctx.state;
@@ -952,6 +987,9 @@ function finishInitialScroll(ctx, options) {
       recalculateSettledScroll(ctx);
     }
     setInitialRenderState(ctx, { didInitialScroll: true });
+    if (state.pendingMaintainScrollAtEnd) {
+      doMaintainScrollAtEnd(ctx);
+    }
     if (shouldReleaseDeferredPublicOnScroll) {
       releaseDeferredPublicOnScroll(ctx, finalScrollOffset);
     }
@@ -1007,11 +1045,25 @@ function updateContentMetricsState(ctx) {
 }
 
 // src/core/addTotalSize.ts
+// HAPPIER-NULLKEY-ABSOLUTE-TOTAL (owner half): `addTotalSize` overloaded `key === null` as its
+// ABSOLUTE-WRITE sentinel ("this number IS the total"), and `getId` returns `null` for any index
+// past the end of `data` ("there is no such row"). Two meanings, one value. One probe of a row the
+// current `data` no longer contains therefore assigned the whole content size to be one unmeasured
+// row: getId -> null -> getItemSize falls through to `estimatedItemSize` -> setSize with a null key
+// -> addTotalSize with a null key -> totalSize = est, written absolutely. MEASURED on device with a
+// 4/4 identical stack (checkResetContainers -> calculateItemsInView -> prepareMVCP's closure ->
+// getItemSize(ctx, getId(state, scrollingTo.index), ...)): totalSize 2,322 -> 240 on a 43-row
+// transcript AND 266,020 -> 240 on a 419-row one - the SAME constant, i.e. one row rather than a
+// sum - then alignItemsAtEndPadding grown to 297 (12 + 142 + 240 + 297 = 691, the viewport, to the
+// pixel) and UIScrollView clamping contentOffset to that one-viewport content.
+// The sentinel is given a private value no item key can ever take, so the invalid state stops being
+// representable rather than being guarded against at one caller.
+var HAPPIER_TOTAL_SIZE_ABSOLUTE = { happierTotalSizeAbsolute: true };
 function addTotalSize(ctx, key, add, notifyTotalSize = true) {
   const state = ctx.state;
   const prevTotalSize = state.totalSize;
   let totalSize = state.totalSize;
-  if (key === null) {
+  if (key === HAPPIER_TOTAL_SIZE_ABSOLUTE) {
     totalSize = add;
     if (state.timeoutSetPaddingTop) {
       clearTimeout(state.timeoutSetPaddingTop);
@@ -1036,6 +1088,17 @@ function addTotalSize(ctx, key, add, notifyTotalSize = true) {
 
 // src/core/setSize.ts
 function setSize(ctx, itemKey, size, notifyTotalSize = true) {
+  // HAPPIER-NULLKEY-NO-ROW-NO-SIZE (choke-point half): `getId` returns `null` for an index past the
+  // end of `data`, and ~15 call sites hand a `getId` result straight to `getItemSize` with no null
+  // check (the MEASURED device stack goes through prepareMVCP's closure, NOT through
+  // `getItemSizeAtIndex`, so guarding that one caller would not have covered it). `setSize` is the
+  // single choke point every resolved size passes, so refusing a key that is not a row here keeps a
+  // non-existent row from contributing to the running total or occupying a `sizes` entry, for every
+  // present and future caller. Paired with HAPPIER-NULLKEY-ABSOLUTE-TOTAL: that one removes the
+  // ambiguity, this one removes the phantom contribution the disambiguation would otherwise leave.
+  if (itemKey === null) {
+    return;
+  }
   const state = ctx.state;
   const { sizes } = state;
   const previousSize = sizes.get(itemKey);
@@ -1091,11 +1154,58 @@ function findContainerId(ctx, key) {
 }
 
 // src/utils/getItemSize.ts
-function getKnownOrFixedSize(ctx, key, index, data, resolved) {
+function reverseItemSizeContribution(state, key) {
+  const contribution = state.sizeContributions.get(key);
+  if (contribution === void 0) {
+    return;
+  }
+  state.sizeContributions.delete(key);
+  const averages = state.averageSizes[contribution.itemType];
+  if (!averages) {
+    return;
+  }
+  if (averages.num <= 1) {
+    delete state.averageSizes[contribution.itemType];
+    return;
+  }
+  const remainingNum = averages.num - 1;
+  averages.avg = (averages.avg * averages.num - contribution.size) / remainingNum;
+  averages.num = remainingNum;
+}
+function validateItemSizeVersion(ctx, key, index, data) {
+  const state = ctx.state;
+  const getItemSizeVersion = state.props.getItemSizeVersion;
+  if (!getItemSizeVersion) {
+    return;
+  }
+  const itemSizeVersions = state.itemSizeVersions;
+  const nextVersion = getItemSizeVersion(data, index);
+  if (itemSizeVersions.has(key) && itemSizeVersions.get(key) !== nextVersion) {
+    reverseItemSizeContribution(state, key);
+    state.sizesKnown.delete(key);
+    const previouslyContributedSize = state.sizes.get(key);
+    if (previouslyContributedSize !== void 0) {
+      addTotalSize(ctx, key, -previouslyContributedSize, false);
+    }
+    state.sizes.delete(key);
+    const mountedContainerId = state.containerItemKeys.get(key);
+    if (mountedContainerId !== void 0) {
+      scheduleContainerLayout(ctx, mountedContainerId);
+    }
+  }
+  itemSizeVersions.set(key, nextVersion);
+}
+function getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion = false) {
+  if (!didValidateItemSizeVersion) {
+    validateItemSizeVersion(ctx, key, index, data);
+  }
+  return ctx.state.sizesKnown.get(key);
+}
+function getKnownOrFixedSize(ctx, key, index, data, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const { getFixedItemSize, getItemType } = state.props;
-  let size = key ? state.sizesKnown.get(key) : void 0;
+  let size = key ? getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion) : void 0;
   if (size === void 0 && key && getFixedItemSize) {
     const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
     const fixedSize = (resolved == null ? void 0 : resolved.didResolveFixedItemSize) ? resolved.fixedItemSize : getFixedItemSize(data, index, itemType);
@@ -1118,7 +1228,7 @@ function areKnownOrFixedItemSizesAvailable(ctx, startIndex, endIndex) {
   }
   return true;
 }
-function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved) {
+function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b, _c, _d;
   const state = ctx.state;
   const {
@@ -1127,7 +1237,7 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     props: { estimatedItemSize, getItemType },
     scrollingTo
   } = state;
-  const sizeKnown = state.sizesKnown.get(key);
+  const sizeKnown = getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion);
   if (sizeKnown !== void 0) {
     return sizeKnown;
   }
@@ -1138,12 +1248,23 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
       return renderedSize;
     }
   }
-  size = getKnownOrFixedSize(ctx, key, index, data, resolved);
+  size = getKnownOrFixedSize(ctx, key, index, data, resolved, true);
   if (size !== void 0) {
     setSize(ctx, key, size, notifyTotalSize);
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  if (size === void 0) {
+    const getEstimatedItemSizeProp = state.props.getEstimatedItemSize;
+    if (getEstimatedItemSizeProp) {
+      const estimatedForItem = getEstimatedItemSizeProp(data, index, itemType);
+      if (estimatedForItem !== void 0) {
+        size = roundSize(estimatedForItem);
+        setSize(ctx, key, size, notifyTotalSize);
+        return size;
+      }
+    }
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1223,18 +1344,34 @@ function clampScrollOffset(ctx, offset, scrollTarget) {
 }
 
 // src/core/finishScrollTo.ts
-function finishScrollTo(ctx) {
+function cancelPendingNonAnimatedScrollCompletion(state) {
+  if (!state) return;
+  const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+  state.pendingNonAnimatedScrollCompletion = void 0;
+  pendingCompletion == null ? void 0 : pendingCompletion.cancel();
+}
+function finishScrollTo(ctx, requestedCompletion) {
   var _a3, _b;
   const state = ctx.state;
+  const pendingCompletion = requestedCompletion != null ? requestedCompletion : state == null ? void 0 : state.pendingNonAnimatedScrollCompletion;
+  if (pendingCompletion && pendingCompletion.scrollingTo !== (state == null ? void 0 : state.scrollingTo)) {
+    return;
+  }
+  if (pendingCompletion && state.pendingNonAnimatedScrollCompletion === pendingCompletion) {
+    state.pendingNonAnimatedScrollCompletion = void 0;
+    pendingCompletion.cancel();
+  }
   if (state == null ? void 0 : state.scrollingTo) {
-    const resolvePendingScroll = state.pendingScrollResolve;
-    state.pendingScrollResolve = void 0;
+    const resolvePendingScroll = pendingCompletion ? pendingCompletion.resolve : state.pendingScrollResolve;
+    if (!pendingCompletion || state.pendingScrollResolve === pendingCompletion.resolve) {
+      state.pendingScrollResolve = void 0;
+    }
     const scrollingTo = state.scrollingTo;
     state.scrollHistory.length = 0;
     state.scrollingTo = void 0;
     state.scrollTargetPinnedRange = void 0;
     if (state.pendingTotalSize !== void 0) {
-      addTotalSize(ctx, null, state.pendingTotalSize);
+      addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, state.pendingTotalSize);
     }
     {
       state.scrollAdjustHandler.commitPendingAdjust(scrollingTo);
@@ -1278,6 +1415,7 @@ function doScrollTo(ctx, params) {
   const contentSize = isHorizontal ? getContentSize(ctx) : void 0;
   const left = isHorizontal ? toNativeHorizontalOffset(state, offset, contentSize) : 0;
   const top = isHorizontal ? 0 : offset;
+  cancelPendingNonAnimatedScrollCompletion(state);
   scroller.scrollTo({ animated: isAnimated, x: left, y: top });
   if (isAnimated) {
     const target = (_b = (_a3 = scroller.getScrollEventTarget) == null ? void 0 : _a3.call(scroller)) != null ? _b : null;
@@ -1288,9 +1426,16 @@ function doScrollTo(ctx, params) {
     });
   } else {
     state.scroll = offset;
-    setTimeout(() => {
-      finishScrollTo(ctx);
-    }, 100);
+    const scrollingTo = state.scrollingTo;
+    const resolve = state.pendingScrollResolve;
+    let completionTimeout;
+    const completion = {
+      cancel: () => clearTimeout(completionTimeout),
+      resolve,
+      scrollingTo
+    };
+    completionTimeout = setTimeout(() => finishScrollTo(ctx, completion), 100);
+    state.pendingNonAnimatedScrollCompletion = completion;
   }
 }
 function listenForScrollEnd(ctx, params) {
@@ -1346,6 +1491,21 @@ function listenForScrollEnd(ctx, params) {
 }
 
 // src/core/doMaintainScrollAtEnd.ts
+function hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd) {
+  return !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+}
+function retireFinishedInitialScrollForMaintainedEnd(ctx) {
+  const state = ctx.state;
+  if (!state.initialScroll) {
+    return true;
+  }
+  if (!state.didFinishInitialScroll || state.scrollingTo && state.scrollingTo.isInitialScroll) {
+    return false;
+  }
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTarget(state);
+  return true;
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
   const {
@@ -1355,7 +1515,14 @@ function doMaintainScrollAtEnd(ctx) {
     props: { maintainScrollAtEnd }
   } = state;
   const isWithinMaintainScrollAtEndThreshold = peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
-  const shouldMaintainScrollAtEnd = !!(isWithinMaintainScrollAtEndThreshold && maintainScrollAtEnd && didContainersLayout);
+  const hasExternalMaintainScrollAtEndIntent2 = hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+  const shouldMaintainScrollAtEnd = !!((isWithinMaintainScrollAtEndThreshold || hasExternalMaintainScrollAtEndIntent2) && maintainScrollAtEnd && didContainersLayout);
+  if (shouldMaintainScrollAtEnd && state.initialScroll) {
+    state.pendingMaintainScrollAtEnd = true;
+    if (!retireFinishedInitialScrollForMaintainedEnd(ctx)) {
+      return false;
+    }
+  }
   if (pendingNativeMVCPAdjust) {
     state.pendingMaintainScrollAtEnd = shouldMaintainScrollAtEnd;
     return false;
@@ -1371,7 +1538,10 @@ function doMaintainScrollAtEnd(ctx) {
       const activeState = maintainScrollAtEnd.animated ? "animated" : "instant";
       state.maintainingScrollAtEnd = pendingState;
       requestAnimationFrame(() => {
-        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold")) {
+        if (state.maintainingScrollAtEnd !== pendingState) {
+          return;
+        }
+        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold") || typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd()) {
           state.maintainingScrollAtEnd = activeState;
           const scroller = refScroller.current;
           if (state.props.horizontal && isHorizontalRTL(state)) {
@@ -1716,7 +1886,11 @@ function prepareMVCP(ctx, dataChanged) {
         return;
       }
       if (Math.abs(positionDiff) > MVCP_POSITION_EPSILON) {
-        const shouldSkipAdjustForMaintainedEnd = (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "animated") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const maintainScrollAtEnd = props.maintainScrollAtEnd;
+        const maintainingScrollAtEnd = state.maintainingScrollAtEnd;
+        const hasExternalMaintainedEndOwnership = state.didContainersLayout && hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+        const hasActiveMaintainedEndOwnership = !!maintainScrollAtEnd && (maintainingScrollAtEnd === "pending-animated" || maintainingScrollAtEnd === "animated" || maintainingScrollAtEnd === "pending-instant" || maintainingScrollAtEnd === "instant") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const shouldSkipAdjustForMaintainedEnd = scrollTarget === void 0 && (hasExternalMaintainedEndOwnership || hasActiveMaintainedEndOwnership);
         if (!shouldSkipAdjustForMaintainedEnd) {
           requestAdjust(ctx, positionDiff);
         }
@@ -1788,7 +1962,6 @@ function isInMVCPActiveMode(state) {
 
 // src/core/updateScroll.ts
 function updateScroll(ctx, newScroll, forceUpdate, options) {
-  var _a3;
   const state = ctx.state;
   const { ignoreScrollFromMVCP, lastScrollAdjustForHistory, scrollAdjustHandler, scrollHistory, scrollingTo } = state;
   const prevScroll = state.scroll;
@@ -1862,7 +2035,7 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
     } else {
       runCalculateItems();
     }
-    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd || !!((_a3 = state.props.maintainScrollAtEnd) == null ? void 0 : _a3.onDataChange);
+    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd;
     if (didResolvePendingNativeMVCPAdjust && shouldMaintainScrollAtEndAfterPendingSettle) {
       state.pendingMaintainScrollAtEnd = false;
       doMaintainScrollAtEnd(ctx);
@@ -2213,7 +2386,8 @@ function advanceCurrentInitialScrollSession(ctx, options) {
 }
 
 // src/utils/checkAllSizesKnown.ts
-function checkAllSizesKnown(state, start, end) {
+function checkAllSizesKnown(ctx, start, end) {
+  const state = ctx.state;
   if (start == null || end == null || start < 0 || end < start) {
     return false;
   }
@@ -2223,7 +2397,7 @@ function checkAllSizesKnown(state, start, end) {
     if (index !== void 0 && index >= start && index <= end) {
       hasMountedIndex = true;
       const id = getId(state, index);
-      if (id === void 0 || !state.sizesKnown.has(id)) {
+      if (id === void 0 || getKnownItemSize(ctx, id, index, state.props.data[index]) === void 0) {
         return false;
       }
     }
@@ -2603,11 +2777,13 @@ function handleBootstrapInitialScrollDataChange(ctx, options) {
       setInitialScrollTarget(ctx, updatedInitialScroll, {
         resetDidFinish: shouldResetDidFinish
       });
-      rearmBootstrapInitialScroll(ctx, {
-        scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
-        seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
-        targetIndexSeed: updatedInitialScroll.index
-      });
+      if (!!bootstrapInitialScroll || shouldResetDidFinish || !state.didFinishInitialScroll) {
+        rearmBootstrapInitialScroll(ctx, {
+          scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
+          seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
+          targetIndexSeed: updatedInitialScroll.index
+        });
+      }
       return;
     }
   }
@@ -2728,7 +2904,7 @@ function evaluateBootstrapInitialScroll(ctx) {
     bootstrapInitialScroll.targetIndexSeed = void 0;
   }
   const resolvedOffset = resolveInitialScrollOffset(ctx, initialScroll);
-  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
   const didResolvedOffsetChange = Math.abs(bootstrapInitialScroll.scroll - resolvedOffset) > 1;
   const { data } = state.props;
   const visibleIndices = getBootstrapRevealVisibleIndices({
@@ -2746,7 +2922,7 @@ function evaluateBootstrapInitialScroll(ctx) {
   const areVisibleIndicesMeasured = visibleIndices.length > 0 && visibleIndices.every((index) => {
     var _a4;
     const id = (_a4 = state.idCache[index]) != null ? _a4 : getId(state, index);
-    return state.sizesKnown.has(id);
+    return getKnownItemSize(ctx, id, index, data[index]) !== void 0;
   });
   const previousResolvedOffset = bootstrapInitialScroll.previousResolvedOffset;
   const previousVisibleIndices = bootstrapInitialScroll.visibleIndices;
@@ -3325,9 +3501,27 @@ function updateTotalSize(ctx) {
     positions,
     props: { data }
   } = state;
+  // HAPPIER-TOTALSIZE-STALE-POSITIONS: `state.props` is mutated during RENDER, so `props.data`
+  // can already be the next render's array while `positions` still describes the previous one.
+  // Any event-driven pass (a delivered scroll, a layout) that runs before the reconciling
+  // `dataChanged` pass sees that pair. When the new data is SHORTER, updateItemPositions' walk
+  // range (`startIndex = minIndexSizeChanged ?? startBuffered`) is already past its end, so no row
+  // is walked and the absolute total below is computed from stale positions: with a one-row
+  // `data`, `positions[0]` is 0 and the row size is unknown, so the list publishes exactly
+  // `estimatedItemSize` for a transcript of any height. Measured on device: 2,322 -> 240 on a
+  // 43-row transcript and 266,020 -> 240 on a 419-row one (the SAME 240, i.e. one row, not a sum),
+  // alignItemsAtEndPadding then grown to fill the viewport and UIScrollView clamping
+  // contentOffset to that one-viewport content.
+  // The mirror case is already refused: when data GREW, `positions[lastIndex]` is undefined and
+  // the `lastPosition !== void 0` guard below suppresses the publish. This makes the shrink
+  // direction symmetric. Nothing is lost by waiting - the reconciling pass walks from index 0,
+  // leaves `positions.length === data.length`, and republishes the exact total.
+  if (positions.length > data.length) {
+    return;
+  }
   const numColumns = (_a3 = peek$(ctx, "numColumns")) != null ? _a3 : 1;
   if (data.length === 0) {
-    addTotalSize(ctx, null, 0);
+    addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, 0);
   } else {
     const lastIndex = data.length - 1;
     const lastId = getId(state, lastIndex);
@@ -3350,12 +3544,12 @@ function updateTotalSize(ctx) {
             maxSize = size;
           }
         }
-        addTotalSize(ctx, null, lastPosition + maxSize);
+        addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, lastPosition + maxSize);
       } else {
         const lastSize = getItemSize(ctx, lastId, lastIndex, data[lastIndex]);
         if (lastSize !== void 0) {
           const totalSize = lastPosition + lastSize;
-          addTotalSize(ctx, null, totalSize);
+          addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, totalSize);
         }
       }
     }
@@ -3418,7 +3612,7 @@ function updateItemPositions(ctx, dataChanged, {
   const shouldOptimize = !forceFullUpdate && !dataChanged && (optimizeForVisibleWindow || Math.abs(velocity) > 0 || state.scrollLength > 0 && lastScrollDelta > state.scrollLength);
   const maxVisibleArea = scrollBottomBuffered + 1e3;
   const useAverageSize = true;
-  const preferCachedSize = !doMVCP || dataChanged || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
+  const preferCachedSize = !doMVCP || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
   const notifyTotalSizeWhileCachingSizes = false;
   let currentRowTop = 0;
   let column = 1;
@@ -3447,7 +3641,7 @@ function updateItemPositions(ctx, dataChanged, {
       const prevIndex = startIndex - 1;
       const prevId = getId(state, prevIndex);
       const prevPosition = (_c = positions[prevIndex]) != null ? _c : 0;
-      const prevSize = (_d = sizesKnown.get(prevId)) != null ? _d : getItemSize(
+      const prevSize = getItemSize(
         ctx,
         prevId,
         prevIndex,
@@ -3465,7 +3659,25 @@ function updateItemPositions(ctx, dataChanged, {
   let breakAt;
   for (let i = startIndex; i < dataLength; i++) {
     if (shouldOptimize && breakAt !== void 0 && i > breakAt) {
-      didBreakEarly = true;
+      const staleRowTop = positions[i];
+      if (staleRowTop === void 0) {
+        didBreakEarly = true;
+        break;
+      }
+      const positionShift = currentRowTop - staleRowTop;
+      if (positionShift !== 0) {
+        for (let j = i; j < dataLength; j++) {
+          const stalePosition = positions[j];
+          if (stalePosition === void 0) {
+            break;
+          }
+          const shiftedPosition = stalePosition + positionShift;
+          positions[j] = shiftedPosition;
+          if (hasPositionListeners) {
+            notifyPosition$(ctx, idCache[j] !== void 0 ? idCache[j] : getId(state, j), shiftedPosition);
+          }
+        }
+      }
       break;
     }
     if (shouldOptimize && breakAt === void 0 && !scrollingTo && !dataChanged && currentRowTop > maxVisibleArea) {
@@ -3487,8 +3699,7 @@ function updateItemPositions(ctx, dataChanged, {
       column = 1;
       maxSizeInRow = 0;
     }
-    const knownSize = sizesKnown.get(id);
-    const size = knownSize !== void 0 ? knownSize : getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
+    const size = getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
     if (IS_DEV && needsIndexByKey) {
       if (indexByKeyForChecking.has(id)) {
         console.error(
@@ -4380,6 +4591,17 @@ function calculateItemsInView(ctx, params = {}) {
         }
       }
     }
+    if (startBuffered === null && endBuffered === null && dataLength > 0) {
+      const lastIndexInData = dataLength - 1;
+      const lastIdInData = idCache[lastIndexInData] != null ? idCache[lastIndexInData] : getId(state, lastIndexInData);
+      const lastTopInData = positions[lastIndexInData];
+      const lastSizeInData = sizes.get(lastIdInData) != null ? sizes.get(lastIdInData) : getItemSize(ctx, lastIdInData, lastIndexInData, data[lastIndexInData]);
+      if (lastTopInData !== void 0 && lastSizeInData !== void 0 && lastTopInData + lastSizeInData <= scroll) {
+        startBuffered = lastIndexInData;
+        startBufferedId = lastIdInData;
+        endBuffered = lastIndexInData;
+      }
+    }
     Object.assign(state, {
       endBuffered,
       endNoBuffer: visibleRange.endNoBuffer,
@@ -4583,7 +4805,7 @@ function calculateItemsInView(ctx, params = {}) {
     }
     maybeEmitFirstVisibleItemChanged(state, visibleRange.startNoBuffer);
     if (!queuedInitialLayout && !state.didContainersLayout) {
-      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(state, state.startBuffered, state.endBuffered) : checkAllSizesKnown(state, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered) : checkAllSizesKnown(ctx, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
       if (isInitialLayoutReady) {
         setDidLayout(ctx);
         handleInitialScrollLayoutReady(ctx);
@@ -4769,11 +4991,16 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   } = state;
   if (!data) return { didMeasureUserScrollAnchorResetItem };
   const index = state.indexByKey.get(itemKey);
+  const itemData = index === void 0 ? void 0 : state.props.data[index];
+  if (index === void 0 && state.props.getItemSizeVersion) {
+    return { didMeasureUserScrollAnchorResetItem };
+  }
+  const didValidateItemSizeVersion = index !== void 0;
+  const prevSizeKnown = didValidateItemSizeVersion ? getKnownItemSize(ctx, itemKey, index, itemData) : state.sizesKnown.get(itemKey);
   if (getFixedItemSize) {
     if (index === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
-    const itemData = state.props.data[index];
     if (itemData === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4786,7 +5013,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       };
     }
     const size2 = resolvedMeasurementItem.fixedItemSize;
-    if (size2 !== void 0 && size2 === sizesKnown.get(itemKey)) {
+    if (size2 !== void 0 && size2 === prevSizeKnown) {
       updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4794,8 +5021,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   let needsRecalculate = !didContainersLayout;
   let shouldMaintainScrollAtEnd = false;
   let minIndexSizeChanged;
-  const prevSizeKnown = state.sizesKnown.get(itemKey);
-  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem);
+  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion);
   const size = roundSize(horizontal ? sizeObj.width : sizeObj.height);
   if (diff !== 0) {
     minIndexSizeChanged = minIndexSizeChanged !== void 0 ? Math.min(minIndexSizeChanged, index) : index;
@@ -4804,7 +5030,8 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     if (!needsRecalculate && state.containerItemKeys.has(itemKey)) {
       needsRecalculate = true;
     }
-    if (prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
+    const hasExternalMaintainScrollAtEndIntent = !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+    if (hasExternalMaintainScrollAtEndIntent || prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
       shouldMaintainScrollAtEnd = true;
     }
     onItemSizeChanged == null ? void 0 : onItemSizeChanged({
@@ -4820,7 +5047,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     state.minIndexSizeChanged = state.minIndexSizeChanged !== void 0 ? Math.min(state.minIndexSizeChanged, minIndexSizeChanged) : minIndexSizeChanged;
   }
   updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
-  if (didContainersLayout || checkAllSizesKnown(state, state.startBuffered, state.endBuffered)) {
+  if (didContainersLayout || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered)) {
     const canMaintainScrollAtEnd = shouldMaintainScrollAtEnd && !!(maintainScrollAtEnd == null ? void 0 : maintainScrollAtEnd.onItemLayout);
     return {
       didChange: diff !== 0,
@@ -4834,7 +5061,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     didMeasureUserScrollAnchorResetItem
   };
 }
-function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
+function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const {
@@ -4857,7 +5084,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     fixedItemSize,
     itemType
   } : void 0;
-  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize);
+  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize, didValidateItemSizeVersion);
   const rawSize = horizontal ? sizeObj.width : sizeObj.height;
   const prevSizeKnown = sizesKnown.get(itemKey);
   const size = Math.round(rawSize) ;
@@ -4877,6 +5104,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       averages.avg = (averages.avg * averages.num + size) / (averages.num + 1);
       averages.num++;
     }
+    state.sizeContributions.set(itemKey, { itemType, size });
   }
   if (!prevSize || Math.abs(prevSize - size) > 0.1) {
     setSize(ctx, itemKey, size);
@@ -5338,7 +5566,8 @@ function processContainerLayout({ containerId, ctx, rectangle, ref, state }) {
   const axis = state.horizontal ? "width" : "height";
   const size = roundSize(rectangle[axis]);
   state.lastSize ? roundSize(state.lastSize[axis]) : void 0;
-  const coreKnownSize = listState.sizesKnown.get(currentItemKey);
+  const currentItemIndex = listState.indexByKey.get(currentItemKey);
+  const coreKnownSize = currentItemIndex === void 0 ? void 0 : getKnownItemSize(ctx, currentItemKey, currentItemIndex, listState.props.data[currentItemIndex]);
   const previousSize = coreKnownSize ;
   const applyLayout = () => {
     state.lastSize = layout;
@@ -6670,6 +6899,8 @@ function checkResetContainers(ctx, dataProp, { didColumnsChange = false } = {})
   if (didColumnsChange) {
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7089,6 +7320,13 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   const IMPERATIVE_SCROLL_SETTLE_MAX_WAIT_MS = 800;
   const IMPERATIVE_SCROLL_SETTLE_STABLE_FRAMES = 2;
   let imperativeScrollToken = 0;
+  let pendingReadinessFrame;
+  const cancelPendingReadinessFrame = () => {
+    if (pendingReadinessFrame !== void 0) {
+      cancelAnimationFrame(pendingReadinessFrame);
+      pendingReadinessFrame = void 0;
+    }
+  };
   const isSettlingAfterDataChange = () => !!state.didDataChange || !!state.didColumnsChange || state.queuedMVCPRecalculate !== void 0 || state.ignoreScrollFromMVCP !== void 0;
   const isScrollToIndexReady = (targetIndex, allowEmpty = false) => {
     var _a3;
@@ -7110,6 +7348,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const startedAt = Date.now();
     let stableFrames = 0;
     const check = () => {
+      pendingReadinessFrame = void 0;
       if (token !== imperativeScrollToken) {
         return;
       }
@@ -7123,9 +7362,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
         run();
         return;
       }
-      requestAnimationFrame(check);
+      pendingReadinessFrame = requestAnimationFrame(check);
     };
-    requestAnimationFrame(check);
+    cancelPendingReadinessFrame();
+    pendingReadinessFrame = requestAnimationFrame(check);
   };
   const runScrollRequest = (token, resolve, run, isReady = () => true) => {
     const runNow = () => {
@@ -7148,12 +7388,22 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   };
   const startImperativeScroll = (resolve) => {
     var _a3;
+    cancelPendingReadinessFrame();
     const token = ++imperativeScrollToken;
     state.pendingScrollToEnd = void 0;
     (_a3 = state.pendingScrollResolve) == null ? void 0 : _a3.call(state);
     state.pendingScrollResolve = resolve;
     return token;
   };
+  state.disposeImperativeHandle = () => {
+    imperativeScrollToken++;
+    cancelPendingReadinessFrame();
+    cancelPendingNonAnimatedScrollCompletion(state);
+    state.pendingScrollToEnd = void 0;
+    const resolvePendingScroll = state.pendingScrollResolve;
+    state.pendingScrollResolve = void 0;
+    resolvePendingScroll == null ? void 0 : resolvePendingScroll();
+  };
   const runScrollWithPromise = (run, isReady = () => true) => new Promise((resolve) => {
     const token = startImperativeScroll(resolve);
     runScrollRequest(token, resolve, run, isReady);
@@ -7194,6 +7444,8 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const mode = (_a3 = options == null ? void 0 : options.mode) != null ? _a3 : "sizes";
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7214,6 +7466,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     (_b = state.triggerCalculateItemsInView) == null ? void 0 : _b.call(state, { forceFullItemPositions: true });
   };
   return {
+    cancelInitialScrollPreservation: () => cancelInitialScrollPreservation(ctx),
     clearCaches,
     flashScrollIndicators: () => refScroller.current.flashScrollIndicators(),
     getAnimatableRef: () => {
@@ -7251,7 +7504,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
       scroll: state.scroll,
       scrollLength: state.scrollLength,
       scrollVelocity: getScrollVelocity(state),
-      sizeAtIndex: (index) => state.sizesKnown.get(getId(state, index)),
+      sizeAtIndex: (index) => {
+        const key = getId(state, index);
+        return getKnownItemSize(ctx, key, index, state.props.data[index]);
+      },
       sizes: state.sizesKnown,
       start: state.startNoBuffer,
       startBuffered: state.startBuffered
@@ -7432,7 +7688,8 @@ function normalizeMaintainScrollAtEnd(value) {
   const normalizedTriggers = normalizeMaintainScrollAtEndOn(value.on, "on" in value);
   return {
     ...normalizedTriggers,
-    animated: (_a3 = value.animated) != null ? _a3 : false
+    animated: (_a3 = value.animated) != null ? _a3 : false,
+    isMaintainingScrollAtEnd: value.isMaintainingScrollAtEnd
   };
 }
 
@@ -7562,6 +7819,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
+    getItemSizeVersion,
     getItemType,
     horizontal,
     rtl,
@@ -7754,6 +8013,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         scrollTime: 0,
         sizes: /* @__PURE__ */ new Map(),
         sizesKnown: /* @__PURE__ */ new Map(),
+        itemSizeVersions: /* @__PURE__ */ new Map(),
+        sizeContributions: /* @__PURE__ */ new Map(),
         startBuffered: -1,
         startNoBuffer: -1,
         startReachedSnapshot: void 0,
@@ -7819,6 +8080,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
+    getItemSizeVersion: useWrapIfItem(getItemSizeVersion),
     getItemType: useWrapIfItem(getItemType),
     horizontal: !!horizontal,
     itemsAreEqual,
@@ -7860,7 +8123,10 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
   if (shouldResetFreshDataLayout) {
     resetInitialRenderState(ctx, {
       resetInitialScroll: !!initialScrollProp,
-      resetLayout: true
+      resetLayout: true,
+      // HAPPIER-MONOTONIC-DATASET-VISIBILITY: a transient empty projection of the same
+      // dataset still needs fresh layout, but must not turn an already painted list opaque.
+      preserveReadyToRender: !didDataKeyChangeLocal && state.didLoad
     });
   }
   const memoizedLastItemKeys = useMemo(() => {
@@ -8088,10 +8354,20 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         cancelAnimationFrame(state.queuedFullDrawDistancePrewarm);
         state.queuedFullDrawDistancePrewarm = void 0;
       }
+      if (state.animFrameCheckFinishedScroll !== void 0) {
+        cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+        state.animFrameCheckFinishedScroll = void 0;
+      }
+      if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+        clearTimeout(state.timeoutCheckFinishedScrollFallback);
+        state.timeoutCheckFinishedScrollFallback = void 0;
+      }
       for (const timeout of state.timeouts) {
         clearTimeout(timeout);
       }
       state.timeouts.clear();
+      state.disposeImperativeHandle == null ? void 0 : state.disposeImperativeHandle();
+      state.disposeImperativeHandle = void 0;
     };
   }, [state]);
   useLayoutEffect(() => {
diff --git a/node_modules/@legendapp/list/react.d.ts b/node_modules/@legendapp/list/react.d.ts
index db7841d..f00d3a3 100644
--- a/node_modules/@legendapp/list/react.d.ts
+++ b/node_modules/@legendapp/list/react.d.ts
@@ -222,6 +222,12 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Version token for the item size. Changing it invalidates only that item’s cached size.
+     * An observed `undefined` token is distinct from an item that has not been observed yet.
+     */
+    getItemSizeVersion?: (item: ItemT, index: number) => Key | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
@@ -515,6 +521,12 @@ interface MaintainScrollAtEndOptions {
      * Whether maintainScrollAtEnd should animate when it scrolls to the end.
      */
     animated?: boolean;
+    /**
+     * Reports an authoritative semantic end-follow intent. Event-driven maintenance
+     * remains active while this is true even when late measurement moved the
+     * physical viewport beyond the proximity threshold.
+     */
+    isMaintainingScrollAtEnd?: () => boolean;
     /**
      * Which events should keep the list pinned to the end.
      * - If omitted, object values default to all triggers.
@@ -570,6 +582,10 @@ type LegendListState$1 = {
     startBuffered: number;
 };
 type LegendListRef$1 = {
+    /**
+     * Cancels retained initial-scroll correction authority without scrolling.
+     */
+    cancelInitialScrollPreservation(): void;
     /**
      * Clears internal virtualization caches.
      * @param options - Cache clearing options.
diff --git a/node_modules/@legendapp/list/react.js b/node_modules/@legendapp/list/react.js
index 544cca8..6bac4eb 100644
--- a/node_modules/@legendapp/list/react.js
+++ b/node_modules/@legendapp/list/react.js
@@ -873,7 +873,8 @@ function scheduleFullDrawDistancePrewarm(ctx) {
 // src/utils/setInitialRenderState.ts
 function resetInitialRenderState(ctx, {
   resetLayout,
-  resetInitialScroll
+  resetInitialScroll,
+  preserveReadyToRender
 }) {
   const { state } = ctx;
   if (resetLayout) {
@@ -883,7 +884,9 @@ function resetInitialRenderState(ctx, {
   if (resetInitialScroll) {
     state.didFinishInitialScroll = false;
   }
-  set$(ctx, "readyToRender", false);
+  if (!preserveReadyToRender) {
+    set$(ctx, "readyToRender", false);
+  }
   resetAdaptiveRender(ctx);
 }
 function setInitialRenderState(ctx, {
@@ -936,6 +939,38 @@ function clearPreservedInitialScrollTarget(state) {
   state.initialScroll = void 0;
   setInitialScrollSession(state);
 }
+function cancelInitialScrollPreservation(ctx) {
+  const state = ctx.state;
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTargetTimeout(state);
+  clearBootstrapInitialScrollSession(state);
+  const activeInitialScroll = state.scrollingTo;
+  if (activeInitialScroll == null ? void 0 : activeInitialScroll.isInitialScroll) {
+    const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+    if ((pendingCompletion == null ? void 0 : pendingCompletion.scrollingTo) === activeInitialScroll) {
+      cancelPendingNonAnimatedScrollCompletion(state);
+    }
+    if (state.animFrameCheckFinishedScroll !== void 0) {
+      cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+      state.animFrameCheckFinishedScroll = void 0;
+    }
+    if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+      clearTimeout(state.timeoutCheckFinishedScrollFallback);
+      state.timeoutCheckFinishedScrollFallback = void 0;
+    }
+    if (state.scrollingTo === activeInitialScroll) {
+      state.scrollingTo = void 0;
+      state.scrollTargetPinnedRange = void 0;
+    }
+  }
+  clearPreservedInitialScrollTarget(state);
+  clearInitialScrollSession(state);
+  setInitialRenderState(ctx, { didInitialScroll: true });
+  state.pendingMaintainScrollAtEnd = false;
+  if (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "pending-instant") {
+    state.maintainingScrollAtEnd = void 0;
+  }
+}
 function finishInitialScroll(ctx, options) {
   var _a3, _b, _c;
   const state = ctx.state;
@@ -973,6 +1008,9 @@ function finishInitialScroll(ctx, options) {
       recalculateSettledScroll(ctx);
     }
     setInitialRenderState(ctx, { didInitialScroll: true });
+    if (state.pendingMaintainScrollAtEnd) {
+      doMaintainScrollAtEnd(ctx);
+    }
     if (shouldReleaseDeferredPublicOnScroll) {
       releaseDeferredPublicOnScroll(ctx, finalScrollOffset);
     }
@@ -1028,11 +1066,25 @@ function updateContentMetricsState(ctx) {
 }
 
 // src/core/addTotalSize.ts
+// HAPPIER-NULLKEY-ABSOLUTE-TOTAL (owner half): `addTotalSize` overloaded `key === null` as its
+// ABSOLUTE-WRITE sentinel ("this number IS the total"), and `getId` returns `null` for any index
+// past the end of `data` ("there is no such row"). Two meanings, one value. One probe of a row the
+// current `data` no longer contains therefore assigned the whole content size to be one unmeasured
+// row: getId -> null -> getItemSize falls through to `estimatedItemSize` -> setSize with a null key
+// -> addTotalSize with a null key -> totalSize = est, written absolutely. MEASURED on device with a
+// 4/4 identical stack (checkResetContainers -> calculateItemsInView -> prepareMVCP's closure ->
+// getItemSize(ctx, getId(state, scrollingTo.index), ...)): totalSize 2,322 -> 240 on a 43-row
+// transcript AND 266,020 -> 240 on a 419-row one - the SAME constant, i.e. one row rather than a
+// sum - then alignItemsAtEndPadding grown to 297 (12 + 142 + 240 + 297 = 691, the viewport, to the
+// pixel) and UIScrollView clamping contentOffset to that one-viewport content.
+// The sentinel is given a private value no item key can ever take, so the invalid state stops being
+// representable rather than being guarded against at one caller.
+var HAPPIER_TOTAL_SIZE_ABSOLUTE = { happierTotalSizeAbsolute: true };
 function addTotalSize(ctx, key, add, notifyTotalSize = true) {
   const state = ctx.state;
   const prevTotalSize = state.totalSize;
   let totalSize = state.totalSize;
-  if (key === null) {
+  if (key === HAPPIER_TOTAL_SIZE_ABSOLUTE) {
     totalSize = add;
     if (state.timeoutSetPaddingTop) {
       clearTimeout(state.timeoutSetPaddingTop);
@@ -1057,6 +1109,17 @@ function addTotalSize(ctx, key, add, notifyTotalSize = true) {
 
 // src/core/setSize.ts
 function setSize(ctx, itemKey, size, notifyTotalSize = true) {
+  // HAPPIER-NULLKEY-NO-ROW-NO-SIZE (choke-point half): `getId` returns `null` for an index past the
+  // end of `data`, and ~15 call sites hand a `getId` result straight to `getItemSize` with no null
+  // check (the MEASURED device stack goes through prepareMVCP's closure, NOT through
+  // `getItemSizeAtIndex`, so guarding that one caller would not have covered it). `setSize` is the
+  // single choke point every resolved size passes, so refusing a key that is not a row here keeps a
+  // non-existent row from contributing to the running total or occupying a `sizes` entry, for every
+  // present and future caller. Paired with HAPPIER-NULLKEY-ABSOLUTE-TOTAL: that one removes the
+  // ambiguity, this one removes the phantom contribution the disambiguation would otherwise leave.
+  if (itemKey === null) {
+    return;
+  }
   const state = ctx.state;
   const { sizes } = state;
   const previousSize = sizes.get(itemKey);
@@ -1112,11 +1175,58 @@ function findContainerId(ctx, key) {
 }
 
 // src/utils/getItemSize.ts
-function getKnownOrFixedSize(ctx, key, index, data, resolved) {
+function reverseItemSizeContribution(state, key) {
+  const contribution = state.sizeContributions.get(key);
+  if (contribution === void 0) {
+    return;
+  }
+  state.sizeContributions.delete(key);
+  const averages = state.averageSizes[contribution.itemType];
+  if (!averages) {
+    return;
+  }
+  if (averages.num <= 1) {
+    delete state.averageSizes[contribution.itemType];
+    return;
+  }
+  const remainingNum = averages.num - 1;
+  averages.avg = (averages.avg * averages.num - contribution.size) / remainingNum;
+  averages.num = remainingNum;
+}
+function validateItemSizeVersion(ctx, key, index, data) {
+  const state = ctx.state;
+  const getItemSizeVersion = state.props.getItemSizeVersion;
+  if (!getItemSizeVersion) {
+    return;
+  }
+  const itemSizeVersions = state.itemSizeVersions;
+  const nextVersion = getItemSizeVersion(data, index);
+  if (itemSizeVersions.has(key) && itemSizeVersions.get(key) !== nextVersion) {
+    reverseItemSizeContribution(state, key);
+    state.sizesKnown.delete(key);
+    const previouslyContributedSize = state.sizes.get(key);
+    if (previouslyContributedSize !== void 0) {
+      addTotalSize(ctx, key, -previouslyContributedSize, false);
+    }
+    state.sizes.delete(key);
+    const mountedContainerId = state.containerItemKeys.get(key);
+    if (mountedContainerId !== void 0) {
+      scheduleContainerLayout(ctx, mountedContainerId);
+    }
+  }
+  itemSizeVersions.set(key, nextVersion);
+}
+function getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion = false) {
+  if (!didValidateItemSizeVersion) {
+    validateItemSizeVersion(ctx, key, index, data);
+  }
+  return ctx.state.sizesKnown.get(key);
+}
+function getKnownOrFixedSize(ctx, key, index, data, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const { getFixedItemSize, getItemType } = state.props;
-  let size = key ? state.sizesKnown.get(key) : void 0;
+  let size = key ? getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion) : void 0;
   if (size === void 0 && key && getFixedItemSize) {
     const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
     const fixedSize = (resolved == null ? void 0 : resolved.didResolveFixedItemSize) ? resolved.fixedItemSize : getFixedItemSize(data, index, itemType);
@@ -1139,7 +1249,7 @@ function areKnownOrFixedItemSizesAvailable(ctx, startIndex, endIndex) {
   }
   return true;
 }
-function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved) {
+function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b, _c, _d;
   const state = ctx.state;
   const {
@@ -1148,7 +1258,7 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     props: { estimatedItemSize, getItemType },
     scrollingTo
   } = state;
-  const sizeKnown = state.sizesKnown.get(key);
+  const sizeKnown = getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion);
   if (sizeKnown !== void 0) {
     return sizeKnown;
   }
@@ -1159,12 +1269,23 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
       return renderedSize;
     }
   }
-  size = getKnownOrFixedSize(ctx, key, index, data, resolved);
+  size = getKnownOrFixedSize(ctx, key, index, data, resolved, true);
   if (size !== void 0) {
     setSize(ctx, key, size, notifyTotalSize);
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  if (size === void 0) {
+    const getEstimatedItemSizeProp = state.props.getEstimatedItemSize;
+    if (getEstimatedItemSizeProp) {
+      const estimatedForItem = getEstimatedItemSizeProp(data, index, itemType);
+      if (estimatedForItem !== void 0) {
+        size = roundSize(estimatedForItem);
+        setSize(ctx, key, size, notifyTotalSize);
+        return size;
+      }
+    }
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1244,18 +1365,34 @@ function clampScrollOffset(ctx, offset, scrollTarget) {
 }
 
 // src/core/finishScrollTo.ts
-function finishScrollTo(ctx) {
+function cancelPendingNonAnimatedScrollCompletion(state) {
+  if (!state) return;
+  const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+  state.pendingNonAnimatedScrollCompletion = void 0;
+  pendingCompletion == null ? void 0 : pendingCompletion.cancel();
+}
+function finishScrollTo(ctx, requestedCompletion) {
   var _a3, _b;
   const state = ctx.state;
+  const pendingCompletion = requestedCompletion != null ? requestedCompletion : state == null ? void 0 : state.pendingNonAnimatedScrollCompletion;
+  if (pendingCompletion && pendingCompletion.scrollingTo !== (state == null ? void 0 : state.scrollingTo)) {
+    return;
+  }
+  if (pendingCompletion && state.pendingNonAnimatedScrollCompletion === pendingCompletion) {
+    state.pendingNonAnimatedScrollCompletion = void 0;
+    pendingCompletion.cancel();
+  }
   if (state == null ? void 0 : state.scrollingTo) {
-    const resolvePendingScroll = state.pendingScrollResolve;
-    state.pendingScrollResolve = void 0;
+    const resolvePendingScroll = pendingCompletion ? pendingCompletion.resolve : state.pendingScrollResolve;
+    if (!pendingCompletion || state.pendingScrollResolve === pendingCompletion.resolve) {
+      state.pendingScrollResolve = void 0;
+    }
     const scrollingTo = state.scrollingTo;
     state.scrollHistory.length = 0;
     state.scrollingTo = void 0;
     state.scrollTargetPinnedRange = void 0;
     if (state.pendingTotalSize !== void 0) {
-      addTotalSize(ctx, null, state.pendingTotalSize);
+      addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, state.pendingTotalSize);
     }
     {
       state.scrollAdjustHandler.commitPendingAdjust(scrollingTo);
@@ -1299,6 +1436,7 @@ function doScrollTo(ctx, params) {
   const contentSize = isHorizontal ? getContentSize(ctx) : void 0;
   const left = isHorizontal ? toNativeHorizontalOffset(state, offset, contentSize) : 0;
   const top = isHorizontal ? 0 : offset;
+  cancelPendingNonAnimatedScrollCompletion(state);
   scroller.scrollTo({ animated: isAnimated, x: left, y: top });
   if (isAnimated) {
     const target = (_b = (_a3 = scroller.getScrollEventTarget) == null ? void 0 : _a3.call(scroller)) != null ? _b : null;
@@ -1309,9 +1447,16 @@ function doScrollTo(ctx, params) {
     });
   } else {
     state.scroll = offset;
-    setTimeout(() => {
-      finishScrollTo(ctx);
-    }, 100);
+    const scrollingTo = state.scrollingTo;
+    const resolve = state.pendingScrollResolve;
+    let completionTimeout;
+    const completion = {
+      cancel: () => clearTimeout(completionTimeout),
+      resolve,
+      scrollingTo
+    };
+    completionTimeout = setTimeout(() => finishScrollTo(ctx, completion), 100);
+    state.pendingNonAnimatedScrollCompletion = completion;
   }
 }
 function listenForScrollEnd(ctx, params) {
@@ -1367,6 +1512,21 @@ function listenForScrollEnd(ctx, params) {
 }
 
 // src/core/doMaintainScrollAtEnd.ts
+function hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd) {
+  return !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+}
+function retireFinishedInitialScrollForMaintainedEnd(ctx) {
+  const state = ctx.state;
+  if (!state.initialScroll) {
+    return true;
+  }
+  if (!state.didFinishInitialScroll || state.scrollingTo && state.scrollingTo.isInitialScroll) {
+    return false;
+  }
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTarget(state);
+  return true;
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
   const {
@@ -1376,7 +1536,14 @@ function doMaintainScrollAtEnd(ctx) {
     props: { maintainScrollAtEnd }
   } = state;
   const isWithinMaintainScrollAtEndThreshold = peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
-  const shouldMaintainScrollAtEnd = !!(isWithinMaintainScrollAtEndThreshold && maintainScrollAtEnd && didContainersLayout);
+  const hasExternalMaintainScrollAtEndIntent2 = hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+  const shouldMaintainScrollAtEnd = !!((isWithinMaintainScrollAtEndThreshold || hasExternalMaintainScrollAtEndIntent2) && maintainScrollAtEnd && didContainersLayout);
+  if (shouldMaintainScrollAtEnd && state.initialScroll) {
+    state.pendingMaintainScrollAtEnd = true;
+    if (!retireFinishedInitialScrollForMaintainedEnd(ctx)) {
+      return false;
+    }
+  }
   if (pendingNativeMVCPAdjust) {
     state.pendingMaintainScrollAtEnd = shouldMaintainScrollAtEnd;
     return false;
@@ -1392,7 +1559,10 @@ function doMaintainScrollAtEnd(ctx) {
       const activeState = maintainScrollAtEnd.animated ? "animated" : "instant";
       state.maintainingScrollAtEnd = pendingState;
       requestAnimationFrame(() => {
-        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold")) {
+        if (state.maintainingScrollAtEnd !== pendingState) {
+          return;
+        }
+        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold") || typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd()) {
           state.maintainingScrollAtEnd = activeState;
           const scroller = refScroller.current;
           if (state.props.horizontal && isHorizontalRTL(state)) {
@@ -1737,7 +1907,11 @@ function prepareMVCP(ctx, dataChanged) {
         return;
       }
       if (Math.abs(positionDiff) > MVCP_POSITION_EPSILON) {
-        const shouldSkipAdjustForMaintainedEnd = (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "animated") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const maintainScrollAtEnd = props.maintainScrollAtEnd;
+        const maintainingScrollAtEnd = state.maintainingScrollAtEnd;
+        const hasExternalMaintainedEndOwnership = state.didContainersLayout && hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+        const hasActiveMaintainedEndOwnership = !!maintainScrollAtEnd && (maintainingScrollAtEnd === "pending-animated" || maintainingScrollAtEnd === "animated" || maintainingScrollAtEnd === "pending-instant" || maintainingScrollAtEnd === "instant") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const shouldSkipAdjustForMaintainedEnd = scrollTarget === void 0 && (hasExternalMaintainedEndOwnership || hasActiveMaintainedEndOwnership);
         if (!shouldSkipAdjustForMaintainedEnd) {
           requestAdjust(ctx, positionDiff);
         }
@@ -1809,7 +1983,6 @@ function isInMVCPActiveMode(state) {
 
 // src/core/updateScroll.ts
 function updateScroll(ctx, newScroll, forceUpdate, options) {
-  var _a3;
   const state = ctx.state;
   const { ignoreScrollFromMVCP, lastScrollAdjustForHistory, scrollAdjustHandler, scrollHistory, scrollingTo } = state;
   const prevScroll = state.scroll;
@@ -1883,7 +2056,7 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
     } else {
       runCalculateItems();
     }
-    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd || !!((_a3 = state.props.maintainScrollAtEnd) == null ? void 0 : _a3.onDataChange);
+    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd;
     if (didResolvePendingNativeMVCPAdjust && shouldMaintainScrollAtEndAfterPendingSettle) {
       state.pendingMaintainScrollAtEnd = false;
       doMaintainScrollAtEnd(ctx);
@@ -2234,7 +2407,8 @@ function advanceCurrentInitialScrollSession(ctx, options) {
 }
 
 // src/utils/checkAllSizesKnown.ts
-function checkAllSizesKnown(state, start, end) {
+function checkAllSizesKnown(ctx, start, end) {
+  const state = ctx.state;
   if (start == null || end == null || start < 0 || end < start) {
     return false;
   }
@@ -2244,7 +2418,7 @@ function checkAllSizesKnown(state, start, end) {
     if (index !== void 0 && index >= start && index <= end) {
       hasMountedIndex = true;
       const id = getId(state, index);
-      if (id === void 0 || !state.sizesKnown.has(id)) {
+      if (id === void 0 || getKnownItemSize(ctx, id, index, state.props.data[index]) === void 0) {
         return false;
       }
     }
@@ -2624,11 +2798,13 @@ function handleBootstrapInitialScrollDataChange(ctx, options) {
       setInitialScrollTarget(ctx, updatedInitialScroll, {
         resetDidFinish: shouldResetDidFinish
       });
-      rearmBootstrapInitialScroll(ctx, {
-        scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
-        seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
-        targetIndexSeed: updatedInitialScroll.index
-      });
+      if (!!bootstrapInitialScroll || shouldResetDidFinish || !state.didFinishInitialScroll) {
+        rearmBootstrapInitialScroll(ctx, {
+          scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
+          seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
+          targetIndexSeed: updatedInitialScroll.index
+        });
+      }
       return;
     }
   }
@@ -2749,7 +2925,7 @@ function evaluateBootstrapInitialScroll(ctx) {
     bootstrapInitialScroll.targetIndexSeed = void 0;
   }
   const resolvedOffset = resolveInitialScrollOffset(ctx, initialScroll);
-  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
   const didResolvedOffsetChange = Math.abs(bootstrapInitialScroll.scroll - resolvedOffset) > 1;
   const { data } = state.props;
   const visibleIndices = getBootstrapRevealVisibleIndices({
@@ -2767,7 +2943,7 @@ function evaluateBootstrapInitialScroll(ctx) {
   const areVisibleIndicesMeasured = visibleIndices.length > 0 && visibleIndices.every((index) => {
     var _a4;
     const id = (_a4 = state.idCache[index]) != null ? _a4 : getId(state, index);
-    return state.sizesKnown.has(id);
+    return getKnownItemSize(ctx, id, index, data[index]) !== void 0;
   });
   const previousResolvedOffset = bootstrapInitialScroll.previousResolvedOffset;
   const previousVisibleIndices = bootstrapInitialScroll.visibleIndices;
@@ -3346,9 +3522,27 @@ function updateTotalSize(ctx) {
     positions,
     props: { data }
   } = state;
+  // HAPPIER-TOTALSIZE-STALE-POSITIONS: `state.props` is mutated during RENDER, so `props.data`
+  // can already be the next render's array while `positions` still describes the previous one.
+  // Any event-driven pass (a delivered scroll, a layout) that runs before the reconciling
+  // `dataChanged` pass sees that pair. When the new data is SHORTER, updateItemPositions' walk
+  // range (`startIndex = minIndexSizeChanged ?? startBuffered`) is already past its end, so no row
+  // is walked and the absolute total below is computed from stale positions: with a one-row
+  // `data`, `positions[0]` is 0 and the row size is unknown, so the list publishes exactly
+  // `estimatedItemSize` for a transcript of any height. Measured on device: 2,322 -> 240 on a
+  // 43-row transcript and 266,020 -> 240 on a 419-row one (the SAME 240, i.e. one row, not a sum),
+  // alignItemsAtEndPadding then grown to fill the viewport and UIScrollView clamping
+  // contentOffset to that one-viewport content.
+  // The mirror case is already refused: when data GREW, `positions[lastIndex]` is undefined and
+  // the `lastPosition !== void 0` guard below suppresses the publish. This makes the shrink
+  // direction symmetric. Nothing is lost by waiting - the reconciling pass walks from index 0,
+  // leaves `positions.length === data.length`, and republishes the exact total.
+  if (positions.length > data.length) {
+    return;
+  }
   const numColumns = (_a3 = peek$(ctx, "numColumns")) != null ? _a3 : 1;
   if (data.length === 0) {
-    addTotalSize(ctx, null, 0);
+    addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, 0);
   } else {
     const lastIndex = data.length - 1;
     const lastId = getId(state, lastIndex);
@@ -3371,12 +3565,12 @@ function updateTotalSize(ctx) {
             maxSize = size;
           }
         }
-        addTotalSize(ctx, null, lastPosition + maxSize);
+        addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, lastPosition + maxSize);
       } else {
         const lastSize = getItemSize(ctx, lastId, lastIndex, data[lastIndex]);
         if (lastSize !== void 0) {
           const totalSize = lastPosition + lastSize;
-          addTotalSize(ctx, null, totalSize);
+          addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, totalSize);
         }
       }
     }
@@ -3439,7 +3633,7 @@ function updateItemPositions(ctx, dataChanged, {
   const shouldOptimize = !forceFullUpdate && !dataChanged && (optimizeForVisibleWindow || Math.abs(velocity) > 0 || state.scrollLength > 0 && lastScrollDelta > state.scrollLength);
   const maxVisibleArea = scrollBottomBuffered + 1e3;
   const useAverageSize = true;
-  const preferCachedSize = !doMVCP || dataChanged || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
+  const preferCachedSize = !doMVCP || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
   const notifyTotalSizeWhileCachingSizes = false;
   let currentRowTop = 0;
   let column = 1;
@@ -3468,7 +3662,7 @@ function updateItemPositions(ctx, dataChanged, {
       const prevIndex = startIndex - 1;
       const prevId = getId(state, prevIndex);
       const prevPosition = (_c = positions[prevIndex]) != null ? _c : 0;
-      const prevSize = (_d = sizesKnown.get(prevId)) != null ? _d : getItemSize(
+      const prevSize = getItemSize(
         ctx,
         prevId,
         prevIndex,
@@ -3486,7 +3680,25 @@ function updateItemPositions(ctx, dataChanged, {
   let breakAt;
   for (let i = startIndex; i < dataLength; i++) {
     if (shouldOptimize && breakAt !== void 0 && i > breakAt) {
-      didBreakEarly = true;
+      const staleRowTop = positions[i];
+      if (staleRowTop === void 0) {
+        didBreakEarly = true;
+        break;
+      }
+      const positionShift = currentRowTop - staleRowTop;
+      if (positionShift !== 0) {
+        for (let j = i; j < dataLength; j++) {
+          const stalePosition = positions[j];
+          if (stalePosition === void 0) {
+            break;
+          }
+          const shiftedPosition = stalePosition + positionShift;
+          positions[j] = shiftedPosition;
+          if (hasPositionListeners) {
+            notifyPosition$(ctx, idCache[j] !== void 0 ? idCache[j] : getId(state, j), shiftedPosition);
+          }
+        }
+      }
       break;
     }
     if (shouldOptimize && breakAt === void 0 && !scrollingTo && !dataChanged && currentRowTop > maxVisibleArea) {
@@ -3508,8 +3720,7 @@ function updateItemPositions(ctx, dataChanged, {
       column = 1;
       maxSizeInRow = 0;
     }
-    const knownSize = sizesKnown.get(id);
-    const size = knownSize !== void 0 ? knownSize : getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
+    const size = getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
     if (IS_DEV && needsIndexByKey) {
       if (indexByKeyForChecking.has(id)) {
         console.error(
@@ -4401,6 +4612,17 @@ function calculateItemsInView(ctx, params = {}) {
         }
       }
     }
+    if (startBuffered === null && endBuffered === null && dataLength > 0) {
+      const lastIndexInData = dataLength - 1;
+      const lastIdInData = idCache[lastIndexInData] != null ? idCache[lastIndexInData] : getId(state, lastIndexInData);
+      const lastTopInData = positions[lastIndexInData];
+      const lastSizeInData = sizes.get(lastIdInData) != null ? sizes.get(lastIdInData) : getItemSize(ctx, lastIdInData, lastIndexInData, data[lastIndexInData]);
+      if (lastTopInData !== void 0 && lastSizeInData !== void 0 && lastTopInData + lastSizeInData <= scroll) {
+        startBuffered = lastIndexInData;
+        startBufferedId = lastIdInData;
+        endBuffered = lastIndexInData;
+      }
+    }
     Object.assign(state, {
       endBuffered,
       endNoBuffer: visibleRange.endNoBuffer,
@@ -4604,7 +4826,7 @@ function calculateItemsInView(ctx, params = {}) {
     }
     maybeEmitFirstVisibleItemChanged(state, visibleRange.startNoBuffer);
     if (!queuedInitialLayout && !state.didContainersLayout) {
-      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(state, state.startBuffered, state.endBuffered) : checkAllSizesKnown(state, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered) : checkAllSizesKnown(ctx, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
       if (isInitialLayoutReady) {
         setDidLayout(ctx);
         handleInitialScrollLayoutReady(ctx);
@@ -4790,11 +5012,16 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   } = state;
   if (!data) return { didMeasureUserScrollAnchorResetItem };
   const index = state.indexByKey.get(itemKey);
+  const itemData = index === void 0 ? void 0 : state.props.data[index];
+  if (index === void 0 && state.props.getItemSizeVersion) {
+    return { didMeasureUserScrollAnchorResetItem };
+  }
+  const didValidateItemSizeVersion = index !== void 0;
+  const prevSizeKnown = didValidateItemSizeVersion ? getKnownItemSize(ctx, itemKey, index, itemData) : state.sizesKnown.get(itemKey);
   if (getFixedItemSize) {
     if (index === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
-    const itemData = state.props.data[index];
     if (itemData === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4807,7 +5034,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       };
     }
     const size2 = resolvedMeasurementItem.fixedItemSize;
-    if (size2 !== void 0 && size2 === sizesKnown.get(itemKey)) {
+    if (size2 !== void 0 && size2 === prevSizeKnown) {
       updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4815,8 +5042,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   let needsRecalculate = !didContainersLayout;
   let shouldMaintainScrollAtEnd = false;
   let minIndexSizeChanged;
-  const prevSizeKnown = state.sizesKnown.get(itemKey);
-  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem);
+  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion);
   const size = roundSize(horizontal ? sizeObj.width : sizeObj.height);
   if (diff !== 0) {
     minIndexSizeChanged = minIndexSizeChanged !== void 0 ? Math.min(minIndexSizeChanged, index) : index;
@@ -4825,7 +5051,8 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     if (!needsRecalculate && state.containerItemKeys.has(itemKey)) {
       needsRecalculate = true;
     }
-    if (prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
+    const hasExternalMaintainScrollAtEndIntent = !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+    if (hasExternalMaintainScrollAtEndIntent || prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
       shouldMaintainScrollAtEnd = true;
     }
     onItemSizeChanged == null ? void 0 : onItemSizeChanged({
@@ -4841,7 +5068,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     state.minIndexSizeChanged = state.minIndexSizeChanged !== void 0 ? Math.min(state.minIndexSizeChanged, minIndexSizeChanged) : minIndexSizeChanged;
   }
   updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
-  if (didContainersLayout || checkAllSizesKnown(state, state.startBuffered, state.endBuffered)) {
+  if (didContainersLayout || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered)) {
     const canMaintainScrollAtEnd = shouldMaintainScrollAtEnd && !!(maintainScrollAtEnd == null ? void 0 : maintainScrollAtEnd.onItemLayout);
     return {
       didChange: diff !== 0,
@@ -4855,7 +5082,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     didMeasureUserScrollAnchorResetItem
   };
 }
-function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
+function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const {
@@ -4878,7 +5105,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     fixedItemSize,
     itemType
   } : void 0;
-  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize);
+  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize, didValidateItemSizeVersion);
   const rawSize = horizontal ? sizeObj.width : sizeObj.height;
   const prevSizeKnown = sizesKnown.get(itemKey);
   const size = Math.round(rawSize) ;
@@ -4898,6 +5125,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       averages.avg = (averages.avg * averages.num + size) / (averages.num + 1);
       averages.num++;
     }
+    state.sizeContributions.set(itemKey, { itemType, size });
   }
   if (!prevSize || Math.abs(prevSize - size) > 0.1) {
     setSize(ctx, itemKey, size);
@@ -5359,7 +5587,8 @@ function processContainerLayout({ containerId, ctx, rectangle, ref, state }) {
   const axis = state.horizontal ? "width" : "height";
   const size = roundSize(rectangle[axis]);
   state.lastSize ? roundSize(state.lastSize[axis]) : void 0;
-  const coreKnownSize = listState.sizesKnown.get(currentItemKey);
+  const currentItemIndex = listState.indexByKey.get(currentItemKey);
+  const coreKnownSize = currentItemIndex === void 0 ? void 0 : getKnownItemSize(ctx, currentItemKey, currentItemIndex, listState.props.data[currentItemIndex]);
   const previousSize = coreKnownSize ;
   const applyLayout = () => {
     state.lastSize = layout;
@@ -6691,6 +6920,8 @@ function checkResetContainers(ctx, dataProp, { didColumnsChange = false } = {})
   if (didColumnsChange) {
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7110,6 +7341,13 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   const IMPERATIVE_SCROLL_SETTLE_MAX_WAIT_MS = 800;
   const IMPERATIVE_SCROLL_SETTLE_STABLE_FRAMES = 2;
   let imperativeScrollToken = 0;
+  let pendingReadinessFrame;
+  const cancelPendingReadinessFrame = () => {
+    if (pendingReadinessFrame !== void 0) {
+      cancelAnimationFrame(pendingReadinessFrame);
+      pendingReadinessFrame = void 0;
+    }
+  };
   const isSettlingAfterDataChange = () => !!state.didDataChange || !!state.didColumnsChange || state.queuedMVCPRecalculate !== void 0 || state.ignoreScrollFromMVCP !== void 0;
   const isScrollToIndexReady = (targetIndex, allowEmpty = false) => {
     var _a3;
@@ -7131,6 +7369,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const startedAt = Date.now();
     let stableFrames = 0;
     const check = () => {
+      pendingReadinessFrame = void 0;
       if (token !== imperativeScrollToken) {
         return;
       }
@@ -7144,9 +7383,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
         run();
         return;
       }
-      requestAnimationFrame(check);
+      pendingReadinessFrame = requestAnimationFrame(check);
     };
-    requestAnimationFrame(check);
+    cancelPendingReadinessFrame();
+    pendingReadinessFrame = requestAnimationFrame(check);
   };
   const runScrollRequest = (token, resolve, run, isReady = () => true) => {
     const runNow = () => {
@@ -7169,12 +7409,22 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   };
   const startImperativeScroll = (resolve) => {
     var _a3;
+    cancelPendingReadinessFrame();
     const token = ++imperativeScrollToken;
     state.pendingScrollToEnd = void 0;
     (_a3 = state.pendingScrollResolve) == null ? void 0 : _a3.call(state);
     state.pendingScrollResolve = resolve;
     return token;
   };
+  state.disposeImperativeHandle = () => {
+    imperativeScrollToken++;
+    cancelPendingReadinessFrame();
+    cancelPendingNonAnimatedScrollCompletion(state);
+    state.pendingScrollToEnd = void 0;
+    const resolvePendingScroll = state.pendingScrollResolve;
+    state.pendingScrollResolve = void 0;
+    resolvePendingScroll == null ? void 0 : resolvePendingScroll();
+  };
   const runScrollWithPromise = (run, isReady = () => true) => new Promise((resolve) => {
     const token = startImperativeScroll(resolve);
     runScrollRequest(token, resolve, run, isReady);
@@ -7215,6 +7465,8 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const mode = (_a3 = options == null ? void 0 : options.mode) != null ? _a3 : "sizes";
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7235,6 +7487,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     (_b = state.triggerCalculateItemsInView) == null ? void 0 : _b.call(state, { forceFullItemPositions: true });
   };
   return {
+    cancelInitialScrollPreservation: () => cancelInitialScrollPreservation(ctx),
     clearCaches,
     flashScrollIndicators: () => refScroller.current.flashScrollIndicators(),
     getAnimatableRef: () => {
@@ -7272,7 +7525,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
       scroll: state.scroll,
       scrollLength: state.scrollLength,
       scrollVelocity: getScrollVelocity(state),
-      sizeAtIndex: (index) => state.sizesKnown.get(getId(state, index)),
+      sizeAtIndex: (index) => {
+        const key = getId(state, index);
+        return getKnownItemSize(ctx, key, index, state.props.data[index]);
+      },
       sizes: state.sizesKnown,
       start: state.startNoBuffer,
       startBuffered: state.startBuffered
@@ -7453,7 +7709,8 @@ function normalizeMaintainScrollAtEnd(value) {
   const normalizedTriggers = normalizeMaintainScrollAtEndOn(value.on, "on" in value);
   return {
     ...normalizedTriggers,
-    animated: (_a3 = value.animated) != null ? _a3 : false
+    animated: (_a3 = value.animated) != null ? _a3 : false,
+    isMaintainingScrollAtEnd: value.isMaintainingScrollAtEnd
   };
 }
 
@@ -7583,6 +7840,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
+    getItemSizeVersion,
     getItemType,
     horizontal,
     rtl,
@@ -7775,6 +8034,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         scrollTime: 0,
         sizes: /* @__PURE__ */ new Map(),
         sizesKnown: /* @__PURE__ */ new Map(),
+        itemSizeVersions: /* @__PURE__ */ new Map(),
+        sizeContributions: /* @__PURE__ */ new Map(),
         startBuffered: -1,
         startNoBuffer: -1,
         startReachedSnapshot: void 0,
@@ -7840,6 +8101,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
+    getItemSizeVersion: useWrapIfItem(getItemSizeVersion),
     getItemType: useWrapIfItem(getItemType),
     horizontal: !!horizontal,
     itemsAreEqual,
@@ -7881,7 +8144,10 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
   if (shouldResetFreshDataLayout) {
     resetInitialRenderState(ctx, {
       resetInitialScroll: !!initialScrollProp,
-      resetLayout: true
+      resetLayout: true,
+      // HAPPIER-MONOTONIC-DATASET-VISIBILITY: a transient empty projection of the same
+      // dataset still needs fresh layout, but must not turn an already painted list opaque.
+      preserveReadyToRender: !didDataKeyChangeLocal && state.didLoad
     });
   }
   const memoizedLastItemKeys = React3.useMemo(() => {
@@ -8109,10 +8375,20 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         cancelAnimationFrame(state.queuedFullDrawDistancePrewarm);
         state.queuedFullDrawDistancePrewarm = void 0;
       }
+      if (state.animFrameCheckFinishedScroll !== void 0) {
+        cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+        state.animFrameCheckFinishedScroll = void 0;
+      }
+      if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+        clearTimeout(state.timeoutCheckFinishedScrollFallback);
+        state.timeoutCheckFinishedScrollFallback = void 0;
+      }
       for (const timeout of state.timeouts) {
         clearTimeout(timeout);
       }
       state.timeouts.clear();
+      state.disposeImperativeHandle == null ? void 0 : state.disposeImperativeHandle();
+      state.disposeImperativeHandle = void 0;
     };
   }, [state]);
   React3.useLayoutEffect(() => {
diff --git a/node_modules/@legendapp/list/react.mjs b/node_modules/@legendapp/list/react.mjs
index 6b2d901..0666b1a 100644
--- a/node_modules/@legendapp/list/react.mjs
+++ b/node_modules/@legendapp/list/react.mjs
@@ -852,7 +852,8 @@ function scheduleFullDrawDistancePrewarm(ctx) {
 // src/utils/setInitialRenderState.ts
 function resetInitialRenderState(ctx, {
   resetLayout,
-  resetInitialScroll
+  resetInitialScroll,
+  preserveReadyToRender
 }) {
   const { state } = ctx;
   if (resetLayout) {
@@ -862,7 +863,9 @@ function resetInitialRenderState(ctx, {
   if (resetInitialScroll) {
     state.didFinishInitialScroll = false;
   }
-  set$(ctx, "readyToRender", false);
+  if (!preserveReadyToRender) {
+    set$(ctx, "readyToRender", false);
+  }
   resetAdaptiveRender(ctx);
 }
 function setInitialRenderState(ctx, {
@@ -915,6 +918,38 @@ function clearPreservedInitialScrollTarget(state) {
   state.initialScroll = void 0;
   setInitialScrollSession(state);
 }
+function cancelInitialScrollPreservation(ctx) {
+  const state = ctx.state;
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTargetTimeout(state);
+  clearBootstrapInitialScrollSession(state);
+  const activeInitialScroll = state.scrollingTo;
+  if (activeInitialScroll == null ? void 0 : activeInitialScroll.isInitialScroll) {
+    const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+    if ((pendingCompletion == null ? void 0 : pendingCompletion.scrollingTo) === activeInitialScroll) {
+      cancelPendingNonAnimatedScrollCompletion(state);
+    }
+    if (state.animFrameCheckFinishedScroll !== void 0) {
+      cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+      state.animFrameCheckFinishedScroll = void 0;
+    }
+    if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+      clearTimeout(state.timeoutCheckFinishedScrollFallback);
+      state.timeoutCheckFinishedScrollFallback = void 0;
+    }
+    if (state.scrollingTo === activeInitialScroll) {
+      state.scrollingTo = void 0;
+      state.scrollTargetPinnedRange = void 0;
+    }
+  }
+  clearPreservedInitialScrollTarget(state);
+  clearInitialScrollSession(state);
+  setInitialRenderState(ctx, { didInitialScroll: true });
+  state.pendingMaintainScrollAtEnd = false;
+  if (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "pending-instant") {
+    state.maintainingScrollAtEnd = void 0;
+  }
+}
 function finishInitialScroll(ctx, options) {
   var _a3, _b, _c;
   const state = ctx.state;
@@ -952,6 +987,9 @@ function finishInitialScroll(ctx, options) {
       recalculateSettledScroll(ctx);
     }
     setInitialRenderState(ctx, { didInitialScroll: true });
+    if (state.pendingMaintainScrollAtEnd) {
+      doMaintainScrollAtEnd(ctx);
+    }
     if (shouldReleaseDeferredPublicOnScroll) {
       releaseDeferredPublicOnScroll(ctx, finalScrollOffset);
     }
@@ -1007,11 +1045,25 @@ function updateContentMetricsState(ctx) {
 }
 
 // src/core/addTotalSize.ts
+// HAPPIER-NULLKEY-ABSOLUTE-TOTAL (owner half): `addTotalSize` overloaded `key === null` as its
+// ABSOLUTE-WRITE sentinel ("this number IS the total"), and `getId` returns `null` for any index
+// past the end of `data` ("there is no such row"). Two meanings, one value. One probe of a row the
+// current `data` no longer contains therefore assigned the whole content size to be one unmeasured
+// row: getId -> null -> getItemSize falls through to `estimatedItemSize` -> setSize with a null key
+// -> addTotalSize with a null key -> totalSize = est, written absolutely. MEASURED on device with a
+// 4/4 identical stack (checkResetContainers -> calculateItemsInView -> prepareMVCP's closure ->
+// getItemSize(ctx, getId(state, scrollingTo.index), ...)): totalSize 2,322 -> 240 on a 43-row
+// transcript AND 266,020 -> 240 on a 419-row one - the SAME constant, i.e. one row rather than a
+// sum - then alignItemsAtEndPadding grown to 297 (12 + 142 + 240 + 297 = 691, the viewport, to the
+// pixel) and UIScrollView clamping contentOffset to that one-viewport content.
+// The sentinel is given a private value no item key can ever take, so the invalid state stops being
+// representable rather than being guarded against at one caller.
+var HAPPIER_TOTAL_SIZE_ABSOLUTE = { happierTotalSizeAbsolute: true };
 function addTotalSize(ctx, key, add, notifyTotalSize = true) {
   const state = ctx.state;
   const prevTotalSize = state.totalSize;
   let totalSize = state.totalSize;
-  if (key === null) {
+  if (key === HAPPIER_TOTAL_SIZE_ABSOLUTE) {
     totalSize = add;
     if (state.timeoutSetPaddingTop) {
       clearTimeout(state.timeoutSetPaddingTop);
@@ -1036,6 +1088,17 @@ function addTotalSize(ctx, key, add, notifyTotalSize = true) {
 
 // src/core/setSize.ts
 function setSize(ctx, itemKey, size, notifyTotalSize = true) {
+  // HAPPIER-NULLKEY-NO-ROW-NO-SIZE (choke-point half): `getId` returns `null` for an index past the
+  // end of `data`, and ~15 call sites hand a `getId` result straight to `getItemSize` with no null
+  // check (the MEASURED device stack goes through prepareMVCP's closure, NOT through
+  // `getItemSizeAtIndex`, so guarding that one caller would not have covered it). `setSize` is the
+  // single choke point every resolved size passes, so refusing a key that is not a row here keeps a
+  // non-existent row from contributing to the running total or occupying a `sizes` entry, for every
+  // present and future caller. Paired with HAPPIER-NULLKEY-ABSOLUTE-TOTAL: that one removes the
+  // ambiguity, this one removes the phantom contribution the disambiguation would otherwise leave.
+  if (itemKey === null) {
+    return;
+  }
   const state = ctx.state;
   const { sizes } = state;
   const previousSize = sizes.get(itemKey);
@@ -1091,11 +1154,58 @@ function findContainerId(ctx, key) {
 }
 
 // src/utils/getItemSize.ts
-function getKnownOrFixedSize(ctx, key, index, data, resolved) {
+function reverseItemSizeContribution(state, key) {
+  const contribution = state.sizeContributions.get(key);
+  if (contribution === void 0) {
+    return;
+  }
+  state.sizeContributions.delete(key);
+  const averages = state.averageSizes[contribution.itemType];
+  if (!averages) {
+    return;
+  }
+  if (averages.num <= 1) {
+    delete state.averageSizes[contribution.itemType];
+    return;
+  }
+  const remainingNum = averages.num - 1;
+  averages.avg = (averages.avg * averages.num - contribution.size) / remainingNum;
+  averages.num = remainingNum;
+}
+function validateItemSizeVersion(ctx, key, index, data) {
+  const state = ctx.state;
+  const getItemSizeVersion = state.props.getItemSizeVersion;
+  if (!getItemSizeVersion) {
+    return;
+  }
+  const itemSizeVersions = state.itemSizeVersions;
+  const nextVersion = getItemSizeVersion(data, index);
+  if (itemSizeVersions.has(key) && itemSizeVersions.get(key) !== nextVersion) {
+    reverseItemSizeContribution(state, key);
+    state.sizesKnown.delete(key);
+    const previouslyContributedSize = state.sizes.get(key);
+    if (previouslyContributedSize !== void 0) {
+      addTotalSize(ctx, key, -previouslyContributedSize, false);
+    }
+    state.sizes.delete(key);
+    const mountedContainerId = state.containerItemKeys.get(key);
+    if (mountedContainerId !== void 0) {
+      scheduleContainerLayout(ctx, mountedContainerId);
+    }
+  }
+  itemSizeVersions.set(key, nextVersion);
+}
+function getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion = false) {
+  if (!didValidateItemSizeVersion) {
+    validateItemSizeVersion(ctx, key, index, data);
+  }
+  return ctx.state.sizesKnown.get(key);
+}
+function getKnownOrFixedSize(ctx, key, index, data, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const { getFixedItemSize, getItemType } = state.props;
-  let size = key ? state.sizesKnown.get(key) : void 0;
+  let size = key ? getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion) : void 0;
   if (size === void 0 && key && getFixedItemSize) {
     const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
     const fixedSize = (resolved == null ? void 0 : resolved.didResolveFixedItemSize) ? resolved.fixedItemSize : getFixedItemSize(data, index, itemType);
@@ -1118,7 +1228,7 @@ function areKnownOrFixedItemSizesAvailable(ctx, startIndex, endIndex) {
   }
   return true;
 }
-function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved) {
+function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, notifyTotalSize, resolved, didValidateItemSizeVersion = false) {
   var _a3, _b, _c, _d;
   const state = ctx.state;
   const {
@@ -1127,7 +1237,7 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
     props: { estimatedItemSize, getItemType },
     scrollingTo
   } = state;
-  const sizeKnown = state.sizesKnown.get(key);
+  const sizeKnown = getKnownItemSize(ctx, key, index, data, didValidateItemSizeVersion);
   if (sizeKnown !== void 0) {
     return sizeKnown;
   }
@@ -1138,12 +1248,23 @@ function getItemSize(ctx, key, index, data, useAverageSize, preferCachedSize, no
       return renderedSize;
     }
   }
-  size = getKnownOrFixedSize(ctx, key, index, data, resolved);
+  size = getKnownOrFixedSize(ctx, key, index, data, resolved, true);
   if (size !== void 0) {
     setSize(ctx, key, size, notifyTotalSize);
     return size;
   }
   const itemType = (_b = resolved == null ? void 0 : resolved.itemType) != null ? _b : getItemType ? (_a3 = getItemType(data, index)) != null ? _a3 : "" : "";
+  if (size === void 0) {
+    const getEstimatedItemSizeProp = state.props.getEstimatedItemSize;
+    if (getEstimatedItemSizeProp) {
+      const estimatedForItem = getEstimatedItemSizeProp(data, index, itemType);
+      if (estimatedForItem !== void 0) {
+        size = roundSize(estimatedForItem);
+        setSize(ctx, key, size, notifyTotalSize);
+        return size;
+      }
+    }
+  }
   if (useAverageSize && !scrollingTo) {
     const averageSizeForType = (_c = averageSizes[itemType]) == null ? void 0 : _c.avg;
     if (averageSizeForType !== void 0) {
@@ -1223,18 +1344,34 @@ function clampScrollOffset(ctx, offset, scrollTarget) {
 }
 
 // src/core/finishScrollTo.ts
-function finishScrollTo(ctx) {
+function cancelPendingNonAnimatedScrollCompletion(state) {
+  if (!state) return;
+  const pendingCompletion = state.pendingNonAnimatedScrollCompletion;
+  state.pendingNonAnimatedScrollCompletion = void 0;
+  pendingCompletion == null ? void 0 : pendingCompletion.cancel();
+}
+function finishScrollTo(ctx, requestedCompletion) {
   var _a3, _b;
   const state = ctx.state;
+  const pendingCompletion = requestedCompletion != null ? requestedCompletion : state == null ? void 0 : state.pendingNonAnimatedScrollCompletion;
+  if (pendingCompletion && pendingCompletion.scrollingTo !== (state == null ? void 0 : state.scrollingTo)) {
+    return;
+  }
+  if (pendingCompletion && state.pendingNonAnimatedScrollCompletion === pendingCompletion) {
+    state.pendingNonAnimatedScrollCompletion = void 0;
+    pendingCompletion.cancel();
+  }
   if (state == null ? void 0 : state.scrollingTo) {
-    const resolvePendingScroll = state.pendingScrollResolve;
-    state.pendingScrollResolve = void 0;
+    const resolvePendingScroll = pendingCompletion ? pendingCompletion.resolve : state.pendingScrollResolve;
+    if (!pendingCompletion || state.pendingScrollResolve === pendingCompletion.resolve) {
+      state.pendingScrollResolve = void 0;
+    }
     const scrollingTo = state.scrollingTo;
     state.scrollHistory.length = 0;
     state.scrollingTo = void 0;
     state.scrollTargetPinnedRange = void 0;
     if (state.pendingTotalSize !== void 0) {
-      addTotalSize(ctx, null, state.pendingTotalSize);
+      addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, state.pendingTotalSize);
     }
     {
       state.scrollAdjustHandler.commitPendingAdjust(scrollingTo);
@@ -1278,6 +1415,7 @@ function doScrollTo(ctx, params) {
   const contentSize = isHorizontal ? getContentSize(ctx) : void 0;
   const left = isHorizontal ? toNativeHorizontalOffset(state, offset, contentSize) : 0;
   const top = isHorizontal ? 0 : offset;
+  cancelPendingNonAnimatedScrollCompletion(state);
   scroller.scrollTo({ animated: isAnimated, x: left, y: top });
   if (isAnimated) {
     const target = (_b = (_a3 = scroller.getScrollEventTarget) == null ? void 0 : _a3.call(scroller)) != null ? _b : null;
@@ -1288,9 +1426,16 @@ function doScrollTo(ctx, params) {
     });
   } else {
     state.scroll = offset;
-    setTimeout(() => {
-      finishScrollTo(ctx);
-    }, 100);
+    const scrollingTo = state.scrollingTo;
+    const resolve = state.pendingScrollResolve;
+    let completionTimeout;
+    const completion = {
+      cancel: () => clearTimeout(completionTimeout),
+      resolve,
+      scrollingTo
+    };
+    completionTimeout = setTimeout(() => finishScrollTo(ctx, completion), 100);
+    state.pendingNonAnimatedScrollCompletion = completion;
   }
 }
 function listenForScrollEnd(ctx, params) {
@@ -1346,6 +1491,21 @@ function listenForScrollEnd(ctx, params) {
 }
 
 // src/core/doMaintainScrollAtEnd.ts
+function hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd) {
+  return !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+}
+function retireFinishedInitialScrollForMaintainedEnd(ctx) {
+  const state = ctx.state;
+  if (!state.initialScroll) {
+    return true;
+  }
+  if (!state.didFinishInitialScroll || state.scrollingTo && state.scrollingTo.isInitialScroll) {
+    return false;
+  }
+  state.preservedEndAnchorCorrection = void 0;
+  clearPreservedInitialScrollTarget(state);
+  return true;
+}
 function doMaintainScrollAtEnd(ctx) {
   const state = ctx.state;
   const {
@@ -1355,7 +1515,14 @@ function doMaintainScrollAtEnd(ctx) {
     props: { maintainScrollAtEnd }
   } = state;
   const isWithinMaintainScrollAtEndThreshold = peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
-  const shouldMaintainScrollAtEnd = !!(isWithinMaintainScrollAtEndThreshold && maintainScrollAtEnd && didContainersLayout);
+  const hasExternalMaintainScrollAtEndIntent2 = hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+  const shouldMaintainScrollAtEnd = !!((isWithinMaintainScrollAtEndThreshold || hasExternalMaintainScrollAtEndIntent2) && maintainScrollAtEnd && didContainersLayout);
+  if (shouldMaintainScrollAtEnd && state.initialScroll) {
+    state.pendingMaintainScrollAtEnd = true;
+    if (!retireFinishedInitialScrollForMaintainedEnd(ctx)) {
+      return false;
+    }
+  }
   if (pendingNativeMVCPAdjust) {
     state.pendingMaintainScrollAtEnd = shouldMaintainScrollAtEnd;
     return false;
@@ -1371,7 +1538,10 @@ function doMaintainScrollAtEnd(ctx) {
       const activeState = maintainScrollAtEnd.animated ? "animated" : "instant";
       state.maintainingScrollAtEnd = pendingState;
       requestAnimationFrame(() => {
-        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold")) {
+        if (state.maintainingScrollAtEnd !== pendingState) {
+          return;
+        }
+        if (peek$(ctx, "isWithinMaintainScrollAtEndThreshold") || typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd()) {
           state.maintainingScrollAtEnd = activeState;
           const scroller = refScroller.current;
           if (state.props.horizontal && isHorizontalRTL(state)) {
@@ -1716,7 +1886,11 @@ function prepareMVCP(ctx, dataChanged) {
         return;
       }
       if (Math.abs(positionDiff) > MVCP_POSITION_EPSILON) {
-        const shouldSkipAdjustForMaintainedEnd = (state.maintainingScrollAtEnd === "pending-animated" || state.maintainingScrollAtEnd === "animated") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const maintainScrollAtEnd = props.maintainScrollAtEnd;
+        const maintainingScrollAtEnd = state.maintainingScrollAtEnd;
+        const hasExternalMaintainedEndOwnership = state.didContainersLayout && hasExternalMaintainScrollAtEndIntent(maintainScrollAtEnd);
+        const hasActiveMaintainedEndOwnership = !!maintainScrollAtEnd && (maintainingScrollAtEnd === "pending-animated" || maintainingScrollAtEnd === "animated" || maintainingScrollAtEnd === "pending-instant" || maintainingScrollAtEnd === "instant") && peek$(ctx, "isWithinMaintainScrollAtEndThreshold");
+        const shouldSkipAdjustForMaintainedEnd = scrollTarget === void 0 && (hasExternalMaintainedEndOwnership || hasActiveMaintainedEndOwnership);
         if (!shouldSkipAdjustForMaintainedEnd) {
           requestAdjust(ctx, positionDiff);
         }
@@ -1788,7 +1962,6 @@ function isInMVCPActiveMode(state) {
 
 // src/core/updateScroll.ts
 function updateScroll(ctx, newScroll, forceUpdate, options) {
-  var _a3;
   const state = ctx.state;
   const { ignoreScrollFromMVCP, lastScrollAdjustForHistory, scrollAdjustHandler, scrollHistory, scrollingTo } = state;
   const prevScroll = state.scroll;
@@ -1862,7 +2035,7 @@ function updateScroll(ctx, newScroll, forceUpdate, options) {
     } else {
       runCalculateItems();
     }
-    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd || !!((_a3 = state.props.maintainScrollAtEnd) == null ? void 0 : _a3.onDataChange);
+    const shouldMaintainScrollAtEndAfterPendingSettle = !!state.pendingMaintainScrollAtEnd;
     if (didResolvePendingNativeMVCPAdjust && shouldMaintainScrollAtEndAfterPendingSettle) {
       state.pendingMaintainScrollAtEnd = false;
       doMaintainScrollAtEnd(ctx);
@@ -2213,7 +2386,8 @@ function advanceCurrentInitialScrollSession(ctx, options) {
 }
 
 // src/utils/checkAllSizesKnown.ts
-function checkAllSizesKnown(state, start, end) {
+function checkAllSizesKnown(ctx, start, end) {
+  const state = ctx.state;
   if (start == null || end == null || start < 0 || end < start) {
     return false;
   }
@@ -2223,7 +2397,7 @@ function checkAllSizesKnown(state, start, end) {
     if (index !== void 0 && index >= start && index <= end) {
       hasMountedIndex = true;
       const id = getId(state, index);
-      if (id === void 0 || !state.sizesKnown.has(id)) {
+      if (id === void 0 || getKnownItemSize(ctx, id, index, state.props.data[index]) === void 0) {
         return false;
       }
     }
@@ -2603,11 +2777,13 @@ function handleBootstrapInitialScrollDataChange(ctx, options) {
       setInitialScrollTarget(ctx, updatedInitialScroll, {
         resetDidFinish: shouldResetDidFinish
       });
-      rearmBootstrapInitialScroll(ctx, {
-        scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
-        seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
-        targetIndexSeed: updatedInitialScroll.index
-      });
+      if (!!bootstrapInitialScroll || shouldResetDidFinish || !state.didFinishInitialScroll) {
+        rearmBootstrapInitialScroll(ctx, {
+          scroll: resolveInitialScrollOffset(ctx, updatedInitialScroll),
+          seedContentOffset: shouldResetDidFinish && !bootstrapInitialScroll ? getObservedBootstrapInitialScrollOffset(state) : void 0,
+          targetIndexSeed: updatedInitialScroll.index
+        });
+      }
       return;
     }
   }
@@ -2728,7 +2904,7 @@ function evaluateBootstrapInitialScroll(ctx) {
     bootstrapInitialScroll.targetIndexSeed = void 0;
   }
   const resolvedOffset = resolveInitialScrollOffset(ctx, initialScroll);
-  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+  const areMountedBufferedIndicesMeasured = checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
   const didResolvedOffsetChange = Math.abs(bootstrapInitialScroll.scroll - resolvedOffset) > 1;
   const { data } = state.props;
   const visibleIndices = getBootstrapRevealVisibleIndices({
@@ -2746,7 +2922,7 @@ function evaluateBootstrapInitialScroll(ctx) {
   const areVisibleIndicesMeasured = visibleIndices.length > 0 && visibleIndices.every((index) => {
     var _a4;
     const id = (_a4 = state.idCache[index]) != null ? _a4 : getId(state, index);
-    return state.sizesKnown.has(id);
+    return getKnownItemSize(ctx, id, index, data[index]) !== void 0;
   });
   const previousResolvedOffset = bootstrapInitialScroll.previousResolvedOffset;
   const previousVisibleIndices = bootstrapInitialScroll.visibleIndices;
@@ -3325,9 +3501,27 @@ function updateTotalSize(ctx) {
     positions,
     props: { data }
   } = state;
+  // HAPPIER-TOTALSIZE-STALE-POSITIONS: `state.props` is mutated during RENDER, so `props.data`
+  // can already be the next render's array while `positions` still describes the previous one.
+  // Any event-driven pass (a delivered scroll, a layout) that runs before the reconciling
+  // `dataChanged` pass sees that pair. When the new data is SHORTER, updateItemPositions' walk
+  // range (`startIndex = minIndexSizeChanged ?? startBuffered`) is already past its end, so no row
+  // is walked and the absolute total below is computed from stale positions: with a one-row
+  // `data`, `positions[0]` is 0 and the row size is unknown, so the list publishes exactly
+  // `estimatedItemSize` for a transcript of any height. Measured on device: 2,322 -> 240 on a
+  // 43-row transcript and 266,020 -> 240 on a 419-row one (the SAME 240, i.e. one row, not a sum),
+  // alignItemsAtEndPadding then grown to fill the viewport and UIScrollView clamping
+  // contentOffset to that one-viewport content.
+  // The mirror case is already refused: when data GREW, `positions[lastIndex]` is undefined and
+  // the `lastPosition !== void 0` guard below suppresses the publish. This makes the shrink
+  // direction symmetric. Nothing is lost by waiting - the reconciling pass walks from index 0,
+  // leaves `positions.length === data.length`, and republishes the exact total.
+  if (positions.length > data.length) {
+    return;
+  }
   const numColumns = (_a3 = peek$(ctx, "numColumns")) != null ? _a3 : 1;
   if (data.length === 0) {
-    addTotalSize(ctx, null, 0);
+    addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, 0);
   } else {
     const lastIndex = data.length - 1;
     const lastId = getId(state, lastIndex);
@@ -3350,12 +3544,12 @@ function updateTotalSize(ctx) {
             maxSize = size;
           }
         }
-        addTotalSize(ctx, null, lastPosition + maxSize);
+        addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, lastPosition + maxSize);
       } else {
         const lastSize = getItemSize(ctx, lastId, lastIndex, data[lastIndex]);
         if (lastSize !== void 0) {
           const totalSize = lastPosition + lastSize;
-          addTotalSize(ctx, null, totalSize);
+          addTotalSize(ctx, HAPPIER_TOTAL_SIZE_ABSOLUTE, totalSize);
         }
       }
     }
@@ -3418,7 +3612,7 @@ function updateItemPositions(ctx, dataChanged, {
   const shouldOptimize = !forceFullUpdate && !dataChanged && (optimizeForVisibleWindow || Math.abs(velocity) > 0 || state.scrollLength > 0 && lastScrollDelta > state.scrollLength);
   const maxVisibleArea = scrollBottomBuffered + 1e3;
   const useAverageSize = true;
-  const preferCachedSize = !doMVCP || dataChanged || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
+  const preferCachedSize = !doMVCP || state.scrollAdjustHandler.getAdjust() !== 0 || ((_b = peek$(ctx, "scrollAdjustPending")) != null ? _b : 0) !== 0;
   const notifyTotalSizeWhileCachingSizes = false;
   let currentRowTop = 0;
   let column = 1;
@@ -3447,7 +3641,7 @@ function updateItemPositions(ctx, dataChanged, {
       const prevIndex = startIndex - 1;
       const prevId = getId(state, prevIndex);
       const prevPosition = (_c = positions[prevIndex]) != null ? _c : 0;
-      const prevSize = (_d = sizesKnown.get(prevId)) != null ? _d : getItemSize(
+      const prevSize = getItemSize(
         ctx,
         prevId,
         prevIndex,
@@ -3465,7 +3659,25 @@ function updateItemPositions(ctx, dataChanged, {
   let breakAt;
   for (let i = startIndex; i < dataLength; i++) {
     if (shouldOptimize && breakAt !== void 0 && i > breakAt) {
-      didBreakEarly = true;
+      const staleRowTop = positions[i];
+      if (staleRowTop === void 0) {
+        didBreakEarly = true;
+        break;
+      }
+      const positionShift = currentRowTop - staleRowTop;
+      if (positionShift !== 0) {
+        for (let j = i; j < dataLength; j++) {
+          const stalePosition = positions[j];
+          if (stalePosition === void 0) {
+            break;
+          }
+          const shiftedPosition = stalePosition + positionShift;
+          positions[j] = shiftedPosition;
+          if (hasPositionListeners) {
+            notifyPosition$(ctx, idCache[j] !== void 0 ? idCache[j] : getId(state, j), shiftedPosition);
+          }
+        }
+      }
       break;
     }
     if (shouldOptimize && breakAt === void 0 && !scrollingTo && !dataChanged && currentRowTop > maxVisibleArea) {
@@ -3487,8 +3699,7 @@ function updateItemPositions(ctx, dataChanged, {
       column = 1;
       maxSizeInRow = 0;
     }
-    const knownSize = sizesKnown.get(id);
-    const size = knownSize !== void 0 ? knownSize : getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
+    const size = getItemSize(ctx, id, i, data[i], useAverageSize, preferCachedSize, notifyTotalSizeWhileCachingSizes);
     if (IS_DEV && needsIndexByKey) {
       if (indexByKeyForChecking.has(id)) {
         console.error(
@@ -4380,6 +4591,17 @@ function calculateItemsInView(ctx, params = {}) {
         }
       }
     }
+    if (startBuffered === null && endBuffered === null && dataLength > 0) {
+      const lastIndexInData = dataLength - 1;
+      const lastIdInData = idCache[lastIndexInData] != null ? idCache[lastIndexInData] : getId(state, lastIndexInData);
+      const lastTopInData = positions[lastIndexInData];
+      const lastSizeInData = sizes.get(lastIdInData) != null ? sizes.get(lastIdInData) : getItemSize(ctx, lastIdInData, lastIndexInData, data[lastIndexInData]);
+      if (lastTopInData !== void 0 && lastSizeInData !== void 0 && lastTopInData + lastSizeInData <= scroll) {
+        startBuffered = lastIndexInData;
+        startBufferedId = lastIdInData;
+        endBuffered = lastIndexInData;
+      }
+    }
     Object.assign(state, {
       endBuffered,
       endNoBuffer: visibleRange.endNoBuffer,
@@ -4583,7 +4805,7 @@ function calculateItemsInView(ctx, params = {}) {
     }
     maybeEmitFirstVisibleItemChanged(state, visibleRange.startNoBuffer);
     if (!queuedInitialLayout && !state.didContainersLayout) {
-      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(state, state.startBuffered, state.endBuffered) : checkAllSizesKnown(state, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(state, state.startBuffered, state.endBuffered);
+      const isInitialLayoutReady = hasActiveInitialScroll(state) ? checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered) : checkAllSizesKnown(ctx, state.startNoBuffer, state.endNoBuffer) || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered);
       if (isInitialLayoutReady) {
         setDidLayout(ctx);
         handleInitialScrollLayoutReady(ctx);
@@ -4769,11 +4991,16 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   } = state;
   if (!data) return { didMeasureUserScrollAnchorResetItem };
   const index = state.indexByKey.get(itemKey);
+  const itemData = index === void 0 ? void 0 : state.props.data[index];
+  if (index === void 0 && state.props.getItemSizeVersion) {
+    return { didMeasureUserScrollAnchorResetItem };
+  }
+  const didValidateItemSizeVersion = index !== void 0;
+  const prevSizeKnown = didValidateItemSizeVersion ? getKnownItemSize(ctx, itemKey, index, itemData) : state.sizesKnown.get(itemKey);
   if (getFixedItemSize) {
     if (index === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
-    const itemData = state.props.data[index];
     if (itemData === void 0) {
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4786,7 +5013,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       };
     }
     const size2 = resolvedMeasurementItem.fixedItemSize;
-    if (size2 !== void 0 && size2 === sizesKnown.get(itemKey)) {
+    if (size2 !== void 0 && size2 === prevSizeKnown) {
       updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
       return { didMeasureUserScrollAnchorResetItem };
     }
@@ -4794,8 +5021,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
   let needsRecalculate = !didContainersLayout;
   let shouldMaintainScrollAtEnd = false;
   let minIndexSizeChanged;
-  const prevSizeKnown = state.sizesKnown.get(itemKey);
-  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem);
+  const diff = updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion);
   const size = roundSize(horizontal ? sizeObj.width : sizeObj.height);
   if (diff !== 0) {
     minIndexSizeChanged = minIndexSizeChanged !== void 0 ? Math.min(minIndexSizeChanged, index) : index;
@@ -4804,7 +5030,8 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     if (!needsRecalculate && state.containerItemKeys.has(itemKey)) {
       needsRecalculate = true;
     }
-    if (prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
+    const hasExternalMaintainScrollAtEndIntent = !!(maintainScrollAtEnd && typeof maintainScrollAtEnd.isMaintainingScrollAtEnd === "function" && maintainScrollAtEnd.isMaintainingScrollAtEnd());
+    if (hasExternalMaintainScrollAtEndIntent || prevSizeKnown !== void 0 && Math.abs(prevSizeKnown - size) > 5) {
       shouldMaintainScrollAtEnd = true;
     }
     onItemSizeChanged == null ? void 0 : onItemSizeChanged({
@@ -4820,7 +5047,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     state.minIndexSizeChanged = state.minIndexSizeChanged !== void 0 ? Math.min(state.minIndexSizeChanged, minIndexSizeChanged) : minIndexSizeChanged;
   }
   updateOtherAxisSizeIfNeeded(ctx, sizeObj, horizontal);
-  if (didContainersLayout || checkAllSizesKnown(state, state.startBuffered, state.endBuffered)) {
+  if (didContainersLayout || checkAllSizesKnown(ctx, state.startBuffered, state.endBuffered)) {
     const canMaintainScrollAtEnd = shouldMaintainScrollAtEnd && !!(maintainScrollAtEnd == null ? void 0 : maintainScrollAtEnd.onItemLayout);
     return {
       didChange: diff !== 0,
@@ -4834,7 +5061,7 @@ function applyItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     didMeasureUserScrollAnchorResetItem
   };
 }
-function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
+function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem, didValidateItemSizeVersion = false) {
   var _a3, _b;
   const state = ctx.state;
   const {
@@ -4857,7 +5084,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
     fixedItemSize,
     itemType
   } : void 0;
-  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize);
+  const prevSize = getItemSize(ctx, itemKey, index, itemData, void 0, void 0, void 0, resolvedItemSize, didValidateItemSizeVersion);
   const rawSize = horizontal ? sizeObj.width : sizeObj.height;
   const prevSizeKnown = sizesKnown.get(itemKey);
   const size = Math.round(rawSize) ;
@@ -4877,6 +5104,7 @@ function updateOneItemSize(ctx, itemKey, sizeObj, resolvedMeasurementItem) {
       averages.avg = (averages.avg * averages.num + size) / (averages.num + 1);
       averages.num++;
     }
+    state.sizeContributions.set(itemKey, { itemType, size });
   }
   if (!prevSize || Math.abs(prevSize - size) > 0.1) {
     setSize(ctx, itemKey, size);
@@ -5338,7 +5566,8 @@ function processContainerLayout({ containerId, ctx, rectangle, ref, state }) {
   const axis = state.horizontal ? "width" : "height";
   const size = roundSize(rectangle[axis]);
   state.lastSize ? roundSize(state.lastSize[axis]) : void 0;
-  const coreKnownSize = listState.sizesKnown.get(currentItemKey);
+  const currentItemIndex = listState.indexByKey.get(currentItemKey);
+  const coreKnownSize = currentItemIndex === void 0 ? void 0 : getKnownItemSize(ctx, currentItemKey, currentItemIndex, listState.props.data[currentItemIndex]);
   const previousSize = coreKnownSize ;
   const applyLayout = () => {
     state.lastSize = layout;
@@ -6670,6 +6899,8 @@ function checkResetContainers(ctx, dataProp, { didColumnsChange = false } = {})
   if (didColumnsChange) {
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7089,6 +7320,13 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   const IMPERATIVE_SCROLL_SETTLE_MAX_WAIT_MS = 800;
   const IMPERATIVE_SCROLL_SETTLE_STABLE_FRAMES = 2;
   let imperativeScrollToken = 0;
+  let pendingReadinessFrame;
+  const cancelPendingReadinessFrame = () => {
+    if (pendingReadinessFrame !== void 0) {
+      cancelAnimationFrame(pendingReadinessFrame);
+      pendingReadinessFrame = void 0;
+    }
+  };
   const isSettlingAfterDataChange = () => !!state.didDataChange || !!state.didColumnsChange || state.queuedMVCPRecalculate !== void 0 || state.ignoreScrollFromMVCP !== void 0;
   const isScrollToIndexReady = (targetIndex, allowEmpty = false) => {
     var _a3;
@@ -7110,6 +7348,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const startedAt = Date.now();
     let stableFrames = 0;
     const check = () => {
+      pendingReadinessFrame = void 0;
       if (token !== imperativeScrollToken) {
         return;
       }
@@ -7123,9 +7362,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
         run();
         return;
       }
-      requestAnimationFrame(check);
+      pendingReadinessFrame = requestAnimationFrame(check);
     };
-    requestAnimationFrame(check);
+    cancelPendingReadinessFrame();
+    pendingReadinessFrame = requestAnimationFrame(check);
   };
   const runScrollRequest = (token, resolve, run, isReady = () => true) => {
     const runNow = () => {
@@ -7148,12 +7388,22 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
   };
   const startImperativeScroll = (resolve) => {
     var _a3;
+    cancelPendingReadinessFrame();
     const token = ++imperativeScrollToken;
     state.pendingScrollToEnd = void 0;
     (_a3 = state.pendingScrollResolve) == null ? void 0 : _a3.call(state);
     state.pendingScrollResolve = resolve;
     return token;
   };
+  state.disposeImperativeHandle = () => {
+    imperativeScrollToken++;
+    cancelPendingReadinessFrame();
+    cancelPendingNonAnimatedScrollCompletion(state);
+    state.pendingScrollToEnd = void 0;
+    const resolvePendingScroll = state.pendingScrollResolve;
+    state.pendingScrollResolve = void 0;
+    resolvePendingScroll == null ? void 0 : resolvePendingScroll();
+  };
   const runScrollWithPromise = (run, isReady = () => true) => new Promise((resolve) => {
     const token = startImperativeScroll(resolve);
     runScrollRequest(token, resolve, run, isReady);
@@ -7194,6 +7444,8 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     const mode = (_a3 = options == null ? void 0 : options.mode) != null ? _a3 : "sizes";
     state.sizes.clear();
     state.sizesKnown.clear();
+    state.itemSizeVersions.clear();
+    state.sizeContributions.clear();
     invalidateContainerFixedItemSizes(state);
     for (const key in state.averageSizes) {
       delete state.averageSizes[key];
@@ -7214,6 +7466,7 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
     (_b = state.triggerCalculateItemsInView) == null ? void 0 : _b.call(state, { forceFullItemPositions: true });
   };
   return {
+    cancelInitialScrollPreservation: () => cancelInitialScrollPreservation(ctx),
     clearCaches,
     flashScrollIndicators: () => refScroller.current.flashScrollIndicators(),
     getAnimatableRef: () => {
@@ -7251,7 +7504,10 @@ function createImperativeHandle(ctx, scheduleImperativeScrollCommit) {
       scroll: state.scroll,
       scrollLength: state.scrollLength,
       scrollVelocity: getScrollVelocity(state),
-      sizeAtIndex: (index) => state.sizesKnown.get(getId(state, index)),
+      sizeAtIndex: (index) => {
+        const key = getId(state, index);
+        return getKnownItemSize(ctx, key, index, state.props.data[index]);
+      },
       sizes: state.sizesKnown,
       start: state.startNoBuffer,
       startBuffered: state.startBuffered
@@ -7432,7 +7688,8 @@ function normalizeMaintainScrollAtEnd(value) {
   const normalizedTriggers = normalizeMaintainScrollAtEndOn(value.on, "on" in value);
   return {
     ...normalizedTriggers,
-    animated: (_a3 = value.animated) != null ? _a3 : false
+    animated: (_a3 = value.animated) != null ? _a3 : false,
+    isMaintainingScrollAtEnd: value.isMaintainingScrollAtEnd
   };
 }
 
@@ -7562,6 +7819,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     estimatedListSize,
     extraData,
     getFixedItemSize,
+    getEstimatedItemSize,
+    getItemSizeVersion,
     getItemType,
     horizontal,
     rtl,
@@ -7754,6 +8013,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         scrollTime: 0,
         sizes: /* @__PURE__ */ new Map(),
         sizesKnown: /* @__PURE__ */ new Map(),
+        itemSizeVersions: /* @__PURE__ */ new Map(),
+        sizeContributions: /* @__PURE__ */ new Map(),
         startBuffered: -1,
         startNoBuffer: -1,
         startReachedSnapshot: void 0,
@@ -7819,6 +8080,8 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
     drawDistance,
     estimatedItemSize,
     getFixedItemSize: useWrapIfItem(getFixedItemSize),
+    getEstimatedItemSize: useWrapIfItem(getEstimatedItemSize),
+    getItemSizeVersion: useWrapIfItem(getItemSizeVersion),
     getItemType: useWrapIfItem(getItemType),
     horizontal: !!horizontal,
     itemsAreEqual,
@@ -7860,7 +8123,10 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
   if (shouldResetFreshDataLayout) {
     resetInitialRenderState(ctx, {
       resetInitialScroll: !!initialScrollProp,
-      resetLayout: true
+      resetLayout: true,
+      // HAPPIER-MONOTONIC-DATASET-VISIBILITY: a transient empty projection of the same
+      // dataset still needs fresh layout, but must not turn an already painted list opaque.
+      preserveReadyToRender: !didDataKeyChangeLocal && state.didLoad
     });
   }
   const memoizedLastItemKeys = useMemo(() => {
@@ -8088,10 +8354,20 @@ var LegendListInner = typedForwardRef(function LegendListInner2(props, forwarded
         cancelAnimationFrame(state.queuedFullDrawDistancePrewarm);
         state.queuedFullDrawDistancePrewarm = void 0;
       }
+      if (state.animFrameCheckFinishedScroll !== void 0) {
+        cancelAnimationFrame(state.animFrameCheckFinishedScroll);
+        state.animFrameCheckFinishedScroll = void 0;
+      }
+      if (state.timeoutCheckFinishedScrollFallback !== void 0) {
+        clearTimeout(state.timeoutCheckFinishedScrollFallback);
+        state.timeoutCheckFinishedScrollFallback = void 0;
+      }
       for (const timeout of state.timeouts) {
         clearTimeout(timeout);
       }
       state.timeouts.clear();
+      state.disposeImperativeHandle == null ? void 0 : state.disposeImperativeHandle();
+      state.disposeImperativeHandle = void 0;
     };
   }, [state]);
   useLayoutEffect(() => {
diff --git a/node_modules/@legendapp/list/reanimated.d.ts b/node_modules/@legendapp/list/reanimated.d.ts
index 940da28..56e98f9 100644
--- a/node_modules/@legendapp/list/reanimated.d.ts
+++ b/node_modules/@legendapp/list/reanimated.d.ts
@@ -200,6 +200,12 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Version token for the item size. Changing it invalidates only that item’s cached size.
+     * An observed `undefined` token is distinct from an item that has not been observed yet.
+     */
+    getItemSizeVersion?: (item: ItemT, index: number) => Key | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
@@ -493,6 +499,12 @@ interface MaintainScrollAtEndOptions {
      * Whether maintainScrollAtEnd should animate when it scrolls to the end.
      */
     animated?: boolean;
+    /**
+     * Reports an authoritative semantic end-follow intent. Event-driven maintenance
+     * remains active while this is true even when late measurement moved the
+     * physical viewport beyond the proximity threshold.
+     */
+    isMaintainingScrollAtEnd?: () => boolean;
     /**
      * Which events should keep the list pinned to the end.
      * - If omitted, object values default to all triggers.
@@ -548,6 +560,10 @@ type LegendListState = {
     startBuffered: number;
 };
 type LegendListRef$1 = {
+    /**
+     * Cancels retained initial-scroll correction authority without scrolling.
+     */
+    cancelInitialScrollPreservation(): void;
     /**
      * Clears internal virtualization caches.
      * @param options - Cache clearing options.
diff --git a/node_modules/@legendapp/list/section-list.d.ts b/node_modules/@legendapp/list/section-list.d.ts
index 8da5a66..62fab27 100644
--- a/node_modules/@legendapp/list/section-list.d.ts
+++ b/node_modules/@legendapp/list/section-list.d.ts
@@ -256,6 +256,12 @@ interface LegendListSpecificProps<ItemT, TItemType extends string | undefined> {
      * In case items always have a fixed size, you can provide a function to return it.
      */
     getFixedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    getEstimatedItemSize?: (item: ItemT, index: number, type: TItemType) => number | undefined;
+    /**
+     * Version token for the item size. Changing it invalidates only that item’s cached size.
+     * An observed `undefined` token is distinct from an item that has not been observed yet.
+     */
+    getItemSizeVersion?: (item: ItemT, index: number) => Key | undefined;
     /**
      * Returns a stable item type used for pooling and size estimation.
      */
@@ -549,6 +555,12 @@ interface MaintainScrollAtEndOptions {
      * Whether maintainScrollAtEnd should animate when it scrolls to the end.
      */
     animated?: boolean;
+    /**
+     * Reports an authoritative semantic end-follow intent. Event-driven maintenance
+     * remains active while this is true even when late measurement moved the
+     * physical viewport beyond the proximity threshold.
+     */
+    isMaintainingScrollAtEnd?: () => boolean;
     /**
      * Which events should keep the list pinned to the end.
      * - If omitted, object values default to all triggers.
@@ -604,6 +616,10 @@ type LegendListState = {
     startBuffered: number;
 };
 type LegendListRef$1 = {
+    /**
+     * Cancels retained initial-scroll correction authority without scrolling.
+     */
+    cancelInitialScrollPreservation(): void;
     /**
      * Clears internal virtualization caches.
      * @param options - Cache clearing options.
